---
phase: 06-classic-api-load-run-validation
plan: 06-02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - test/op/SGFP4ClassicAPITest.cpp
  - test/CMakeLists.txt
autonomous: true
requirements: [SGINJ-05, SGINJ-06]

must_haves:
  truths:
    - "D-01: the test generates the base model at test time, runs the injection in-process, then loads the injected result via the classic API — fully self-contained, no committed .mnn/sidecar fixtures"
    - "D-04: the injected weight source is a 512x512 all-UNIFORM_64 v2 container of the Phase 5 demo-container lineage (byte-framing verified against the oracle); structured/quadtree containers stay in Phase 7"
    - "D-09: the harness is a new filtered run_test.out suite (test/op/SGFP4ClassicAPITest.cpp registered op/sgfp4/classic_api + op/sgfp4/classic_api_missing_sidecar), dodging the known FP4ModelTest.cpp full-build blocker"
    - "D-14: corrupted-payload-byte probing is explicitly SKIPPED (Phase 7 / SGINJ-08 territory) — must not appear in this plan's tasks"
    - "D-15: out-of-bounds-offset probing via hand-tampered artifacts is explicitly SKIPPED (injector provably can't produce bad offsets) — must not appear in this plan's tasks"
    - "Interpreter::createFromFile -> createSession -> runSession returns NO_ERROR on an injected artifact, and getSessionInputAll/getSessionOutputAll return the base-model names 'input'/'output' (ROADMAP success criterion 1, D-16)"
    - "Injected end-to-end output matches the FP32 baseline (base model whose weight is dequant_sgfp4_container_cpu of the same container) within rtol 1e-4 on CPU (ROADMAP success criterion 2, D-05..D-08)"
    - "The sidecar resolves via the op's literal externalPath with NO session-level setExternalFile, and a missing sidecar fails gracefully (runSession != NO_ERROR, no crash) (ROADMAP success criterion 3, D-13)"
  artifacts:
    - path: "test/op/SGFP4ClassicAPITest.cpp"
      provides: "SGFP4ClassicAPITest + SGFP4ClassicAPIMissingSidecarTest registered op/sgfp4/classic_api and op/sgfp4/classic_api_missing_sidecar"
      contains: "MNNTestSuiteRegister"
  key_links:
    - from: "test/op/SGFP4ClassicAPITest.cpp"
      to: "include/MNN/Interpreter.hpp"
      via: "createFromFile/createSession/runSession/getSessionInputAll/getSessionOutputAll"
      pattern: "createFromFile|createSession|runSession|getSessionInputAll|getSessionOutputAll"
    - from: "test/op/SGFP4ClassicAPITest.cpp"
      to: "tools/fp4/sgfp4_inject_core.hpp"
      via: "sgfp4_inject::run (in-process injection, D-12)"
      pattern: "sgfp4_inject::run"
    - from: "test/op/SGFP4ClassicAPITest.cpp"
      to: "include/MNN/SGFP4DequantUtils.hpp"
      via: "dequant_sgfp4_container_cpu oracle + sgfp4_is_v2_container + framing constants"
      pattern: "dequant_sgfp4_container_cpu|sgfp4_is_v2_container"
---

<objective>
Prove the Phase 5 injected artifact loads and runs through the classic `Interpreter`/`Session` API — `Interpreter::createFromFile` → `createSession` → `resizeSession` → `runSession` — the exact path downstream `SGProcessingManager::MNN_Tensor::Process()` uses (SGINJ-05), and that end-to-end inference matches an FP32 baseline on CPU with external-sidecar resolution via the op itself (SGINJ-06). This closes the workstream's core-value claim (classic path was never previously verified; Phase 5 only proved Express `Module::load`).

Purpose: Two `run_test.out` suites in one new file: `op/sgfp4/classic_api` (full happy path + named-I/O identification + FP32 parity) and `op/sgfp4/classic_api_missing_sidecar` (D-13 graceful-failure probe). The test is fully self-contained — base model built in-test with named I/O, 512×512 all-UNIFORM_64 container generated in-test, synthetic niche dir synthesized in-test, injection via the shared core header (D-12), no committed fixtures, no env-var skips.

Output: `test/op/SGFP4ClassicAPITest.cpp` (gated `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE`, registered `op/sgfp4/classic_api` + `op/sgfp4/classic_api_missing_sidecar`). Optional one-line `test/CMakeLists.txt` change only if MSVC `/W3` warnings appear (Open Q4).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/REQUIREMENTS.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/06-classic-api-load-run-validation/06-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/06-classic-api-load-run-validation/06-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/06-classic-api-load-run-validation/06-VALIDATION.md

# Read-only canonical references (the exact patterns to follow)
@demo/exec/pictureRecognition.cpp (classic-API flow: createFromFile -> createSession -> getSessionInput/Output -> resize -> run -> read)
@include/MNN/Interpreter.hpp (createFromFile/createSession/resizeSession/runSession/getSessionInputAll/getSessionOutputAll)
@source/core/Session.cpp (Session::run returns COMPUTE_SIZE_ERROR when mNeedResize; resize error propagation relevant to D-13)
@test/op/SGFP4InjectTest.cpp (tempPath helper at line 67, base-model _Input/_Const/_MatMul construction, MNNTestSuiteRegister, #ifdef gate)
@test/op/SGFP4DequantFixtures.h (fixture precedent — NOT usable for 512×512 per Pitfall 5)
@test/TestUtils.h (checkVectorByRelativeError)
@include/MNN/SGFP4DequantUtils.hpp (kSGFP4Magic/kSGFP4Version/kSGFP4FixedHeaderSize/kSGFP4RecordCountOffset/kSGFP4RecordOffsetTableStart/kSGFP4LayoutUniform64/sgfp4_align16/sgfp4_read_u32_le/sgfp4_is_v2_container/dequant_sgfp4_container_cpu)
@tools/fp4/encode_sgfp4.py (lines 391-460: encode_macroblock + encode_container — the framing the in-test generator mirrors)
@tools/fp4/sgfp4_inject_core.hpp (created by 06-01; the in-process injection entry sgfp4_inject::run)
@tools/fp4/sha256.hpp (sgfp4::sha256_hex for the niche-dir manifest)
@tools/fp4/CMakeLists.txt (_CRT_SECURE_NO_WARNINGS precedent)
@test/CMakeLists.txt (glob-recurses *.cpp; known FP4ModelTest.cpp full-build blocker)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SGFP4ClassicAPITest — in-test fixture + injection + classic load/run + named-I/O + FP32 baseline (op/sgfp4/classic_api)</name>
  <files>test/op/SGFP4ClassicAPITest.cpp</files>
  <read_first>
    - test/op/SGFP4InjectTest.cpp (tempPath helper lines 67-70, _Input/_Const/_MatMul graph construction lines 160-180, MNNTestSuiteRegister at file bottom, #ifdef MNN_SUPPORT_TRANSFORMER_FUSE at line 17)
    - demo/exec/pictureRecognition.cpp (lines 32-130: the canonical createFromFile → createSession → getSessionInput/Output → resizeSession → runSession → copyToHostTensor flow)
    - include/MNN/Interpreter.hpp (createFromFile, createSession, resizeSession (returns VOID), runSession, getSessionInputAll/getSessionOutputAll/getSessionInput/getSessionOutput)
    - include/MNN/SGFP4DequantUtils.hpp (constants kSGFP4Magic/kSGFP4Version/kSGFP4FixedHeaderSize/kSGFP4RecordCountOffset/kSGFP4RecordOffsetTableStart/kSGFP4RecordOffsetEntrySize/kSGFP4LayoutUniform64/kSGFP4LeafHeaderScaleShift/kSGFP4LeafHeaderBiasMask/sgfp4_align16/sgfp4_read_u32_le/sgfp4_is_v2_container/dequant_sgfp4_container_cpu)
    - tools/fp4/encode_sgfp4.py (lines 391-460: encode_macroblock + encode_container — the exact framing to mirror for the degenerate all-UNIFORM_64 case)
    - tools/fp4/sgfp4_inject_core.hpp (the sgfp4_inject::run signature and CLI flag names --model/--niche-dir/--output)
    - tools/fp4/sha256.hpp (sgfp4::sha256_hex signature)
    - test/TestUtils.h (checkVectorByRelativeError signature)
  </read_first>
  <action>
    Create `test/op/SGFP4ClassicAPITest.cpp` with the whole file behind `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE` (mandatory — the SGFP4 suites only execute under this flag). Includes: `MNN_generated.h`, `MNN/SGFP4DequantUtils.hpp`, `MNN/expr/Expr.hpp`, `MNN/expr/ExprCreator.hpp`, `MNN/Interpreter.hpp`, `MNN/Tensor.hpp`, `MNNTestSuite.h`, `TestUtils.h`, and `fp4/sgfp4_inject_core.hpp` (resolves via the global `tools/` include dir). `using namespace MNN::Express;`.

    Implement file-static helpers first (no test class yet):
    - `tempPath(const char* prefix, const char* suffix)` — copy the exact helper from `SGFP4InjectTest.cpp:67` (unique temp name pattern `sgfp4_classic_<prefix>_<time>_<rand>`).
    - `buildContainerUniform64(std::vector<uint8_t>& out)` — generate a 512×512 all-`UNIFORM_64` v2 container in-test, mirroring `encode_sgfp4.py:391-460` for the degenerate case. Use the header constants (NOT re-derived literals): header = `kSGFP4Magic` u32 LE + `kSGFP4Version` byte at `kSGFP4VersionByteOffset` + record count `B=64` u32 LE at `kSGFP4RecordCountOffset` (padded to `kSGFP4FixedHeaderSize`); offset table = 64 × u32 LE starting at `kSGFP4RecordOffsetTableStart`; record region starts at `sgfp4_align16(kSGFP4RecordOffsetTableStart + 64 * kSGFP4RecordOffsetEntrySize)`; 64 records in linear order (record 0 fills output[0..4096), record 1 fills [4096..8192), … per the sequential-linear decode order), each record = 4-byte sb_header (u32 LE, layout enum in bits 0-2; `kSGFP4LayoutUniform64` = value 0) + 4-byte leaf header (u32 LE, scale/bias bits) + 8 bytes alignment padding + 2048-byte mode-0 nibble payload (4096 elements / 8 elements-per-word), mirroring `encode_macroblock`'s `struct.pack('<I', sb_header)` / `struct.pack('<I', header)` + `align16`-pad (16-byte aligned record, 2064 bytes total). Each `record_offsets[b]` = absolute byte offset of record b. Fill every payload nibble with a constant chosen 4-bit code so the decode is deterministic (e.g. code 0x0). Keep framing minimal — this is the one place the phase writes framing code; the oracle validates it.
    - `writeNicheDir(const std::vector<uint8_t>& containerBytes, const std::string& dir)` — write `<dir>/phase6_fixture.sgfp4` (the container bytes) and `<dir>/manifest.json` with the JSON shape `{"fp4_binary":{"path":"phase6_fixture.sgfp4","sha256":"<sgfp4::sha256_hex(containerBytes)>","stats":{"shape":[512,512]}}}` (D-11). Build the JSON string with the actual hex digest inline (no JSON library needed in the test — write the exact literal text the tool's rapidjson parser expects).
    - `buildNamedBaseModel(const std::vector<float>& weight, const std::string& path)` — build `_Input({1, 512}, NHWC, halide_type_of<float>())` named `"input"`, `_Const(weight.data(), {512, 512}, NHWC, halide_type_of<float>())` named `"weight"`, `_MatMul(input, weight)` named `"output"`, then `Variable::save({out}, path.c_str())` (D-16/D-02).
    - `fillLcgInput(std::vector<float>& input)` — deterministic LCG: `uint32_t state = 0x9E3779B9u;` then for each of 512 elements `state = state * 1664525u + 1013904223u; input[i] = (float)(state >> 8) / 16777216.0f;` (D-08; identical values feed both injected and baseline sessions).

    Then define `class SGFP4ClassicAPITest : public MNNTestCase` with `bool run(int precision)` implementing the full happy path in order:
    1. Generate `containerBytes` via `buildContainerUniform64`; assert `sgfp4_is_v2_container(containerBytes.data(), containerBytes.size())` is true.
    2. Decode oracle: `std::vector<float> oracleBuf(512*512);` call `dequant_sgfp4_container_cpu(containerBytes.data(), containerBytes.size(), oracleBuf.data(), 512*512)` and assert it returns true (this resolves research A1 — the generated container is a valid 512×512 container; the oracle is also the FP32 weight source, D-06).
    3. Build the base model with weight = `oracleBuf` via `buildNamedBaseModel(oracleBuf, basePath)` (basePath from `tempPath("base_", ".mnn")`). This same `basePath` IS the FP32 baseline model (D-05/D-06).
    4. Write the synthetic niche dir via `writeNicheDir(containerBytes, nicheDir)` (nicheDir from a portable temp-dir helper — a unique directory path, no `<filesystem>`; use the `tempPath` prefix pattern plus a manual directory create via the platform API already used in `sgfp4_inject_core.hpp`, or `GetTempPathA`/`_mkdir` on Windows with `mkdir` fallback).
    5. Inject in-process via the shared core (D-12, absolute paths per Pitfall 3): `const char* argv[] = {"sgfp4_inject", "--model", basePath.c_str(), "--niche-dir", nicheDir.c_str(), "--output", outPath.c_str(), nullptr};` assert `sgfp4_inject::run(5, argv) == 0`. `outPath` is absolute (`tempPath("out_", ".mnn")`), so the sidecar `outPath + ".weight"` is absolute.
    6. Classic load + named-I/O identification (SGINJ-05, D-16): `std::shared_ptr<Interpreter> net(Interpreter::createFromFile(outPath.c_str()), Interpreter::destroy);` assert non-null; `ScheduleConfig cfg; cfg.type = MNN_FORWARD_CPU;` `auto session = net->createSession(cfg);` assert non-null. `const auto& inAll = net->getSessionInputAll(session);` `const auto& outAll = net->getSessionOutputAll(session);` assert `inAll.count("input") == 1` AND `outAll.count("output") == 1` (if these keys differ after injection — research A2/Open Q2 — the test fails; record the actual keys via `MNN_ERROR` and correct the asserted literals only if the dump proves a suffix is appended, then keep the assert against the actual surviving name).
    7. Feed + run (D-08): `auto inputTensor = net->getSessionInput(session, nullptr);` `net->resizeSession(session);` (returns void — do NOT read a code from it, Pitfall 1) then `::memcpy(inputTensor->host<float>(), inputVals.data(), 512 * sizeof(float));` `ErrorCode code = net->runSession(session);` assert `code == NO_ERROR`.
    8. Read output: `auto outputTensor = net->getSessionOutput(session, nullptr);` `std::shared_ptr<Tensor> outUser(new Tensor(outputTensor, Tensor::CAFFE));` `outputTensor->copyToHostTensor(outUser.get());` `const float* got = outUser->host<float>();` (512 floats).
    9. FP32 baseline (D-05/D-06/D-07): build a second classic session over `basePath` (the pre-injection model whose weight is already the decoded container): `std::shared_ptr<Interpreter> baseNet(Interpreter::createFromFile(basePath.c_str()), Interpreter::destroy);` same `ScheduleConfig` `MNN_FORWARD_CPU`, `createSession`, `getSessionInput(session, nullptr)`, `resizeSession`, memcpy the SAME `inputVals`, `runSession`, `copyToHostTensor` → `const float* baseline = baseUser->host<float>();`.
    10. Compare: `return checkVectorByRelativeError<float>(got, baseline, 512, 1e-4f);` (D-07; rtol 1e-4, NOT bit-exact).
    11. Cleanup: `std::remove` basePath/outPath/sidecar/niche files; return the comparison result.

    The test MUST NOT call any `setExternalFile` on the Interpreter or session (SGINJ-06/SC3 — the sidecar resolves via the op's literal `externalPath`). No `createFromBuffer` (D-03). No committed fixture bytes, no env-var skip (D-10). Every non-obvious literal (512, 64, 2048, record count, nibble width) must be a named constant or the existing `kSGFP4*` header constant.
  </action>
  <acceptance_criteria>
    - `test/op/SGFP4ClassicAPITest.cpp` exists and contains `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE` (grep).
    - The file contains `MNNTestSuiteRegister(SGFP4ClassicAPITest, "op/sgfp4/classic_api")` (grep for the exact string).
    - The file contains each classic-API token: `Interpreter::createFromFile`, `createSession`, `runSession`, `getSessionInputAll`, `getSessionOutputAll`, `resizeSession` (grep each).
    - The file contains `setName("input")` and `setName("output")` (D-16 named I/O; grep).
    - The file contains `sgfp4_inject::run(` (D-12 in-process injection; grep) and `dequant_sgfp4_container_cpu` (oracle/baseline) and `checkVectorByRelativeError` (D-07; grep).
    - The file does NOT contain `setExternalFile` (SGINJ-06/SC3 — no session-level external file on the classic path; grep -c == 0).
    - The file contains `sgfp4::sha256_hex` (D-11 manifest digest; grep).
    - `.\run_test.out op/sgfp4/classic_api` exits 0 and prints a PASS line (filtered-suite build per the FP4ModelTest.cpp workaround).
  </acceptance_criteria>
  <verify>
    <!-- Windows/MSVC build dir (.build), PowerShell; filtered-suite build uses the FP4ModelTest.cpp exclusion workaround (STATE.md). -->
    <automated>Set-Location .build; .\run_test.out op/sgfp4/classic_api</automated>
  </verify>
  <done>The injected artifact loads and runs through the classic API (createFromFile → createSession → resizeSession → runSession = NO_ERROR), getSessionInputAll/getSessionOutputAll return the named input/output, and the injected output matches the decoded-container FP32 baseline within rtol 1e-4 on CPU — with no session-level setExternalFile.</done>
</task>

<task type="auto">
  <name>Task 2: SGFP4ClassicAPIMissingSidecarTest — D-13 graceful-failure probe (op/sgfp4/classic_api_missing_sidecar)</name>
  <files>test/op/SGFP4ClassicAPITest.cpp</files>
  <read_first>
    - test/op/SGFP4ClassicAPITest.cpp (the Task 1 file — reuse its file-static helpers buildContainerUniform64/writeNicheDir/buildNamedBaseModel/fillLcgInput/tempPath)
    - source/core/Session.cpp (lines 238-243: Session::run returns COMPUTE_SIZE_ERROR when mNeedResize; lines 288-295: getSessionInfo RESIZE_STATUS)
    - source/core/Interpreter.cpp (lines 483-497: resizeSession returns VOID and discards Session::resize ErrorCode — the D-13 observability trap)
    - source/backend/cpu/CPUSGFP4Dequant.cpp (lines 48-77: queryFileSize failure path returns NOT_SUPPORT on missing sidecar)
    - include/MNN/Interpreter.hpp (getSessionInfo / RESIZE_STATUS enum for the optional secondary observable)
  </read_first>
  <action>
    Extend `test/op/SGFP4ClassicAPITest.cpp` (same `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE` block, below the first class) with a second class `SGFP4ClassicAPIMissingSidecarTest : public MNNTestCase` registered `MNNTestSuiteRegister(SGFP4ClassicAPIMissingSidecarTest, "op/sgfp4/classic_api_missing_sidecar")`, with `bool run(int precision)`:

    1. Reuse the Task 1 helpers to produce an injected artifact exactly as the happy path does: generate `containerBytes` (buildContainerUniform64), decode oracle, `buildNamedBaseModel(oracleBuf, basePath)`, `writeNicheDir(containerBytes, nicheDir)`, then `sgfp4_inject::run(5, argv) == 0` → `outPath` + sidecar `outPath + ".weight"`.
    2. Delete the sidecar: `std::remove((outPath + ".weight").c_str());` (D-13 — probe the missing-sidecar failure).
    3. Classic load still succeeds on the (valid) model: `std::shared_ptr<Interpreter> net(Interpreter::createFromFile(outPath.c_str()), Interpreter::destroy);` assert non-null; `auto session = net->createSession(cfg)` (MNN_FORWARD_CPU) assert non-null.
    4. `net->resizeSession(session);` — return value intentionally ignored (it is void and swallows the resize error, Pitfall 1).
    5. Probe at run: `ErrorCode code = net->runSession(session);` — assert `code != NO_ERROR` (the missing sidecar makes `CPUSGFP4Dequant::onResize` return `NOT_SUPPORT`, leaving `mNeedResize` true, so `Session::run` returns `COMPUTE_SIZE_ERROR`). The assertion is `runSession != NO_ERROR` AND no crash. If `runSession` unexpectedly returns `NO_ERROR` (research A4), fall back to `int status = 0; net->getSessionInfo(session, Interpreter::RESIZE_STATUS, &status);` and assert `status != 0` — but the primary observable asserted is the non-zero `runSession` code.
    6. Cleanup temp files and return the assertion result.

    Do NOT call `setExternalFile`; do NOT feed input or attempt a successful run in this probe. The point is to document that the failure surfaces as a non-zero `runSession` code (not a crash) for the downstream `SGProcessingManager` team (whose `Process()` path has an unchecked-nullptr deref).
  </action>
  <acceptance_criteria>
    - `test/op/SGFP4ClassicAPITest.cpp` contains `MNNTestSuiteRegister(SGFP4ClassicAPIMissingSidecarTest, "op/sgfp4/classic_api_missing_sidecar")` (grep for the exact string).
    - The file contains `std::remove(` applied to the sidecar path (grep `std::remove`) and an assertion comparing `runSession`'s return to `NO_ERROR` (grep `runSession`).
    - The file contains `resizeSession` with a code comment noting the error is swallowed there and must be observed at `runSession` (Pitfall 1; grep `resizeSession`).
    - `.\run_test.out op/sgfp4/classic_api_missing_sidecar` exits 0 and prints a PASS line.
    - `.\run_test.out op/sgfp4/` (all SGFP4 suites incl. the new two) exits 0.
  </acceptance_criteria>
  <verify>
    <!-- Windows/MSVC build dir (.build), PowerShell; filtered-suite build per the FP4ModelTest.cpp workaround. -->
    <automated>Set-Location .build; .\run_test.out op/sgfp4/classic_api_missing_sidecar; .\run_test.out op/sgfp4/</automated>
  </verify>
  <done>With the sidecar absent, the classic-API load/create succeeds but runSession returns a non-zero ErrorCode (no crash), documenting graceful failure for the downstream SGProcessingManager path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-test container bytes → oracle + injector | Generated `.sgfp4` bytes cross into `dequant_sgfp4_container_cpu` and `sgfp4_is_v2_container` (trusted: produced in-test, not external input) |
| synthetic niche dir → injector | `manifest.json` + `phase6_fixture.sgfp4` cross into `sgfp4_inject::run` (exercises the tool's real parse+sha256+version-gate contract, D-11) |
| injected `.mnn` + sidecar → classic loader | The artifact produced by the shared core is re-consumed via `Interpreter::createFromFile`/`Session` (the thing being proven) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Reliability | missing sidecar | mitigate | D-13 probe asserts `runSession != NO_ERROR` and no crash; `CPUSGFP4Dequant::onResize` gates on `queryFileSize` failure (already in place) |
| T-06-03 | Integrity | container framing | mitigate | In-test generated bytes validated by `sgfp4_is_v2_container` + `dequant_sgfp4_container_cpu` round-trip (oracle is also the baseline source) |
| T-06-04 | Spoofing | niche-dir manifest | mitigate | `sgfp4::sha256_hex` computed over the exact container bytes and written into `manifest.json`; the injector re-verifies sha256 before injection |
| T-06-05 | Logic | named-I/O identification | mitigate | D-16 asserts `getSessionInputAll`/`getSessionOutputAll` return the base-model names; a mismatch fails the test and surfaces the flagged friction |
| T-06-SC | Tampering | package installs | accept | No npm/pip/cargo installs; in-repo test + shared core only |

</threat_model>

<verification>
- `.\run_test.out op/sgfp4/classic_api` exits 0 (classic load/create/run + named-I/O + FP32 baseline within 1e-4).
- `.\run_test.out op/sgfp4/classic_api_missing_sidecar` exits 0 (graceful non-zero runSession, no crash).
- `.\run_test.out op/sgfp4/` exits 0 (no regression to the pre-existing SGFP4 suites).
- `cmake --build . --target sgfp4_inject.out` still builds (the D-12 refactor target from 06-01 remains green).
</verification>

<success_criteria>
- Success criterion 1 (SGINJ-05): `createFromFile → createSession → runSession` succeeds on the injected artifact with `getSessionInputAll`/`getSessionOutputAll` returning `"input"`/`"output"`.
- Success criterion 2 (SGINJ-06): injected output matches the decoded-container FP32 baseline within rtol 1e-4 on CPU.
- Success criterion 3 (SGINJ-06): the sidecar resolves via the op's literal `externalPath` with no session-level `setExternalFile`, and a missing sidecar fails gracefully at `runSession`.
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/06-classic-api-load-run-validation/06-02-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (Plan 06-02)

New symbols created by this plan (excluded from plan-review-convergence drift verification):

| Symbol | Kind | Location |
|--------|------|----------|
| `SGFP4ClassicAPITest` | test class | `test/op/SGFP4ClassicAPITest.cpp` |
| `SGFP4ClassicAPIMissingSidecarTest` | test class | `test/op/SGFP4ClassicAPITest.cpp` |
| `"op/sgfp4/classic_api"` | test registration string | `test/op/SGFP4ClassicAPITest.cpp` |
| `"op/sgfp4/classic_api_missing_sidecar"` | test registration string | `test/op/SGFP4ClassicAPITest.cpp` |
| `tempPath(const char*, const char*)` | file-static helper | `test/op/SGFP4ClassicAPITest.cpp` |
| `buildContainerUniform64(std::vector<uint8_t>&)` | file-static helper | `test/op/SGFP4ClassicAPITest.cpp` |
| `writeNicheDir(const std::vector<uint8_t>&, const std::string&)` | file-static helper | `test/op/SGFP4ClassicAPITest.cpp` |
| `buildNamedBaseModel(const std::vector<float>&, const std::string&)` | file-static helper | `test/op/SGFP4ClassicAPITest.cpp` |
| `fillLcgInput(std::vector<float>&)` | file-static helper | `test/op/SGFP4ClassicAPITest.cpp` |

New file paths created:
- `test/op/SGFP4ClassicAPITest.cpp`

Modified file paths:
- `test/CMakeLists.txt` (DISCRETIONARY only — add `_CRT_SECURE_NO_WARNINGS` to `run_test.out` if MSVC `/W3` warnings appear from the included `sgfp4_inject_core.hpp`; Open Q4. No change required otherwise.)

No runtime-code changes (`CPUSGFP4Dequant`, `ShapeSGFP4Dequant`, `SGFP4DequantUtils.hpp` decode all untouched), no new schema fields, no new OpTypes.
