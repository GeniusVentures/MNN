---
phase: 03-vulkan-decode-uniform-layouts
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - test/op/SGFP4VulkanDequantTest.cpp
autonomous: true
requirements: [SGV2-14, SGV2-12]

must_haves:
  truths:
    - "Each uniform-layout fixture decodes through BOTH dequant_sgfp4_container_cpu AND a Vulkan session through the new Execution (same sidecar file, same descriptor), and the two agree within float tolerance"
    - "The test degrades gracefully (skip with clear message, suite still passes) when no Vulkan device is available"
    - "The parity test passes via ./run_test.out on this machine's GPU (RTX 4070 Ti SUPER)"
  artifacts:
    - path: "test/op/SGFP4VulkanDequantTest.cpp"
      provides: "Dual-backend parity test (SGV2-14) + end-to-end SGV2-12/13 GPU exercise"
      min_lines: 120
  key_links:
    - from: "SGFP4VulkanDequantTest"
      to: "sgfp4_fixtures::kFixtures / SGFP4DequantFixtures.h"
      via: "fixture loop (uniform-only filter)"
      pattern: "kFixtures|SGFP4DequantFixtures"
    - from: "SGFP4VulkanDequantTest"
      to: "dequant_sgfp4_container_cpu"
      via: "CPU reference decode at test time"
      pattern: "dequant_sgfp4_container_cpu"
    - from: "SGFP4VulkanDequantTest"
      to: "Module::load + MNN_FORWARD_VULKAN runtime"
      via: "runSgfp4Module pattern with op->externalPath set directly (Pitfall 4)"
      pattern: "externalPath|MNN_FORWARD_VULKAN"
---

<objective>
Write the dual-backend parity test: drive the identical committed fixtures through both the CPU reference decode and a Vulkan module session (real sidecar file, `{magic, offset, size}` descriptor, `op->externalPath` set directly), comparing outputs with `checkVectorByRelativeError` (SGV2-14, per D-02/D-07). This test is also the first real GPU execution of the shader + Execution from plans 03-02/03-03, closing SGV2-12/13 end-to-end.

Purpose: The phase's measurable success criterion — Vulkan decode matches the CPU reference within float tolerance, verified via `./run_test.out`.
Output: One new auto-globbed test file (picked up by `test/CMakeLists.txt` GLOB_RECURSE).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-PATTERNS.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-03-SUMMARY.md
@test/op/VulkanFP4DequantTest.cpp
@test/op/SGFP4DequantTest.cpp
@test/op/SGFP4DequantFixtures.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the dual-backend parity test (SGV2-14)</name>
  <files>test/op/SGFP4VulkanDequantTest.cpp</files>
  <read_first>
    - test/op/VulkanFP4DequantTest.cpp (whole harness: MNN_SUPPORT_TRANSFORMER_FUSE ifdef gate + includes lines 10-20, availability guard + ScheduleConfig/backendConfig lines 66-81, MNNTestSuiteRegister line 338)
    - test/op/SGFP4DequantTest.cpp (runSgfp4Module lines 384-470 — the 0-input module pattern: OpT construction, externalPath set DIRECTLY on the OpT, Expr::create(op, {}), Variable::save, Module::load(buffer,...,rtmgr), onForward({}) + readMap<float>; sidecar temp-file write lines 363-381; kFixtureRelativeTolerance=1e-4f lines 30-31)
    - test/op/SGFP4DequantFixtures.h (Fixture struct + kFixtures table lines 123-140: 11 uniform-enum fixtures — the 8 mode×layout combos + mode0_uniform16_b3 + mixed_allsplit(layout=5) + uniform_collapse(layout=0); mixed_asymmetric has layout==4 = true MIXED)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md (Pitfall 2 precision/tolerance, Pitfall 4 zero-input module, parity skeleton in Code Examples)
  </read_first>
  <action>
    1. Structure: wrap the file in `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE` with the same include set as VulkanFP4DequantTest.cpp plus `#include "SGFP4DequantFixtures.h"` and the SGFP4DequantUtils.hpp include for the CPU reference. Register with `MNNTestSuiteRegister(SGFP4VulkanDequantTest, "op/sgfp4/vulkan_uniform_parity")` (keeps the Phase-1 `op/sgfp4/` namespace; quick-verify `./run_test.out "op/sgfp4/"` picks it up).
    2. run(int precision): availability guard first — `MNN::MNNGetExtraRuntimeCreator(MNN_FORWARD_VULKAN)`; null → MNN_PRINT("Vulkan backend not available — skipping SGFP4 Vulkan parity test\n") + return true (D-07 graceful skip).
    3. Session config: `config.type = MNN_FORWARD_VULKAN`; `backendConfig.precision = MNN::BackendConfig::Precision_High` (forces FP32 tensors + the FP32 shader variant → tight tolerance works, Pitfall 2); `backendConfig.memory = MNN::BackendConfig::Memory_High`; rtmgr via `Executor::RuntimeManager::createRuntimeManager(config)`.
    4. Fixture loop over sgfp4_fixtures::kFixtures with an EXPLICIT named uniform-only filter: skip fixtures whose `layout == kSGFP4LayoutMixed` (i.e. enum 4 — only `mixed_asymmetric` in the current table). Note in a comment: `mixed_allsplit` (layout 5/FULL_4X4 collapse) and `uniform_collapse` (layout 0/UNIFORM_64 collapse) are uniform-enum containers per the Phase-2 collapse rule and ARE parity targets. Do not use fixture.mode == -1 as the filter — mixed_allsplit/uniform_collapse carry mode -1 but decode fine as uniform layouts (the per-leaf header carries the real mode).
    5. Per fixture: (a) write fixture.container bytes to a temp sidecar file (std::ofstream binary|trunc — clone SGFP4DequantTest lines 363-372); (b) CPU reference: dequant_sgfp4_container_cpu(fixture.container, fixture.containerSize, cpuOut.data(), fixture.expectedCount) — assert it returns true; (c) also assert fixture.expected matches cpuOut at 1e-4 (guards against fixture drift); (d) Vulkan: clone runSgfp4Module exactly — OpT with type OpType_SGFP4Dequant, main.type OpParameter_SGFP4DequantParam, param->magic = MNN::kSGFP4Magic, param->external = {0, (int64_t)fixture.containerSize}, param->dims = {fixture.dimO, fixture.dimI}, and `op->externalPath = sidecarPath` set DIRECTLY on the OpT (Phase-1 pitfall: rtmgr->setExternalFile does not populate this op type); Expr::create(op.get(), {}) → Variable::save → Module::load(buffer, rtmgr) → onForward({}) → outputs[0]->readMap<float>(); (e) compare GPU vs CPU reference with checkVectorByRelativeError<float> at kFixtureRelativeTolerance = 1e-4f; on mismatch MNN_ERROR with the fixture name and return false.
    6. Delete the temp sidecar at scope exit (or use a unique per-fixture filename pattern under the temp dir consistent with the Phase-1 test).
    7. Optional relaxed second pass (D-06/Pitfall 2 coverage): a default-precision (non-Precision_High) rtmgr run comparing at rtol 2e-3 to exercise the FP16 shader variant on a GPU that reports FP16 support — keep it as a separate loop guarded so the tight pass already failed-fast. If the device selects FP32 anyway (useFP16() false), the pass trivially re-runs FP32 at looser tolerance — harmless.
  </action>
  <verify>
    <automated>cmake --build .build --target run_test.out && ./.build/run_test.out.exe "op/sgfp4/" (all sgfp4 suites incl. the new vulkan_uniform_parity pass on the GPU)</automated>
  </verify>
  <acceptance_criteria>
    - `run_test.out.exe "op/sgfp4/vulkan_uniform_parity"` passes (10 uniform fixtures + optional second pass), running a real GPU decode on the fixtures
    - The Vulkan path uses the sidecar file + `{0, size}` descriptor + direct op->externalPath (grep: externalPath, SGFP4DequantParam, FileLoader-free test side but real module pipeline) — GPU exercised through production external-container plumbing (D-02)
    - Comparison is checkVectorByRelativeError at 1e-4 against the CPU reference decode (both directions of truth verified: fixtures↔CPU and CPU↔GPU)
    - Removing/renaming the GPU (simulated by the availability guard) results in skip + pass, per the nullptr-creator branch
    - Test file is inside the `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE` gate like its siblings
    - `run_test.out.exe "op/fp4"` and `"op/vulkan/fp4_dequant_correctness"` still pass (E2M1 additive-not-replacement guard)
  </acceptance_criteria>
  <done>SGV2-14 complete: uniform-layout CPU/Vulkan parity within float tolerance verified via run_test.out on the physical GPU, with graceful skip on Vulkan-less machines.</done>
</task>

<task type="auto">
  <name>Task 2: Regression sweep + phase verification gate</name>
  <files>test/op/SGFP4VulkanDequantTest.cpp (fix-ups only if regressions surface)</files>
  <read_first>
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-VALIDATION.md (Sampling Rate + Phase gate)
    - .planning/workstreams/sgfp4-pivot/phases/01-affine-dual-mode-decode-core-cpu-uniform-layouts/deferred-items.md (FP4ModelTest stub workaround if the full build is attempted)
  </read_first>
  <action>
    1. Run the merge-wave sweep per 03-VALIDATION.md: `./.build/run_test.out.exe "op/sgfp4/"` + `./.build/run_test.out.exe "op/fp4"` + `./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness"` + the artifact grep set from plan 03-02 (`sgfp4_dequant` counts in the three generated files).
    2. Run the full suite if buildable (`./.build/run_test.out.exe`); if the FP4ModelTest.cpp blocker fires on incremental linking of unrelated targets, apply the Phase-1 temp-stub workaround (restore byte-for-byte immediately, never commit) — otherwise skip the full-suite step and note it in the SUMMARY for /gsd-verify-work.
    3. Confirm the working tree contains exactly this phase's intended changes (git status): sgfp4_dequant.comp, macro.json, 3 regenerated artifacts, VulkanSGFP4Dequant.{hpp,cpp}, SGFP4VulkanDequantTest.cpp — and nothing else (no E2M1 file changes, no generated-file hand edits).
  </action>
  <verify>
    <automated>./.build/run_test.out.exe "op/sgfp4/" && ./.build/run_test.out.exe "op/fp4" && ./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness" — all green</automated>
  </verify>
  <acceptance_criteria>
    - All three filtered suites report pass
    - Regenerated-artifact grep counts unchanged from plan 03-02 (≥4 / ==4 / ==2)
    - `git status` shows only the 8 intended phase files (plus planning docs)
    - Any temp stub restored: `git diff --exit-code test/op/FP4ModelTest.cpp`
  </acceptance_criteria>
  <done>Phase 3 verification gate green: parity test passing, E2M1 regression guard clean, working tree contains exactly the phase's additive changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-side fixture bytes → temp sidecar | Trusted committed data; exercises the same untrusted-file posture as production (probe + validate reject paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | DoS | test machine without Vulkan device | accept | Graceful skip (D-07): nullptr creator check returns true with a clear MNN_PRINT — suite never fails from a missing device |
| T-03-SC | Tampering | package installs | accept | No registry packages in this phase (C++/GLSL in-tree only); Wave-0 system packages covered by T-03-00a in plan 03-01 |
</threat_model>

<verification>
- `run_test.out.exe "op/sgfp4/"` green including vulkan_uniform_parity
- `run_test.out.exe "op/fp4"` + `"op/vulkan/fp4_dequant_correctness"` green (additivity)
- Artifact greps intact; working tree minimal and additive
</verification>

<success_criteria>
SGV2-14 delivered; phase-3 quick-verify command green; the E2M1 contract (additive, not a replacement) verified by regression suites.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Symbols / Names |
|----------|------|-----------------|
| `test/op/SGFP4VulkanDequantTest.cpp` | new MNNTestSuite test | `class SGFP4VulkanDequantTest : public MNNTestCase`, registered as `"op/sgfp4/vulkan_uniform_parity"` |

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-04-SUMMARY.md` when done
</output>
