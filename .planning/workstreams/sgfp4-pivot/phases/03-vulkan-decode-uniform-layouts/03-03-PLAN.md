---
phase: 03-vulkan-decode-uniform-layouts
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.hpp
  - source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp
autonomous: true
requirements: [SGV2-13]

must_haves:
  truths:
    - "The new Vulkan Execution class decodes uniform-layout SGFP4 v2 containers on the Vulkan buffer backend, producing weights for both code modes"
    - "It is registered in the buffer-backend execution table on OpType_SGFP4Dequant"
    - "It loads the same {magic, offset, size} external-sidecar descriptor as the CPU path (FileLoader + real file-size probe), validating the container host-side ONCE before any upload or dispatch"
    - "FP16/FP32 shader-variant selection follows the backend convention (vkBn->useFP16() selects glsl_sgfp4_dequant_FP16_comp)"
  artifacts:
    - path: "source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.hpp"
      provides: "VulkanBasicExecution subclass declaration"
      contains: "class VulkanSGFP4Dequant"
    - path: "source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp"
      provides: "Host sidecar load + pre-validation + SSBO upload + dispatch + creator registration"
      min_lines: 120
  key_links:
    - from: "VulkanSGFP4Dequant.cpp creator"
      to: "op->main_as_SGFP4DequantParam() + op->externalPath()"
      via: "descriptor read in onCreate (useFP16 gate + external {offset,size} + sidecar path)"
      pattern: "externalPath|SGFP4DequantParam"
    - from: "VulkanSGFP4Dequant.cpp"
      to: "dequant_sgfp4_container_cpu"
      via: "scratch-buffer pre-validation pass (D-05)"
      pattern: "dequant_sgfp4_container_cpu"
    - from: "VulkanSGFP4Dequant.cpp"
      to: "glsl_sgfp4_dequant[_FP16]_comp"
      via: "vkBn->getPipeline(shaderName, types)"
      pattern: "glsl_sgfp4_dequant"
    - from: "VulkanSGFP4Dequant.cpp"
      to: "VulkanBackend::addCreator(OpType_SGFP4Dequant, ...)"
      via: "static gResistor lambda"
      pattern: "OpType_SGFP4Dequant"
---

<objective>
Implement and register the Vulkan buffer-backend Execution class for SGFP4 v2, structurally cloning `VulkanFP4Dequant` (pipeline/descriptor/dispatch) with host-side container loading cloned from `CPUSGFP4Dequant` (FileLoader + ifstream size probe) and one-time host pre-validation (SGV2-13, per D-01/D-05/D-06).

Purpose: The GPU counterpart of Phase 1's CPU Execution — same external-sidecar plumbing, same error semantics, additive to the E2M1 path.
Output: Two new source files; picked up automatically by `source/backend/vulkan/CMakeLists.txt` GLOB_RECURSE (no CMake edits).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-PATTERNS.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-02-SUMMARY.md
@include/MNN/SGFP4DequantUtils.hpp
@source/backend/cpu/CPUSGFP4Dequant.cpp
@source/backend/vulkan/buffer/execution/VulkanFP4Dequant.hpp
@source/backend/vulkan/buffer/execution/VulkanFP4Dequant.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: VulkanSGFP4Dequant header + host container loading and pre-validation</name>
  <files>source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.hpp, source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp</files>
  <read_first>
    - source/backend/vulkan/buffer/execution/VulkanFP4Dequant.hpp (whole file — clone structure verbatim; add mContainerBuffer/mContainer members)
    - source/backend/vulkan/buffer/execution/VulkanFP4Dequant.cpp (constructor pipeline selection lines 14-49; creator + static registration lines 93-109)
    - source/backend/cpu/CPUSGFP4Dequant.cpp (queryFileSize helper lines 25-36; descriptor gate + bounded FileLoader read lines 43-96 — the exact host-loading pattern to clone)
    - include/MNN/SGFP4DequantUtils.hpp (dequant_sgfp4_container_cpu signature — the scratch-validation call)
    - source/backend/vulkan/buffer/execution/VulkanBasicExecution.hpp (NO onResize hook — why loading/validation must live in the creator/constructor path)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-PATTERNS.md (VulkanSGFP4Dequant.cpp section — both analogs excerpted)
  </read_first>
  <action>
    1. Header `VulkanSGFP4Dequant.hpp`: clone `VulkanFP4Dequant.hpp` structure; class VulkanSGFP4Dequant : public VulkanBasicExecution with constructor taking (Backend* bn, std::vector<uint8_t> container, uint32_t outElementCount, bool useFP32Output) — the creator hands over the ALREADY-VALIDATED container bytes; private members mContainerBuffer (shared_ptr<VulkanBuffer>, the SSBO), mContainer, mConstBuffer, mDequantPipeline, mDescriptorSet, mUseFP32Output, mContainerBytes, mOutElementCount. Header guard `#ifndef VulkanSGFP4Dequant_hpp`.
    2. In the .cpp, implement the creator-side host loading, EXACTLY mirroring CPUSGFP4Dequant.cpp: a file-local `queryFileSize(const std::string&, size_t&)` clone (std::ifstream binary|ate + tellg probe); descriptor gate — param = op->main_as_SGFP4DequantParam(), null→INVALID_VALUE-equivalent (return nullptr from onCreate with MNN_ERROR); USE_EXTERNAL_DATA(param) && op->externalPath() non-null required; external = param->external()->data(); offset/size = external[0]/external[1]; bound check `offsetSize > fileSize || readSize > fileSize - offsetSize` → reject (T-03-02 DoS posture: probe BEFORE any allocation); FileLoader(externalPath, true) + offset(offset) + read into mContainer.
    3. D-05 pre-validation before constructing the pipeline: outElementCount from outputs (the creator receives the output tensor — derive element count from outputs[0] with TensorUtils, or from param dims product dimO*dimI, whichever matches how CPUSGFP4Dequant/ShapeSGFP4Dequant derive it — be consistent and note it); run `dequant_sgfp4_container_cpu(mContainer.data(), mContainer.size(), scratch.data(), outElementCount)` into a std::vector<float> scratch; failure → reject the op (return nullptr from onCreate + MNN_ERROR) — same error semantics as the CPU path: no upload, no dispatch, no partial output writes.
    4. D-01 upload after validation: mContainerBuffer = make_shared<VulkanBuffer>(vkBn->getMemoryPool(), /*separate=*/false, containerBytes, mContainer.data(), VK_BUFFER_USAGE_STORAGE_BUFFER_BIT) (hostData ctor — keeps onEncode copy-free).
  </action>
  <verify>
    <automated>cmake --build .build --target run_test.out (files auto-globbed); source assertions: grep "OpType_SGFP4Dequant" + "dequant_sgfp4_container_cpu" + "queryFileSize" in VulkanSGFP4Dequant.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `VulkanSGFP4Dequant.hpp` declares the class with header guard VulkanSGFP4Dequant_hpp; no onResize override (base has none)
    - `VulkanSGFP4Dequant.cpp` contains: a queryFileSize-style ifstream probe invoked BEFORE mContainer.resize and BEFORE any VulkanBuffer construction (source-order checkable)
    - The scratch validation calls `dequant_sgfp4_container_cpu` and a false return prevents Execution construction entirely (nullptr from creator)
    - Descriptor read path uses op->main_as_SGFP4DequantParam() + op->externalPath() identically to CPUSGFP4Dequant.cpp
    - Code compiles under the MSVC build with no exceptions/RTTI (error paths are returns, not throws)
  </acceptance_criteria>
  <done>Host side complete: sidecar loading with DoS bound, one-time pre-validation, validated-bytes-only SSBO upload, all in the creator/constructor path (compensating for the missing onResize hook).</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline selection, onEncode dispatch, and Creator registration</name>
  <files>source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp</files>
  <read_first>
    - source/backend/vulkan/buffer/execution/VulkanFP4Dequant.cpp (onEncode lines 54-90: const map/unmap, writeBuffer x3, bind, UP_DIV dispatch, barrierSource; creator + gResistor lines 93-109)
    - source/backend/vulkan/buffer/backend/VulkanBackend.cpp (getPipeline line ~152; useFP16 line ~102 — Precision_High gate)
    - The shader written in plan 03-02 (binding layout: 0=container SSBO, 1=Dst, 2=ConstBuffer{outElementCount, containerBytes})
  </read_first>
  <action>
    1. Constructor (runs only with validated bytes): const buffer — struct SGFP4DequantConst { uint32_t outElementCount; uint32_t containerBytes; } at binding 2; descriptor types vector {VK_DESCRIPTOR_TYPE_STORAGE_BUFFER /*0: container*/, VK_DESCRIPTOR_TYPE_STORAGE_BUFFER /*1: output*/, VK_DESCRIPTOR_TYPE_UNIFORM_BUFFER /*2: const*/}; 3-way shader selection exactly cloning VulkanFP4Dequant lines 37-48 with renamed keys: mUseFP32Output→"glsl_sgfp4_dequant_comp", vkBn->useFP16()→"glsl_sgfp4_dequant_FP16_comp", else "glsl_sgfp4_dequant_comp"; mDequantPipeline = vkBn->getPipeline(shaderName, types); mDescriptorSet.reset(mDequantPipeline->createSet()).
    2. onEncode(inputs, outputs, cmdBuffer): the op is 0-input Const-like — do NOT touch any input tensor; elementCount = outputs[0]->elementSize(); map const buffer, write both fields, unmap; outputBuffer/outputSize = extra->getTensorBuffer(outputs[0]) / getTensorSize(outputs[0]) via the AUTOTURN/extra pattern used by VulkanFP4Dequant's onEncode; mDescriptorSet->writeBuffer(mContainerBuffer->buffer(), 0, mContainerBuffer->size(), 0); writeBuffer(outputBuffer.first->buffer(), 1, outputSize, outputBuffer.second); writeBuffer(mConstBuffer->buffer(), 2, mConstBuffer->size()); bind; `vkCmdDispatch(cmdBuffer->get(), UP_DIV(elementCount, 256), 1, 1)` — 256 must match the shader's local_size_x (name it, e.g. constexpr uint32_t kSgfp4WorkgroupSize = 256); `cmdBuffer->barrierSource(outputBuffer.first->buffer(), outputBuffer.second, outputSize)`; return NO_ERROR.
    3. Creator + registration: class VulkanSGFP4DequantCreator : public VulkanBackend::Creator with onCreate(const std::vector<Tensor*>& inputs, const std::vector<Tensor*>& outputs, const MNN::Op* op, Backend* backend) performing Task 1's load+validate, returning nullptr on any failure or `new VulkanSGFP4Dequant(...)` on success; `static bool gResistor = []() { VulkanBackend::addCreator(OpType_SGFP4Dequant, new VulkanSGFP4DequantCreator); return true; }();`
    4. Build and run CPU regression to prove no registration-order or link regressions: `cmake --build .build --target run_test.out && ./.build/run_test.out.exe "op/sgfp4/"`.
  </action>
  <verify>
    <automated>cmake --build .build --target run_test.out && ./.build/run_test.out.exe "op/sgfp4/" (CPU SGFP4 suites still pass; op runs on CPU unaffected) — full GPU proof lands in plan 03-04's parity test</automated>
  </verify>
  <acceptance_criteria>
    - `VulkanBackend::addCreator(OpType_SGFP4Dequant, ...)` present via a static initializer lambda (grep)
    - Shader-key strings "glsl_sgfp4_dequant_comp" and "glsl_sgfp4_dequant_FP16_comp" both present, with vkBn->useFP16() in the selection chain
    - onEncode reads NO input tensor (0-input op) and dispatches UP_DIV(elementCount, kSgfp4WorkgroupSize) with kSgfp4WorkgroupSize == 256 == shader local_size_x
    - barrierSource on the output buffer follows the dispatch
    - The E2M1 VulkanFP4Dequant files are untouched (`git diff --exit-code source/backend/vulkan/buffer/execution/VulkanFP4Dequant.*`)
    - Build green; `op/sgfp4/` CPU suite passes
  </acceptance_criteria>
  <done>SGV2-13 complete: a registered Vulkan buffer-backend Execution that loads the same external-sidecar descriptor as the CPU path, pre-validates host-side, uploads validated bytes, and dispatches both shader variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted sidecar file → host memory | Attacker-controlled external()[0..1] and file bytes cross here |
| Host memory → GPU SSBO | Only validated bytes cross (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | DoS | declared size vs real file (oversized VulkanBuffer alloc) | mitigate | ifstream size probe BEFORE mContainer/VulkanBuffer allocation — exact CPUSGFP4Dequant T-01-04 clone, source-order verified in acceptance criteria |
| T-03-03 | DoS/Tampering | attacker-controlled record count B / offsets | mitigate | Reused fully-bounds-checked dequant_sgfp4_container_cpu walk as the validator (offset-table bounds + exact out-count equality already Phase-1-tested) — zero new validation code |
| T-03-04 | Info disclosure | externalPath handling | accept | Path originates from the model file via FlatBuffers, identical surface to Convolution2D external weights (locked design); no new path input |
</threat_model>

<verification>
- Build green with the new files auto-globbed (no CMake edits)
- Source-assertion greps: OpType_SGFP4Dequant registration, dequant_sgfp4_container_cpu validation, both shader keys
- `run_test.out.exe "op/sgfp4/"` CPU suites green (additivity)
</verification>

<success_criteria>
SGV2-13 delivered: registered Execution, same sidecar descriptor as CPU, host pre-validation before upload/dispatch, FP16/FP32 variant selection, E2M1 path untouched.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Symbols / Names |
|----------|------|-----------------|
| `source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.hpp` | new header | `class VulkanSGFP4Dequant : public VulkanBasicExecution` |
| `source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp` | new implementation | `VulkanSGFP4Dequant::VulkanSGFP4Dequant`, `VulkanSGFP4Dequant::onEncode`, `class VulkanSGFP4DequantCreator`, `struct SGFP4DequantConst`, `queryFileSize` (file-local), `static bool gResistor` registration on `OpType_SGFP4Dequant` |

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-03-SUMMARY.md` when done
</output>
