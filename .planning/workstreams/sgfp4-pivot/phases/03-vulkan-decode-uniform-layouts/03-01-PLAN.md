---
phase: 03-vulkan-decode-uniform-layouts
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .build/ (CMake cache reconfiguration only — no source files)
autonomous: false
requirements: [SGV2-12]

must_haves:
  truths:
    - "glslangValidator is reachable from the POSIX environment that runs makeshader.py (WSL or Git Bash)"
    - "A build directory is configured with MNN_VULKAN=ON and MNN_VULKAN_IMAGE=OFF and produces run_test.out with the Vulkan buffer backend linked"
    - "The pre-existing Vulkan test op/vulkan/fp4_dequant_correctness passes on this machine, proving the GPU device path end-to-end before any SGFP4 Vulkan code is written"
  artifacts:
    - path: ".build/CMakeCache.txt"
      provides: "MNN_VULKAN:BOOL=ON, MNN_VULKAN_IMAGE:BOOL=OFF build configuration"
      contains: "MNN_VULKAN:BOOL=ON"
  key_links:
    - from: "WSL/Git Bash environment"
      to: "glslangValidator"
      via: "PATH resolution"
      pattern: "glslangValidator --version"
---

<objective>
Provision the missing offline-shader toolchain (`glslangValidator` — verified absent from both Windows PATH and WSL) and reconfigure the build for the Vulkan buffer backend (current `.build` cache has `MNN_VULKAN=OFF`), then smoke-test the machine's Vulkan execution path with the pre-existing `op/vulkan/fp4_dequant_correctness` test.

Purpose: SGV2-12's makeshader.py regeneration and the phase's GPU parity test (SGV2-14) are impossible without these prerequisites (RESEARCH "Environment Availability" — glslangValidator is the only missing dependency with no fallback).
Output: A working shader-compiler toolchain, a Vulkan-buffer-enabled build, and a green Vulkan smoke test proving the RTX device path works before SGFP4 code lands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose and install the glslang toolchain</name>
  <read_first>
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md (section "Environment Availability" and Pitfall 3)
  </read_first>
  <what-built>
    Nothing yet — this is the Wave-0 provisioning decision discovered during research: `glslangValidator` is installed nowhere on this machine (WSL: python3 3.10 + xxd present, no glslang; Windows: no Vulkan SDK on PATH; nothing vendored in-repo).
  </what-built>
  <decision>
    How to provision `glslangValidator` for `makeshader.py` (which needs a POSIX environment: it shells out to `find` and `xxd`, so Windows PowerShell cannot host it).
  </decision>
  <context>
    makeshader.py regenerates `AllShader.cpp` / `AllShader.h` / `VulkanShaderMap.cpp` (the locked CLAUDE.md contract). WSL already has python3 and xxd; it lacks only glslang — but `sudo` requires the user's password (interactive, the agent cannot enter it). The alternative is a Windows Vulkan SDK install, but makeshader.py must then be run from Git Bash for POSIX `find`/`xxd`.
  </context>
  <options>
    <option id="option-a">
      <name>WSL glslang-tools (recommended)</name>
      <pros>python3 + xxd already verified present in WSL; one package install; `sudo apt-get install -y glslang-tools spirv-tools` (spirv-tools optional — makeshader.py degrades gracefully without spirv-opt)</pros>
      <cons>Requires the user to type the sudo password once (interactive)</cons>
    </option>
    <option id="option-b">
      <name>Windows Vulkan SDK + Git Bash</name>
      <pros>No WSL dependency; SDK also provides spirv-opt</pros>
      <cons>SDK installer download/run on the user; makeshader.py must be invoked from Git Bash (POSIX find/xxd); new toolchain path unverified</cons>
    </option>
  </options>
  <action>
    Present the two options and pause for the user to select one and perform the install (sudo password / SDK installer cannot be entered by the agent). After the user resumes, verify the install with the automated command below and record the chosen environment (WSL vs Git Bash) in the SUMMARY — plans 03-02's regeneration task depends on knowing which POSIX host to use.
  </action>
  <verify>
    <automated>wsl -e sh -c 'which glslangValidator && glslangValidator --version'  (option A) — or Git Bash: glslangValidator --version (option B)</automated>
  </verify>
  <done>glslangValidator --version succeeds in the chosen POSIX environment; the environment is recorded in the SUMMARY for plan 03-02.</done>
  <resume-signal>Select: option-a or option-b (after performing the install)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconfigure the build for the Vulkan buffer backend and smoke-test the device path</name>
  <files>.build/CMakeCache.txt (regenerated by cmake — no hand edits)</files>
  <read_first>
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md (Pitfall 5 and Pitfall 6)
    - .planning/workstreams/sgfp4-pivot/phases/01-affine-dual-mode-decode-core-cpu-uniform-layouts/deferred-items.md (the FP4ModelTest.cpp build blocker and the temp-stub workaround)
    - .build/CMakeCache.txt (current state — confirm MNN_VULKAN:BOOL=OFF before reconfiguring)
  </read_first>
  <action>
    1. Reconfigure the existing `.build` directory with exactly: `cmake -S . -B .build -DMNN_VULKAN=ON -DMNN_VULKAN_IMAGE=OFF -DMNN_BUILD_TEST=ON -DMNN_SUPPORT_TRANSFORMER_FUSE=ON` (MSVC 17 2022 generator is already cached; `MNN_VULKAN_IMAGE=OFF` is required or `source/backend/vulkan/buffer/*` is excluded entirely).
    2. Build the test target: `cmake --build .build --target run_test.out --config Release` (match the config the existing cache uses). If the build fails inside `test/op/FP4ModelTest.cpp` (pre-existing `milestone`-workstream dead code, unrelated to this phase), apply the Phase-1 temp-stub workaround: temporarily stub ONLY that file's body so it compiles, build, then restore the file byte-for-byte (verify with `git diff --exit-code test/op/FP4ModelTest.cpp`). NEVER commit the stub.
    3. Smoke-test the machine's Vulkan path with the already-committed test: `./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness"`. The driver `C:\Windows\System32\vulkan-1.dll` (RTX 4070 Ti SUPER, driver 32.0.15.9186) is verified present; the dlopen wrapper (`vulkan_wrapper.cpp`, `MNN_USE_SYSTEM_LIB=OFF` default) loads it at runtime.
    4. Also run the CPU regression baseline: `./.build/run_test.out.exe "op/sgfp4/"` — must stay green (Phase 1/2 suites).
  </action>
  <verify>
    <automated>Select-String -Path .build/CMakeCache.txt -Pattern 'MNN_VULKAN:BOOL=ON'; then ./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness" reports pass; ./.build/run_test.out.exe "op/sgfp4/" reports pass</automated>
  </verify>
  <acceptance_criteria>
    - `.build/CMakeCache.txt` contains `MNN_VULKAN:BOOL=ON` and `MNN_VULKAN_IMAGE:BOOL=OFF`
    - `run_test.out` builds with the Vulkan buffer backend linked (linker resolves `VulkanBackend` symbols)
    - `op/vulkan/fp4_dequant_correctness` passes (not skipped) — proves an actual GPU dispatch round-trip on this machine
    - `op/sgfp4/` CPU suites still pass
    - `git diff --exit-code test/op/FP4ModelTest.cpp` is clean afterward if the stub workaround was used
  </acceptance_criteria>
  <done>Toolchain installed (Task 1), build reconfigured with the Vulkan buffer backend, and the pre-existing Vulkan FP4 test passes on the physical GPU — Wave 0 prerequisites for SGV2-12/13/14 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS package install (sudo/SDK) | Standard distro/vendor packages only; no registry packages introduced into the project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00a | Tampering | apt/SDK package source | accept | Official Ubuntu archive / LunarG SDK installer only; no third-party repos; no project dependency changes |
| T-03-00b | Tampering | temp-stub workaround on FP4ModelTest.cpp | mitigate | Byte-for-byte restore verified with `git diff --exit-code`; stub never staged/committed |
</threat_model>

<verification>
- `glslangValidator --version` succeeds in the chosen POSIX environment
- `.build/CMakeCache.txt` shows `MNN_VULKAN:BOOL=ON` / `MNN_VULKAN_IMAGE:BOOL=OFF`
- `run_test.out.exe "op/vulkan/fp4_dequant_correctness"` green; `run_test.out.exe "op/sgfp4/"` green
</verification>

<success_criteria>
Wave 0 of 03-VALIDATION.md fully checked: toolchain + build reconfiguration + Vulkan device smoke run all green.
</success_criteria>

## Artifacts this phase produces

This plan produces no source artifacts (toolchain/build provisioning only). Phase-level artifacts are listed in plans 03-02/03-03/03-04.

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-01-SUMMARY.md` when done
</output>
