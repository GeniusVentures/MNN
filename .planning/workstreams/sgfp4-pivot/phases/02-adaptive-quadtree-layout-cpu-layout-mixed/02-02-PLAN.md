---
phase: 02-adaptive-quadtree-layout-cpu-layout-mixed
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tools/fp4/encode_sgfp4.py
  - test/op/SGFP4DequantFixtures.h
  - test/op/SGFP4DequantTest.cpp
autonomous: true
requirements: [SGV2-10, SGV2-11]
tags: [mnn, sgfp4, fp4, quantization, python-encoder, quadtree, layout-mixed, fixtures]

must_haves:
  truths:
    - "The error-driven encoder collapses to a uniform layout when all leaves share one size and otherwise emits LAYOUT_MIXED"
    - "The encoder selects T158 iff e_T158 <= (1+eps)*e_FP4 (default eps=0.10) with the ternary outlier veto respected"
    - "Mixed/adaptive containers round-trip encode->decode within the encoder's per-level error thresholds, verified via ./run_test.out"
    - "A golden pre-order DFS traversal check (independent enumerator) proves leaves are visited TL/TR/BL/BR and 4x4 nodes carry no split bit"
  artifacts:
    - path: "tools/fp4/encode_sgfp4.py"
      provides: "subdivide_macroblock/build_split_map/classify_layout + MIXED record emission + CLI knobs"
      contains: "subdivide_macroblock"
    - path: "tools/fp4/encode_sgfp4.py"
      provides: "MIXED-aware independent reference decoder + selftest mixed cases"
      contains: "decode_container_ref"
    - path: "test/op/SGFP4DequantFixtures.h"
      provides: "encoder-generated committed mixed fixtures"
      contains: "mixed_allsplit"
    - path: "test/op/SGFP4DequantTest.cpp"
      provides: "op/sgfp4/mixed_decode test: golden traversal, mixed round-trip, negative split-map cases"
      contains: "enumerateExpected"
  key_links:
    - from: "classify_layout"
      to: "uniform enum vs LAYOUT_MIXED"
      via: "all-same-size leaf set collapses to the Table 3 uniform enum"
      pattern: "classify_layout"
    - from: "build_split_map"
      to: "split-map bytes"
      via: "bit k = word[k//32] bit k%32, 3 LE words"
      pattern: "MAX_QUADTREE_BITS"
    - from: "test/op/SGFP4DequantTest.cpp golden traversal"
      to: "enumerateExpected independent enumerator"
      via: "marker-encoded S/bias per leaf"
      pattern: "enumerateExpected"
---

<objective>
Extend the existing `tools/fp4/encode_sgfp4.py` reference encoder to produce adaptive LAYOUT_MIXED records via error-driven quadtree subdivision (per-level thresholds, ε=0.10 mode selection, ternary outlier veto, hysteresis, normative uniform-layout collapse), regenerate committed mixed fixtures, and add the golden-traversal + mixed round-trip + negative split-map C++ tests.

Purpose: Complete the full SGFP4 v2 feature set on CPU (SGV2-10, SGV2-11) — the encoder that produces mixed containers and the tests that prove the decoder walks them correctly (D-05/D-08/D-09).

Output: `encode_sgfp4.py` gains subdivision/split-map/classification and MIXED record emission; `test/op/SGFP4DequantFixtures.h` is regenerated with mixed fixtures; `test/op/SGFP4DequantTest.cpp` gains an `op/sgfp4/mixed_decode` test case.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/REQUIREMENTS.md
@.planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-PATTERNS.md
@.planning/sgfp4-arxiv-v2.txt        # Section 6.3 (exemplary encoder policy), 6.2 (split map), 4.3/4.4 (payload + affine encode)
@tools/fp4/encode_sgfp4.py           # file being modified (uniform encoder + ref decoder + fixture generator)
@test/op/SGFP4DequantTest.cpp        # file being modified (add op/sgfp4/mixed_decode)
@test/op/SGFP4DequantFixtures.h      # file being regenerated (encoder output)
@include/MNN/SGFP4DequantUtils.hpp   # decoder side (from Plan 02-01) — source of truth for the wire format
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quadtree subdivision + split-map + layout classification + MIXED record emission in encode_sgfp4.py</name>
  <files>tools/fp4/encode_sgfp4.py</files>
  <read_first>
    - tools/fp4/encode_sgfp4.py (file being modified — see encode_leaf_fp4/encode_leaf_t158/select_mode, pack_leaf_header/pack_payload, encode_macroblock/encode_container, constants block)
    - .planning/sgfp4-arxiv-v2.txt §6.3 (per-level thresholds, epsilon mode selection, outlier veto, hysteresis, recursion floor 4x4, uniform-collapse rule) and §6.2 (split-map bit order)
    - .planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-RESEARCH.md (Pattern 3 subdivision + build_split_map code, Assumptions Log A1-A4)
  </read_first>
  <acceptance_criteria>
    - Source assertion: `tools/fp4/encode_sgfp4.py` defines `SPLIT_MAP_WORDS = 3`, `SPLIT_MAP_BYTES = 12`, `MAX_QUADTREE_BITS = 85`, `QUADTREE_MIN_SPLIT_SIZE = 8`, `LEVEL_THRESHOLDS = {64: 0.01, 32: 0.0047, 16: 0.0022, 8: 0.0011, 4: 0.0005}` (geometric interpolation tagged `[ASSUMED]`), `VETO_FACTOR` and `HYSTERESIS_DELTA` defaults.
    - Source assertion: the file defines `subdivide_macroblock(...)`, `build_split_map(leaves)`, and `classify_layout(leaves)`.
    - Source assertion: `build_split_map` packs 3 little-endian uint32 words via `struct.pack('<3I', *words)` and asserts `len(bits) <= MAX_QUADTREE_BITS`.
    - Source assertion: `classify_layout` returns the Table 3 uniform enum when all leaves share one size and tile exactly, else `LAYOUT_MIXED`.
    - Source assertion: the encoder emits leaves in pre-order DFS order for MIXED and raster order for uniform collapse.
    - Behavior assertion: existing `--selftest` uniform cases still pass (no regression to the uniform encode path).
    - Behavior assertion: `select_mode` still implements `e_T158 <= (1 + eps) * e_FP4` with `MODE_SELECT_EPS = 0.10`, and the ternary veto blocks T158 per the veto rule.
  </acceptance_criteria>
  <action>
    In `tools/fp4/encode_sgfp4.py`, extend the encoder (D-06/D-07) with:

    1. Constants (add near the existing `LAYOUT_TABLE`/`MODE_*` block): `SPLIT_MAP_WORDS = 3`, `SPLIT_MAP_BYTES = 12`, `MAX_QUADTREE_BITS = 85`, `QUADTREE_MIN_SPLIT_SIZE = 8`, `LEVEL_THRESHOLDS = {64: 0.01, 32: 0.0047, 16: 0.0022, 8: 0.0011, 4: 0.0005}` (comment `# [ASSUMED] geometric interpolation 0.01 @ 64 -> 0.0005 @ 4, per-element MSE (A1/A2)`), `VETO_FACTOR = 3.0` (`# [ASSUMED] A3`), `HYSTERESIS_DELTA = 0.05` (`# [ASSUMED] A4`).

    2. Add `def apply_ternary_veto(region, e_fp4, e_t158, veto_factor):` — returns `MODE_FP4_AFFINE` when `max|region - mean(region)| > veto_factor * mean|region - mean(region)|`, otherwise allows T158 (D-06 outlier veto).

    3. Add `def subdivide_macroblock(tile64, x=0, y=0, n=64, eps=MODE_SELECT_EPS, thresholds=LEVEL_THRESHOLDS, veto_factor=VETO_FACTOR, hysteresis_delta=HYSTERESIS_DELTA):` — recursive, returns a list of `(x, y, n, mode, S, bias, codes)`. Per region: compute `s_fp4,b_fp4,c_fp4,e_fp4 = encode_leaf_fp4(region)` and the T158 equivalents; if `n == 4` return one leaf (recursion floor); per-element MSE `mse = min(e_fp4, e_t158) / (n*n)`; if `mse <= thresholds[n]` accept the leaf (mode via `select_mode` then veto); else split into TL/TR/BL/BR quadrants `(x,y),(x+h,y),(x,y+h),(x+h,y+h)` with `h = n//2` and recurse (D-01 quadrant order). Apply hysteresis: only split when the sum of the four child errors is `< (1 - hysteresis_delta) * min(e_fp4, e_t158)`, else accept the parent leaf.

    4. Add `def build_split_map(leaves):` — `leaves` are `(x,y,n,...)` in DFS order; walk the tree recursively emitting 1/0 per node of size >= 8 (4x4 nodes emit nothing), bit k = word[k//32] bit k%32, pack into `struct.pack('<3I', *words)`, assert `len(bits) <= MAX_QUADTREE_BITS`.

    5. Add `def classify_layout(leaves):` — if all leaves share one edge `n` and `n` is a Table 3 uniform size with the exact leaf count and tiling, return that uniform enum (reorder to raster order for emission); else return `LAYOUT_MIXED`.

    6. Add a MIXED record emission path: `def encode_macroblock_mixed(leaves, eps=..., ...):` that takes the subdivision leaf list and produces `sb_header(4=LAYOUT_MIXED) + split_map(12B) + block_headers[N](4N B, DFS order) + pad-to-16 + payloads[N](each padded to 16B)`, reusing `pack_leaf_header` and `pack_payload` verbatim, and returns `(record_bytes, expected)` where `expected` is the concatenated DFS-order reconstruction (FP16-truncated S/bias).

    7. Extend `encode_container` (or add `encode_container_mixed`) to accept MIXED macroblocks alongside uniform ones, keeping the existing uniform path and the `cursor % ALIGNMENT == 0` record-length assertion intact.

    Do NOT create a separate quadtree script (D-07) and do NOT modify `quantize_fp4.py`. Keep every new numeric literal behind a named constant.
  </action>
  <verify>
    <automated>python tools/fp4/encode_sgfp4.py --selftest — existing uniform cases still print [PASS] (no regression)</automated>
    <automated>python -c "import tools.fp4.encode_sgfp4 as e; l=e.subdivide_macroblock(e.np.random.default_rng(1).standard_normal((64,64)).astype(e.np.float32)); print(e.classify_layout(l))" — runs without error and prints either an int layout enum</automated>
  </verify>
  <done>Encoder can subdivide a 64x64 tile, emit a <=85-bit split map, classify the leaf set (uniform collapse vs MIXED), and produce a well-formed MIXED record; uniform path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: MIXED-aware reference decoder + selftest cases + regenerate mixed fixtures</name>
  <files>tools/fp4/encode_sgfp4.py, test/op/SGFP4DequantFixtures.h</files>
  <read_first>
    - tools/fp4/encode_sgfp4.py (file being modified — see decode_container_ref, selftest, build_fixture_cases, emit_cpp_fixture, main/argparse)
    - .planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-PATTERNS.md (fixture struct/table + generator pattern, Shared Pattern 7: never hand-edit fixtures)
    - include/MNN/SGFP4DequantUtils.hpp (from Plan 02-01) — the MIXED branch the Python ref decoder must mirror
  </read_first>
  <acceptance_criteria>
    - Source assertion: `decode_container_ref` handles `LAYOUT_MIXED` by reading the 12-byte split map and walking leaves in pre-order DFS (independent of `build_split_map`).
    - Source assertion: `selftest()` adds cases that round-trip at least one all-split tree, one asymmetric mixed tree, and one uniform-collapse case (encoder emits a uniform enum, not MIXED).
    - Behavior assertion: `python tools/fp4/encode_sgfp4.py --selftest` prints `SELFTEST PASSED` with the new mixed cases present.
    - File assertion: `test/op/SGFP4DequantFixtures.h` contains fixtures named `mixed_allsplit`, `mixed_uniform_collapse`, and `mixed_asymmetric` after regeneration.
    - Behavior assertion: `python tools/fp4/encode_sgfp4.py --emit-cpp-fixture test/op/SGFP4DequantFixtures.h` exits 0 and the header's `kFixtures` table includes the mixed fixture entries with `layout == 4` (LAYOUT_MIXED) for all-split/asymmetric and a uniform enum for the collapse case.
  </acceptance_criteria>
  <action>
    In `tools/fp4/encode_sgfp4.py`:

    1. Extend `decode_container_ref` to decode `LAYOUT_MIXED`: after reading `layout_enum`, if it is `LAYOUT_MIXED`, read the 12-byte split map at `rec_start + 4`, walk it with an independent DFS (separate helper from `build_split_map` — a fresh enumeration of `(x, y, n)` per spec §6.2), then read N block headers and per-leaf variable-size payloads in traversal order (payload words = `n*n/8` mode 0, `n*n/16` mode 1), each padded to 16B. Keep the uniform path byte-identical to today.

    2. Extend `selftest()` with mixed/adaptive round-trip cases (SGV2-10/11): (a) an all-split tree (e.g. a high-variance tile whose per-region errors exceed every threshold, producing 256 4x4 leaves), (b) an asymmetric mixed tree (e.g. spatially varying variance), and (c) a uniform-collapse case (a tile whose leaves all end up the same size) — for (c) assert the emitted `layout_enum` is the corresponding uniform enum, NOT `LAYOUT_MIXED` (Success Criterion 2 / D-06).

    3. Extend `build_fixture_cases()` with deterministic committed fixtures (D-08): `mixed_allsplit`, `mixed_uniform_collapse` (encoder emits the uniform enum, not MIXED), and `mixed_asymmetric`, each with `layout = LAYOUT_MIXED` (or the collapsed uniform enum for the collapse case), seeded rng for determinism.

    4. Regenerate the fixture header by running `python tools/fp4/encode_sgfp4.py --emit-cpp-fixture test/op/SGFP4DequantFixtures.h`. Never hand-edit the header.

    5. Add CLI knobs (D-06 overridable) to `main()`/argparse: `--eps` (default 0.10), `--level-thresholds` (comma-separated 64,32,16,8,4 MSE values), `--veto-factor`, `--hysteresis-delta`, threaded through `subdivide_macroblock`. Update the script docstring and `--help` text accordingly.
  </action>
  <verify>
    <automated>python tools/fp4/encode_sgfp4.py --selftest — prints SELFTEST PASSED with mixed round-trip + uniform-collapse cases</automated>
    <automated>python tools/fp4/encode_sgfp4.py --emit-cpp-fixture test/op/SGFP4DequantFixtures.h && grep -v '^//' test/op/SGFP4DequantFixtures.h | grep -c 'mixed_allsplit\|mixed_uniform_collapse\|mixed_asymmetric' — prints a count >= 3</automated>
  </verify>
  <done>The Python ref decoder round-trips mixed containers independently; selftest covers mixed/adaptive + uniform-collapse; committed fixtures include all-split, uniform-collapse, and asymmetric mixed cases; CLI knobs expose eps/thresholds/veto/hysteresis.</done>
</task>

<task type="auto">
  <name>Task 3: Golden traversal + mixed round-trip + negative split-map tests in SGFP4DequantTest.cpp</name>
  <files>test/op/SGFP4DequantTest.cpp</files>
  <read_first>
    - test/op/SGFP4DequantTest.cpp (file being modified — see testFixtureRoundTrip, testMalformedContainers, buildSingleLeafContainer, MNNTestSuiteRegister)
    - test/op/SGFP4DequantFixtures.h (regenerated by Task 2 — mixed fixture names/fields)
    - include/MNN/SGFP4DequantUtils.hpp (from Plan 02-01 — kSGFP4LayoutMixed, split-map constants, sgfp4_walk_quadtree)
    - .planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-RESEARCH.md (D-05 golden enumerator + marker-encoding recommendation, D-09 negative list)
  </read_first>
  <acceptance_criteria>
    - Source assertion: `test/op/SGFP4DequantTest.cpp` defines an independent enumerator (e.g. `enumerateExpected` / `LeafExpect`) that does NOT call `sgfp4_walk_quadtree`.
    - Source assertion: a new test class or method registered as `op/sgfp4/mixed_decode` is present (via `MNNTestSuiteRegister(..., "op/sgfp4/mixed_decode")`).
    - Behavior assertion: the golden traversal test decodes a mixed fixture and asserts each DFS-position output block holds the marker of the leaf the independent enumerator says occupies that position (marker-encoding via per-leaf S/bias).
    - Behavior assertion: the mixed round-trip test decodes `mixed_allsplit` / `mixed_uniform_collapse` / `mixed_asymmetric` fixtures and matches `expected` within `kFixtureRelativeTolerance`.
    - Behavior assertion: negative split-map cases (split-on-4x4 / past-85-bit, non-tiling area, truncated payload, lying leaf size) each cause `dequant_sgfp4_container_cpu` to return false.
    - Test command: `./run_test.out op/sgfp4` prints the `mixed_decode` PASS line.
  </acceptance_criteria>
  <action>
    In `test/op/SGFP4DequantTest.cpp`, add a new test class `SGFP4MixedDecodeTest` (or a new layer) registered with `MNNTestSuiteRegister(SGFP4MixedDecodeTest, "op/sgfp4/mixed_decode")`:

    1. Add an independent golden enumerator (D-05): a file-local helper (e.g. `struct LeafExpect { int x, y, n; };` + `void enumerateExpected(const uint32_t* map, LeafExpect* out, int& count)`) that re-derives pre-order DFS leaf `(x, y, n)` from the same §6.2 rule with its OWN bit accounting and does NOT share any code path with `MNN::sgfp4_walk_quadtree`.

    2. Add `testGoldenTraversal()`: build (or read from a fixture) a MIXED container whose leaf k encodes a unique marker via `(S, bias)` — e.g. `S = 1.0f + k*0.001f, bias = 100.0f + k` — decode it, then for each output block position p (in leaf-major DFS order) assert that block equals the marker of the leaf `enumerateExpected` says occupies position p (compare each n x n block element to that leaf's expected `S*c + bias` within tolerance). This verifies TL/TR/BL/BR pre-order DFS order and that 4x4 nodes carry no split bit.

    3. Add `testMixedRoundTrip()`: loop over the mixed fixtures (`mixed_allsplit`, `mixed_uniform_collapse`, `mixed_asymmetric`) and compare decoded output to `fixture.expected` via `checkVectorByRelativeError<float>` at `kFixtureRelativeTolerance` — mirroring the existing `testFixtureRoundTrip`.

    4. Add `testMixedNegativeCases()` covering D-09, each asserting decode returns false: (a) split-on-4x4 / past-85-bit — a hand-built map that would force a >85-bit read (e.g. a full all-split map with a stray bit set in word 2 beyond the 85-bit budget), (b) non-tiling leaves — a map whose walk implies an area sum != 4096 (construct via a truncated/short walk), (c) truncated variable-size payload — a valid mixed fixture with the container cut mid-payload, (d) lying leaf sizes — a mixed record whose split map implies N leaves but whose block-headers/payloads region is sized for fewer (truncate after the split map + 1 header). Use the existing mutation-by-located-byte style from `testMalformedContainers`.

    Keep the existing `op/sgfp4/uniform_decode` registration untouched; the new registration is additive.
  </action>
  <verify>
    <automated>./run_test.out op/sgfp4 — expects both "SGFP4DequantTest: all layers PASSED" (uniform) and the mixed_decode PASS line. Build via the FP4ModelTest temporary-local-stub workaround (restore byte-for-byte, never commit).</automated>
  </verify>
  <done>Golden traversal (independent enumerator) proves pre-order DFS TL/TR/BL/BR order and 4x4-no-bit; mixed round-trip matches encoder-generated fixtures; negative split-map/size-abuse cases all reject with decode failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container bytes -> dequant_sgfp4_container_cpu | Untrusted SGFP4 v2 container (ASVS V5) — exercised by the new mixed/negative tests |
| encoder output -> decoder | The encoder is a dev-time oracle; its output is validated by the decoder and selftest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | dequant_sgfp4_container_cpu (lying leaf sizes) | mitigate | leaf n derived only from the split map; payload word count recomputed from n and cross-checked vs record bounds (D-02/D-09) |
| T-02-06 | DoS | dequant_sgfp4_container_cpu (oversized geometry) | mitigate | existing elementCount > outElementCount - outCursor bound rejects oversized/attacker-controlled geometry |
| T-02-07 | Tampering | encode_sgfp4.py (encoder) | accept | build-time dev tool, not a runtime trust boundary; output verified by decode_container_ref in selftest |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase (numpy 2.2.5 already present) |
</threat_model>

<verification>
Build note (RESEARCH Pitfall 6, applies to every `./run_test.out` step): `test/op/FP4ModelTest.cpp` (pre-existing dead code, commit `cffaf4bd`) prevents a from-scratch `run_test.out` build. Temporarily stub it locally, build + run, restore byte-for-byte (`git diff` zero), never commit.

- Task 1: `python tools/fp4/encode_sgfp4.py --selftest` (uniform no-regression) + inline subdivision/classify smoke.
- Task 2: `python tools/fp4/encode_sgfp4.py --selftest` (mixed + collapse) + `--emit-cpp-fixture` regeneration with mixed fixture names present.
- Task 3: `./run_test.out op/sgfp4` — uniform + mixed_decode both PASS.
</verification>

<success_criteria>
- The error-driven encoder collapses to a uniform layout when all leaves share one size and otherwise emits LAYOUT_MIXED, selecting T158 iff `e_T158 <= (1+eps)*e_FP4` (default eps=0.10) with the ternary outlier veto respected.
- Mixed/adaptive containers round-trip encode->decode within the encoder's per-level error thresholds, verified via `./run_test.out`.
- Golden traversal check proves pre-order DFS TL/TR/BL/BR order and 4x4-no-split-bit; negative tests reject malformed split-maps and size abuse.
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/02-adaptive-quadtree-layout-cpu-layout-mixed/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates the following new symbols (exclude from drift/coverage checks against prior artifacts):

**In `tools/fp4/encode_sgfp4.py`:**
- `SPLIT_MAP_WORDS` (= 3), `SPLIT_MAP_BYTES` (= 12), `MAX_QUADTREE_BITS` (= 85), `QUADTREE_MIN_SPLIT_SIZE` (= 8)
- `LEVEL_THRESHOLDS` (= {64: 0.01, 32: 0.0047, 16: 0.0022, 8: 0.0011, 4: 0.0005})
- `VETO_FACTOR` (= 3.0), `HYSTERESIS_DELTA` (= 0.05)
- `def apply_ternary_veto(region, e_fp4, e_t158, veto_factor)`
- `def subdivide_macroblock(tile64, x, y, n, eps, thresholds, veto_factor, hysteresis_delta)`
- `def build_split_map(leaves)`
- `def classify_layout(leaves)`
- `def encode_macroblock_mixed(leaves, ...)`
- (modified) `decode_container_ref` — MIXED branch; (modified) `selftest`, `build_fixture_cases`, `main`/argparse
- CLI flags: `--eps`, `--level-thresholds`, `--veto-factor`, `--hysteresis-delta`

**In `test/op/SGFP4DequantFixtures.h` (regenerated):**
- fixture names `mixed_allsplit`, `mixed_uniform_collapse`, `mixed_asymmetric`

**In `test/op/SGFP4DequantTest.cpp`:**
- `struct LeafExpect` / `void enumerateExpected(...)` (independent golden enumerator)
- `SGFP4MixedDecodeTest` with methods `testGoldenTraversal()`, `testMixedRoundTrip()`, `testMixedNegativeCases()`
- registration string `op/sgfp4/mixed_decode`
