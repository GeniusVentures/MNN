---
phase: 12-end-to-end-validation
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tools/fp4/e2e_validation.ps1
  - tools/fp4/README.md
autonomous: true
requirements: [SGV2-31, SGV2-32]
tags: [sgfp4, e2e, validation, cpu, vulkan, tolerances]

must_haves:
  truths:
    - "A single committed script invocation (tools/fp4/e2e_validation.ps1 -Corpus <alexnet.onnx>) converts the corpus FP32 and SGFP4, runs both artifacts on CPU and Vulkan (classic API), and prints a PASS/FAIL verdict per backend (D-08)"
    - "The SGFP4 CPU output matches the FP32 baseline within the locked tolerance: max-abs <= TolAbs and guarded relative error <= TolRel (SGV2-31, D-01/D-06)"
    - "The SGFP4 Vulkan output matches the SAME FP32 baseline with the SAME tolerance, on the SAME artifact via classic API + MNN_FORWARD_VULKAN (SGV2-32, D-05/D-06), with backendType=7 asserted from stdout"
    - "An environment without Vulkan capability FAILS the script (exit 2) — no SKIP semantics (D-07)"
    - "On failure, per-backend diagnostics print max-abs error, max relative error, and the failing index for both metrics (D-10)"
    - "The script's negative leg proves --sgfp4 + forced conversion failure exits non-zero with no 'Converted Success!' (D-11 consumer proof)"
  artifacts:
    - path: "tools/fp4/e2e_validation.ps1"
      provides: "Committed multi-leg E2E validation script (FP32 convert, SGFP4 convert + node-presence assert, 3 isolated run legs, guarded comparison core, D-11 negative leg, -MeasureOnly tolerance-derivation mode)"
      contains: "TolAbs"
    - path: "tools/fp4/README.md"
      provides: "E2E validation usage section: parameters, tolerance derivation + Phase 10 citation, locked values, seed/range, exit semantics, hard Vulkan requirement"
      contains: "e2e_validation"
  key_links:
    - from: "e2e_validation.ps1"
      to: "MNNConvert.exe"
      via: "flag-off baseline convert + --sgfp4 --dumpPass convert with exit-code and InsertSGFP4Dequant-rewrite stdout asserts"
      pattern: "InsertSGFP4Dequant: ops"
    - from: "e2e_validation.ps1"
      to: "MNNV2Basic.out.exe"
      via: "3 legs (forward 0/0/7), precision mask 1, dims 1x3x224x224, per-leg -WorkingDirectory, input_0.txt in / output.txt out"
      pattern: "backendType is 7"
    - from: "e2e_validation.ps1 comparison core"
      to: "each leg's output.txt"
      via: "absErr_i = |s_i - b_i|; relErr_i = absErr_i / max(|b_i|, 1e-3); gate max(absErr) <= TolAbs -and max(relErr) <= TolRel"
      pattern: "TolRel"
---

<objective>
Create the committed E2E validation script that closes SGV2-31/SGV2-32: the approved AlexNet corpus converts via `mnnconvert --sgfp4` and produces numerically correct inference on CPU AND Vulkan (classic API), gated against a same-path FP32 baseline with Phase 10-anchored, measure-then-locked tolerances — one script invocation, PASS/FAIL per backend, D-10 diagnostics on failure, plus the D-11 negative leg and README documentation.

Purpose: This is the v3.0 milestone's demonstrable artifact — the formal output-accuracy gates Phase 11's smoke deliberately deferred (Phase 11 D-14).

Output: `tools/fp4/e2e_validation.ps1` (committed) + README section; locked tolerance values recorded with derivation provenance.

Planner decisions locked (per RESEARCH open questions and Claude's Discretion):
- **OQ2 = measure-then-lock (Pitfall 9 methodology, satisfies D-02's "anchored, not invented"):** Task 1 ships the script with a `-MeasureOnly` switch and runs it to measure observed max-abs / max-relative on both SGFP4 legs. Locked gate = `2.0 x measured worst across BOTH backends` (documented margin). Form/sanity anchor from `tools/fp4/real_weight_validation_report.json` (context.thresholds."64": max_mse 0.01, max_relative 0.384 — weight-level, cited for provenance only, NEVER transcribed as output tolerances). If the measured margin lands under 1e-5 absolute, halt and surface (text-dump precision floor, Pitfall 2/A3).
- **OQ3 = guarded-denominator relative error (Phase 10 D-07 precedent):** `relErr_i = absErr_i / max(|baseline_i|, $Eps)` with `$Eps = 1e-3`. Primary gate is max-abs; relative is secondary.
- **Script identity:** `tools/fp4/e2e_validation.ps1`, params `-Corpus` (required), `-MnnConvert`, `-Driver`, `-WorkRoot` (all defaulted). Synthetic input: seed `20260901`, uniform `[-1, 1)`, 150,528 floats (D-03/D-04).
- **Node-presence assert: YES** (Claude's Discretion) — cheap `InsertSGFP4Dequant: ops N -> M` stdout assert with `N -ne M` (complements D-11).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/PROJECT.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/12-end-to-end-validation/12-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/12-end-to-end-validation/12-PATTERNS.md
@tools/fp4/w2_failcleanup_probe.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e_validation.ps1 — multi-leg orchestration + guarded comparison core + -MeasureOnly mode</name>
  <read_first>
  - tools/fp4/w2_failcleanup_probe.ps1 (full — the committed-script skeleton: param block, $ErrorActionPreference, Test-Path pre-checks, Start-Process -PassThru -Wait, exit 0/1/2, PASS/FAIL Write-Host lines)
  - tools/cpp/MNNV2Basic.cpp (:97-130 _loadInputFromFile, :193-235 argv parse, :209-215 precision default, :255-257 backupType, :313-320 resize check, :328 backendType print, :422-431 output.txt dump — invocation contract, DO NOT EDIT)
  - tools/fp4/README.md (Phase 11 smoke section — conversion invocation + expected [DumpPass] lines)
  - tools/fp4/real_weight_validation_report.json (context.thresholds."64" — form/sanity anchor only)
  </read_first>
  <files>tools/fp4/e2e_validation.ps1</files>
  <action>
  Create `tools/fp4/e2e_validation.ps1` extending the W-2 probe skeleton. Header comment documents: purpose (Phase 12 E2E gate, SGV2-31/32), usage line, exit semantics (0 = all gates PASS; 1 = tolerance or assertion FAIL; 2 = infra: missing binary/corpus/no Vulkan device), tolerance formula (`max-abs primary; relErr = absErr / max(|base|, 1e-3)`), Phase 10 citation, locked tolerance values + derivation note ("locked = 2.0x measured worst, both backends, <date>"), seed 20260901 / range [-1,1) / 150528 floats, and the text-dump ~1e-5 precision floor caveat.

  Param block: `param([Parameter(Mandatory=$true)][string]$Corpus, [string]$MnnConvert = ".build/Release/MNNConvert.exe", [string]$Driver = ".build/Release/MNNV2Basic.out.exe", [string]$WorkRoot = "tmp/p12_e2e", [switch]$MeasureOnly)`. Script-scope constants `$TolAbs`, `$TolRel` (initialized to placeholder 0.0 with a MUST-LOCK comment in Task 1; Task 2 locks real values), `$Eps = 1e-3`, `$Seed = 20260901`.

  Step 0 (D-07 fail-fast): `Test-Path` all three input paths (exit 2 with FAIL message per missing); run `vulkaninfo --summary`; if the command is missing or stdout matches no device line, `Write-Host "FAIL: no Vulkan device (D-07 hard requirement — no SKIP)"` and `exit 2`.

  Step 1 (input, D-03/D-04): `[Random]::new($Seed)`; generate 150,528 floats uniform in `[-1, 1)` (`$r.NextDouble() * 2.0 - 1.0`); write once as whitespace-separated lines to `$WorkRoot/input_0.txt` — the same file is copied into every leg dir (byte-identical feed).

  Step 2 (conversions): flag-off → `$WorkRoot/baseline.mnn`; `--sgfp4 --dumpPass` → `$WorkRoot/sgfp4.mnn` (capture `2>&1`). Assert both exit codes are 0; assert stdout matches `InsertSGFP4Dequant: ops (\d+) -> (\d+)` with `$Matches[1] -ne $Matches[2]` (node-presence sanity). Any violation → exit 1 with the captured stdout.

  Step 3 (legs, Pattern 1/Pitfall 3): create `$WorkRoot/leg-baseline-cpu`, `leg-sgfp4-cpu`, `leg-sgfp4-vulkan`; copy `input_0.txt` into each; run `$Driver` via `Start-Process -NoNewWindow -PassThru -Wait -WorkingDirectory <legDir>` with argv `<abs model path> 1 0 <forward> 4 1 1x3x224x224` where forward = 0 (baseline-cpu), 0 (sgfp4-cpu), 7 (sgfp4-vulkan). Precision mask MUST be 1 on every leg (Pitfall 1). Assert each leg exit code 0 and that `output.txt` exists and parses to a non-empty float array (whitespace-split, `[double]::Parse` invariant culture). Capture each leg's full stdout; on the Vulkan leg additionally assert it matches `backendType is 7` (Pitfall 7).

  Step 4 (comparison core, D-01/D-10): parse baseline and each SGFP4 leg output into `double[]`; assert equal length (mismatch → exit 1 with both counts). Per index: `absErr_i`, `relErr_i = absErr_i / [math]::Max([math]::Abs($base[$i]), $Eps)`. Track max-abs (+ argmax index), max-rel (+ argmax index). Per backend verdict: `(maxAbs -le $TolAbs) -and (maxRel -le $TolRel)`. Print per backend: `PASS/FAIL: <backend> max-abs=<..> (idx <..>), max-rel=<..> (idx <..>)`. Under `-MeasureOnly`: print measured values for both backends and the suggested lock (`2.0x worst`), then `exit 0` without gating. Normal mode: exit 0 iff CPU PASS and Vulkan PASS.

  Step 5 (D-11 negative leg): write ~1 KB of garbage to `$WorkRoot/corrupt.onnx`; run `$MnnConvert -f ONNX --modelFile corrupt.onnx --MNNModel $WorkRoot/neg.mnn --sgfp4` capturing output; assert non-zero exit AND captured stdout not matching `Converted Success!` — violation → exit 1 ("D-11 regression: converter lied"). Run this leg BEFORE exiting with the overall verdict; it does not short-circuit earlier tolerance failures (diagnostics print first).

  Cleanup: wrap the work body in try/finally with `Remove-Item -Recurse -Force $WorkRoot -ErrorAction SilentlyContinue` ONLY on pass (keep artifacts for debugging on failure) — document this in the header.
  </action>
  <verify>
    <automated>pwsh -File tools/fp4/e2e_validation.ps1 -Corpus W:\gnus\models\alexnet_Opset16.onnx -WorkRoot tmp/p12_measure -MeasureOnly ; echo "exit=$LASTEXITCODE" (expect exit 0, both backends' measured max-abs/max-rel printed)</automated>
  </verify>
  <acceptance_criteria>
  - `tools/fp4/e2e_validation.ps1` exists and contains `param(` with `[Parameter(Mandatory=$true)][string]$Corpus`, `$MeasureOnly`, `$TolAbs`, `$TolRel`, `$Eps = 1e-3`, `$Seed = 20260901`
  - Script contains the vulkaninfo pre-check with `exit 2` on missing device (string `no Vulkan device` present)
  - All three MNNV2Basic legs run with precision mask `1` and per-leg `-WorkingDirectory`; Vulkan leg asserts `backendType is 7`
  - Comparison core implements `absErr / max(abs(base), Eps)` and prints failing indices (D-10)
  - Conversion step asserts `InsertSGFP4Dequant: ops` rewrite with differing counts
  - Negative leg asserts non-zero exit AND absence of `Converted Success!` for `--sgfp4` + corrupt model
  - `-MeasureOnly` run on the real corpus exits 0 and prints measured max-abs/max-rel for CPU and Vulkan
  </acceptance_criteria>
  <done>Script drives the full E2E deterministically; -MeasureOnly produces the measured-worst numbers on both backends needed to lock tolerances; all infra assertions (vulkaninfo, exit codes, backendType, node presence, D-11 negative) active.</done>
</task>

<task type="auto">
  <name>Task 2: Lock tolerances from measured data + full-gate run + README documentation</name>
  <read_first>
  - tools/fp4/e2e_validation.ps1 (as created in Task 1 — the header and $TolAbs/$TolRel definitions to update)
  - tools/fp4/README.md (Phase 11 smoke section ~:89-140 — heading/convention template for the new section)
  - tools/fp4/real_weight_validation_report.json (citation context)
  </read_first>
  <files>tools/fp4/e2e_validation.ps1, tools/fp4/README.md</files>
  <action>
  From the Task 1 `-MeasureOnly` output, compute `measuredWorstAbs = max(CPU max-abs, Vulkan max-abs)` and `measuredWorstRel = max(CPU max-rel, Vulkan max-rel)`. Sanity: if `measuredWorstAbs -lt 1e-5`, STOP and surface in the SUMMARY (text-dump precision floor, Pitfall 2/A3) instead of locking. Otherwise lock `$TolAbs = 2.0 * measuredWorstAbs` and `$TolRel = 2.0 * measuredWorstRel` (margin 2.0x documented), replacing the placeholders. Update the header's derivation note with: measured values per backend, the locked values, the lock date, and the Phase 10 citation `tools/fp4/real_weight_validation_report.json` (context.thresholds."64": max_mse 0.01, max_relative 0.384 — form/sanity anchor, not transcribed). Same gate for both backends (D-06).

  Run the full gate: `pwsh tools/fp4/e2e_validation.ps1 -Corpus W:\gnus\models\alexnet_Opset16.onnx` → expect exit 0 with `PASS: cpu` and `PASS: vulkan` lines and the D-11 negative leg passing. Record the exact PASS lines in the SUMMARY.

  Add a README section `## SGFP4 v2 end-to-end validation (Phase 12 — test-time manual gate)` following the Phase 11 smoke conventions: corpus provenance line (`W:\gnus\models\alexnet_Opset16.onnx`, sha256 `4bc388cc…`, Phase 10 D-01/D-02 approval); prerequisites (built `.build\Release` binaries + WORKING VULKAN DEVICE — hard requirement, no SKIP, D-07); numbered invocation block with `# expect:` lines (per-backend PASS lines, exit 0); parameter table (`-Corpus` required, `-MnnConvert`, `-Driver`, `-WorkRoot`, `-MeasureOnly`); tolerance methodology (formula, Phase 10 anchor, measured-then-locked values with date, text-dump floor caveat); synthetic input spec (seed 20260901, [-1,1), 150,528 floats); exit-code semantics (0/1/2); the D-11 negative leg description. Terminology: "SGFP4 v2" everywhere — never "Ultra FP4".

  Final regression sweep: `run_test.out op/sgfp4` (13/13) + `TestSGFP4Converter.exe`.
  </action>
  <verify>
    <automated>pwsh -File tools/fp4/e2e_validation.ps1 -Corpus W:\gnus\models\alexnet_Opset16.onnx ; echo "exit=$LASTEXITCODE" (expect exit 0, "PASS: cpu" and "PASS: vulkan" in output) ; .build\Release\run_test.out.exe op/sgfp4 ; .build\Release\TestSGFP4Converter.exe</automated>
  </verify>
  <acceptance_criteria>
  - `e2e_validation.ps1` contains locked numeric literals for `$TolAbs`/`$TolRel` (no placeholder 0.0) and a header note naming the measured per-backend values, margin 2.0x, lock date, and the Phase 10 report citation
  - Full-gate run exits 0 with `PASS: cpu` and `PASS: vulkan` lines; D-11 negative leg passes within the same invocation
  - `tools/fp4/README.md` contains heading `SGFP4 v2 end-to-end validation (Phase 12` with invocation block, `# expect:` lines, parameter table, tolerance methodology, seed/range spec, exit semantics, and the hard Vulkan requirement statement
  - README contains no occurrence of the string `Ultra FP4`
  - `run_test.out op/sgfp4` 13/13 and `TestSGFP4Converter.exe` green
  </acceptance_criteria>
  <done>SGV2-31 and SGV2-32 formally gated green in one committed, documented, reproducible script invocation — the v3.0 milestone's demonstrable E2E artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → script | Corpus path, tool paths, leg output files read by the script |
| Local GPU stack → script | Vulkan driver enumerated by vulkaninfo pre-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering (local) | Corrupt/missing corpus or binaries | mitigate | Step-0 `Test-Path` pre-checks + exit 2 before any conversion |
| T-12-04 | Information disclosure (local) | Temp-dir leftovers with model data | mitigate | Work-dir cleanup on pass (kept on failure for debugging, documented) |
| T-12-05 | Spoofing | Vulkan leg silently falling back to CPU | mitigate | `backendType is 7` stdout assert + `backupType` hard-fail semantics + vulkaninfo pre-check (D-07) |
| T-12-SC | Tampering | package installs | accept | No packages installed (audit table empty by design) |
</threat_model>

<verification>
- One script invocation converts, runs (CPU + Vulkan classic API), and gates both backends against the FP32 baseline
- Tolerances locked with recorded derivation (measured-worst x 2.0, both backends, Phase 10 citation)
- D-10 diagnostics observable by forcing a tolerance failure (e.g. temporarily lowering TolRel in a scratch copy) — optional spot check
- README documents the full procedure; all regression suites green
</verification>

<success_criteria>
- `tools/fp4/e2e_validation.ps1 -Corpus W:\gnus\models\alexnet_Opset16.onnx` exits 0 with per-backend PASS lines
- SGV2-31 (CPU) and SGV2-32 (Vulkan) closed; D-01..D-10 satisfied; D-11 consumer leg green
- Committed artifact reproducible: same seed → same input → same verdict
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/12-end-to-end-validation/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols/files created or changed by Phase 12 that plan-review source-grounding should treat as new (not drift):

| Artifact | Kind | Plan |
|----------|------|------|
| `bool RunNetPass(const std::vector<std::string>&, std::unique_ptr<MNN::NetT>&)` | Changed symbol signature (`tools/converter/include/PostConverter.hpp`, `PostConverter.cpp`) | 12-01 |
| MNN_ERROR string `"[ERROR] --sgfp4 conversion failed: InsertSGFP4Dequant pass did not succeed.\n"` | New diagnostic | 12-01 |
| MNN_ERROR string `"[ERROR] Optimize the MNN Net failed, cancel convert.\n"` | New diagnostic (`cli.cpp`) | 12-01 |
| `MNNConverter.cpp` main `useSGFP4`-gated `return 1` | New exit path | 12-01 |
| `TestSGFP4Converter` T10 `RunNetPass`-false assertion | New test assertion | 12-01 |
| `tools/fp4/e2e_validation.ps1` (+ params `-Corpus`, `-MnnConvert`, `-Driver`, `-WorkRoot`, `-MeasureOnly`; exit 0/1/2 semantics) | New committed script | 12-02 |
| Tolerance constants `$TolAbs` / `$TolRel` (+ `$Eps = 1e-3`, seed `20260901`) in `e2e_validation.ps1` | New config values | 12-02 |
| `tools/fp4/README.md` §"SGFP4 v2 end-to-end validation (Phase 12)" | New doc section | 12-02 |
