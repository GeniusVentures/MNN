---
phase: 07-multi-tensor-hardening-structured-data-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fp4/author_structured_fixture.py
  - test/op/SGFP4StructuredFixtures.h
autonomous: true
requirements: [SGINJ-08]
must_haves:
  truths:
    - "A structured (LAYOUT_MIXED) SGFP4 v2 container produced by the REAL gnus-poc encoder exists as a committed C-array fixture header"
    - "The fixture's header comment records provenance (regeneration command, weight recipe, layout_distribution, sha256) proving MIXED presence at authoring time"
  artifacts:
    - path: "tools/fp4/author_structured_fixture.py"
      provides: "re-runnable one-time authoring script calling FP4Exporter.export_weights(..., adaptive=True)"
      contains: "layout_distribution"
    - path: "test/op/SGFP4StructuredFixtures.h"
      provides: "self-contained structured container bytes for the multi-tensor suite"
      contains: "kStructuredMixedData"
  key_links:
    - from: "tools/fp4/author_structured_fixture.py"
      to: "test/op/SGFP4StructuredFixtures.h"
      via: "C-array emission from exporter binary blob"
      pattern: "kStructuredMixedData"
---

<objective>
Produce the structured (LAYOUT_MIXED / quadtree) container from the REAL gnus-poc encoder and bake it into a self-contained test fixture header (D-01, D-02, D-03).

Purpose: SGINJ-08's structured-coverage criterion — the uniform demo lineage is all `UNIFORM_64` and does not count as quadtree coverage. The cross-repo dependency exists at AUTHORING TIME ONLY; tests never run Python and nothing binary is committed (D-02/D-03). Also retires the STATE.md pending todo "structured second artifact required before Phase 7".

Output: `tools/fp4/author_structured_fixture.py` (re-runnable authoring script), `test/op/SGFP4StructuredFixtures.h` (generated C-array fixture + provenance comment).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-PATTERNS.md
@test/op/SGFP4DequantFixtures.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authoring script + structured container export with MIXED self-assert</name>
  <files>tools/fp4/author_structured_fixture.py, test/op/SGFP4StructuredFixtures.h</files>
  <read_first>
  - W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\fp4_exporter.py (FP4Exporter.export_weights signature, __main__ dummy-noise trap, DEFAULT_V2_THRESHOLDS)
  - W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\quadtree.py (_classify_layout → Layout.MIXED condition)
  - W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\manifest.py (manifest schema; the tool reads only path/sha256/stats.shape)
  - test/op/SGFP4DequantFixtures.h (header convention: DO-NOT-EDIT comment, regeneration command, static const arrays, dims constants)
  - include/MNN/SGFP4DequantUtils.hpp (kSGFP4 constants; sgfp4_is_v2_container — for the header-comment contract)
  - .planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md (Q3 authoring recipe + Pitfall 2)
  </read_first>
  <action>
  Two deliverables, one task (the script exists to produce the header; neither is meaningful alone).

  **1. Authoring script `tools/fp4/author_structured_fixture.py`** (Python 3, runs in the gnus-poc repo context; NOT run by any test or CI — D-03):
  - Locate the gnus-poc repo via a constant `GNUST_POC_ROOT` at the top of the script (default the known absolute path `W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/gnus-poc`) and insert it into `sys.path` so `from quantize.fp4_exporter import FP4Exporter` resolves.
  - Call the API programmatically (NEVER the CLI `__main__` — it exports `randn(512,512)*0.01` dummy noise, the all-UNIFORM lineage): construct `FP4Exporter()`, then `binary, stats = exporter.export_weights(weights, "phase7_structured", adaptive=True)`.
  - **Weight recipe (discretion area D-fixed-here; guards Pitfall 2):** construct a 512×512 float32 numpy matrix where (a) most 64×64 superblocks are near-constant (amplitude far below the 64-size gate threshold 0.01), and (b) at least a few superblocks carry an asymmetric structure — e.g. a diagonal ramp of amplitude ~1.0 confined to the top-left quadrant of the superblock, other quadrants near-constant — so the quadtree must split to different leaf depths within one superblock. Do NOT use symmetric quadrant constants: FP4 affine represents them exactly and the tree may stay uniform (RESEARCH Q3 caveat). Exact magnitudes may be iterated until the assert passes.
  - **Mandatory self-assert (RESEARCH Q3):** `assert stats["layout_distribution"][4] > 0` (index 4 = Layout.MIXED); also record the full distribution dict. Prefer a distribution with BOTH uniform and MIXED superblocks present. On assert failure the script must exit non-zero with the distribution printed (loud failure, never silent — Pitfall 2).
  - Validate framing in the script before emitting: first 4 bytes equal the v2 magic and byte 4 == 0x02, and total size == len(binary).
  - **Emit `test/op/SGFP4StructuredFixtures.h`** following the `SGFP4DequantFixtures.h` convention: `DO NOT EDIT BY HAND` banner + regeneration command line (the exact python invocation of this script), then the provenance block (weight recipe description, complete `layout_distribution` dict, MIXED count, sha256 hex of the container bytes, byte length — deliberately NO date/timestamp, and no unseeded RNG anywhere in the recipe, so regeneration is byte-identical), then the array `static const unsigned char kStructuredMixedData[] = { 0x.., ... };` (comma-separated `0xNN` bytes, wrapped at 120 cols) and constants `constexpr int kStructuredDimO = 512;`, `constexpr int kStructuredDimI = 512;`, `constexpr size_t kStructuredSize = sizeof(kStructuredMixedData);`, `constexpr int kStructuredMixedCount = <N>;` (documentation-only provenance of MIXED superblock count — the C++ side cannot re-derive MIXED-ness). Header guard `SGFP4StructuredFixtures_h` in the repo style (`#ifndef SGFP4StructuredFixtures_h`).
  - Script writes the header file path as its only CLI arg (default `test/op/SGFP4StructuredFixtures.h` relative to repo root) and prints a summary (bytes, distribution, sha256).

  **2. Run the script once** in this task to generate the committed header (the one-time D-03 authoring run). If the gnus-poc Python env fails to import numpy/quantize (Assumption A1), fix the invocation from the MNN workspace (e.g. set PYTHONPATH) — the script must be re-runnable later without edits. Iterate the recipe amplitudes until the MIXED assert passes.

  Note: header size ~0.7-1.6 MB of `0xNN, ` text at 512×512 is acceptable (RESEARCH Open Question 1 default: keep 512×512, mirrors D-05 exactly).
  </action>
  <verify>
    <automated>python tools/fp4/author_structured_fixture.py $env:TEMP\SGFP4StructuredFixtures.regen.h; if ($LASTEXITCODE -eq 0) { fc.exe /b test\op\SGFP4StructuredFixtures.h $env:TEMP\SGFP4StructuredFixtures.regen.h; if ($LASTEXITCODE -eq 0) { Select-String -Path test\op\SGFP4StructuredFixtures.h -Pattern 'kStructuredMixedData','kStructuredDimO = 512','kStructuredMixedCount = ' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count } }</automated>
    <!-- regenerates to a TEMP path and byte-compares against the committed header — the committed fixture is never dirtied by the verify run -->
  </verify>
  <acceptance_criteria>
  - `tools/fp4/author_structured_fixture.py` exists and contains the literal assert `stats["layout_distribution"][4] > 0` and the export call with `adaptive=True`.
  - `test/op/SGFP4StructuredFixtures.h` exists, defines `kStructuredMixedData` (non-empty byte array whose first bytes are the v2 magic), `kStructuredDimO = 512`, `kStructuredDimI = 512`, `kStructuredSize`, and `kStructuredMixedCount` with value > 0.
  - The header's provenance comment records the full `layout_distribution`, the sha256 of the array bytes, and the regeneration command.
  - Re-running the verify command exits 0 with `fc.exe /b` reporting no differences (script is fully deterministic: no date in provenance, no unseeded RNG — regeneration to a temp path is byte-identical to the committed header, so `git diff` stays clean after a verify run).
  - No `.sgfp4` binary, JSON manifest, or other generated artifact is committed besides the header.
  </acceptance_criteria>
  <done>The structured LAYOUT_MIXED container from the real gnus-poc encoder is frozen as a self-contained fixture, with authoring-time MIXED proof recorded in the header provenance (Pitfall 2 guarded); STATE.md structured-container todo retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gnus-poc exporter output → committed fixture header | trusted at authoring time (developer machine); the header is generated data, not code — carries no execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | committed fixture bytes vs real-encoder output | mitigate | provenance comment records sha256 + regeneration command; script re-derives and asserts deterministically |
| T-07-05 | Repudiation | fixture silently all-uniform (quadtree coverage claimed falsely) | mitigate | mandatory `layout_distribution[4] > 0` self-assert + `kStructuredMixedCount > 0` baked into header (Pitfall 2) |
</threat_model>

<verification>
- Script run exits 0 with MIXED assert passing; header regeneration round-trips.
- Header grep assertions (Task automated verify).
- Decode-side behavior of the fixture is proven by Plan 07-03's multi-tensor suite (oracle + classic run), not here.
</verification>

<success_criteria>
- `kStructuredMixedData` present with MAGIC-prefixed bytes; `kStructuredMixedCount > 0`.
- No runtime Python/external-path dependency introduced into any test.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

- `tools/fp4/author_structured_fixture.py` — new script; key symbols: `GNUST_POC_ROOT`, the `export_weights(..., adaptive=True)` call, the `layout_distribution[4] > 0` assert.
- `test/op/SGFP4StructuredFixtures.h` — new generated header; C-array/symbol names consumed downstream: `kStructuredMixedData`, `kStructuredDimO` (=512), `kStructuredDimI` (=512), `kStructuredSize`, `kStructuredMixedCount`.
- No suites or C++ compilation units (this header is data-only; it is included by Plan 07-03's test).

From sibling plans: `failCleanup` (07-01, `tools/fp4/sgfp4_inject_core.hpp`), `tools/fp4/README.md` (07-01); `test/op/SGFP4MultiTensorTest.cpp` with suites `op/sgfp4/multi_tensor` + `op/sgfp4/malformed_inputs` (07-03).
</artifacts_produced>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-02-SUMMARY.md` when done
</output>
