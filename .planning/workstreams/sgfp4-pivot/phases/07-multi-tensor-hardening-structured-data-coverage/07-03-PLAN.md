---
phase: 07-multi-tensor-hardening-structured-data-coverage
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - test/op/SGFP4MultiTensorTest.cpp
autonomous: true
requirements: [SGINJ-07, SGINJ-08]
must_haves:
  truths:
    - "Two containers (one structured MIXED + one uniform) inject into a single artifact whose dequant ops carry disjoint 16-byte-aligned sidecar ranges, byte-identical to the source containers (SGINJ-07)"
    - "The multi-tensor artifact loads and runs via classic Interpreter/Session with named I/O and FP32-parity against oracle-decoded weights (SGINJ-07, structured/quadtree half of SGINJ-08; D-04: structured container runs the same classic-API inject→load→run chain inside the multi-tensor test — no separate dedicated suite)"
    - "Every malformed-input probe (truncated/empty, bad sha256, v1/magic, manifest-missing-fields, zero-match, multi-match, garbage-body) exits non-zero with a diagnostic and leaves NO output files behind (SGINJ-08, D-09/D-10/D-11)"
  artifacts:
    - path: "test/op/SGFP4MultiTensorTest.cpp"
      provides: "op/sgfp4/multi_tensor and op/sgfp4/malformed_inputs suites"
      contains: "MNNTestSuiteRegister(SGFP4MultiTensorTest"
  key_links:
    - from: "test/op/SGFP4MultiTensorTest.cpp"
      to: "test/op/SGFP4StructuredFixtures.h"
      via: "kStructuredMixedData bytes fed into a synthetic niche dir"
      pattern: "kStructuredMixedData"
    - from: "test/op/SGFP4MultiTensorTest.cpp"
      to: "tools/fp4/sgfp4_inject_core.hpp"
      via: "in-process sgfp4_inject::run(argc, argv) with two --niche-dir pairs"
      pattern: "sgfp4_inject::run"
---

<objective>
Prove multi-tensor injection + structured (LAYOUT_MIXED) end-to-end coverage and enforce the full malformed-input failure matrix with D-11 atomicity assertions — one new test file, two suites (D-12).

Purpose: SGINJ-07 (multiple containers → single artifact with collision-free sidecar ranges, classic-API run) and SGINJ-08 (structured quadtree path end-to-end; malformed inputs fail cleanly with no partial output). The tests exercise existing tool machinery — multi-niche-dir loop, monotonic aligned sidecar cursor, exact-shape pairing (Phases 5/6) — they do not build it.

Output: `test/op/SGFP4MultiTensorTest.cpp` registering `SGFP4MultiTensorTest` → `op/sgfp4/multi_tensor` and `SGFP4MalformedInputsTest` → `op/sgfp4/malformed_inputs`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-PATTERNS.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-02-SUMMARY.md
@test/op/SGFP4ClassicAPITest.cpp
@test/op/SGFP4InjectTest.cpp
@test/op/SGFP4StructuredFixtures.h
@tools/fp4/sgfp4_inject_core.hpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structured fixture niche-dir authoring + multi-tensor positive suite (op/sgfp4/multi_tensor)</name>
  <files>test/op/SGFP4MultiTensorTest.cpp</files>
  <read_first>
  - test/op/SGFP4ClassicAPITest.cpp (ENTIRE file — the direct template: header includes incl. `fp4/sgfp4_inject_core.hpp`, dual `using namespace MNN;` + `using namespace MNN::Express;` MSVC fix, buildContainerUniform64 + sgfp4_align16 runtime-assert workaround, writeNicheDir + manifest basename cross-check, runClassicSession, in-process argv run, cleanupGuard)
  - test/op/SGFP4StructuredFixtures.h (Plan 07-02 output: kStructuredMixedData/dimO/dimI/Size/MixedCount; include as "SGFP4StructuredFixtures.h")
  - test/op/SGFP4InjectTest.cpp (collectExprs walker + graph-structure assertion, lines ~112-120, 268-306)
  - tools/fp4/sgfp4_inject_core.hpp (makeDequantOp; the two `--niche-dir` argv shape, argc=9)
  - include/MNN/SGFP4DequantUtils.hpp (dequant_sgfp4_container_cpu, framing constants)
  </read_first>
  <behavior>
  - Test 1 (structure): after a 2-niche injection, `Variable::loadMap(outPath)` yields exactly 2 `OpType_SGFP4Dequant` exprs; their `SGFP4DequantParam.external` ranges are disjoint; `offset % 16 == 0` for both; each `size` equals its source container length; `dims` are `{512,512}` and `{512,64}` respectively; sidecar `memcmp` at each range is byte-identical to the source bytes (structured fixture array vs its range; in-test uniform bytes vs its range).
  - Test 2 (classic run): `Interpreter::createFromFile(outPath)` → `createSession` → `resizeSession` → `runSession` returns NO_ERROR; `getSessionInputAll`/`getSessionOutputAll` report exactly the names `input`/`output`; the `[1,64]` output matches the FP32 baseline (base model pre-injection, weights = `dequant_sgfp4_container_cpu` of both containers) via `checkVectorByRelativeError<float>(..., 64, 1e-4)` (zero-by-construction parity, Phase 6 D-06 pattern extended to two weights).
  - Test 3 (MIXED provenance guard): `kStructuredMixedCount > 0` re-asserted (compile-time data; loud guards against fixture regeneration drift, RESEARCH Pitfall 2).
  </behavior>
  <action>
  Create `test/op/SGFP4MultiTensorTest.cpp` gated `#ifdef MNN_SUPPORT_TRANSFORMER_FUSE`, header block + `using namespace MNN; using namespace MNN::Express;` copied verbatim from `SGFP4ClassicAPITest.cpp` (MSVC traps: constexpr-vs-align16 C2131 workaround, NOMINMAX).

  **Test-graph topology (D-05/D-06; chained per RESEARCH Q5):** `_Input({1,512}, NCHW) 'input'` → `MatMul(input, w1[512,512]) 'h'` → `MatMul(h, w2[512,64]) 'output'`. `w1` = oracle decode of the structured fixture (`dequant_sgfp4_container_cpu` over `kStructuredMixedData`, `kStructuredSize`); `w2` = oracle decode of an in-test uniform container. Base model saved via `Variable::save` BEFORE injection; baseline run uses that pre-injection file.

  **Generalize the uniform container builder** to `buildContainerUniform64(dimO, dimI, out)` (512×64: 8 records, offset-table bytes 16+8*4=48, `align16(48)=48`); keep the runtime agreement assert against `sgfp4_align16` (arithmetic expression — NOT constexpr calling the inline helper, MSVC C2131; Pitfall 4).

  **Two niche dirs** (D-06): dir A = structured (`kStructuredMixedData` bytes as `struct_fixture.sgfp4`, manifest stats.shape `[512,512]`); dir B = uniform builder output (`uniform_fixture.sgfp4`, shape `[512,64]`). Extend `writeNicheDir` to take name+shape+dims; keep the manifest basename cross-check pitfall (Pitfall 5); sha256 always via `sgfp4::sha256_hex`. All paths absolute cwd-anchored via the Phase 6 `cwdPath()`/`tempPath()` helpers (`op->externalPath` is baked literally — Pitfall 3, and fresh time+rand paths per invocation — Pitfall 6).

  **Inject in-process** with 9 argv entries: `{"sgfp4_inject", "--model", base, "--niche-dir", dirA, "--niche-dir", dirB, "--output", out}` → `sgfp4_inject::run(9, argv)` must return 0.

  **Structure + collision assertions** (SGINJ-07 core; Phase 5 walker precedent): reload `Variable::loadMap(outPath)`, `collectExprs` from outputs, count `OpType_SGFP4Dequant` == 2; per op read `main_as_SGFP4DequantParam()` `external{offset,size}` + `dims`; assert dims as in `<behavior>` Test 1; assert disjoint/aligned; read the sidecar file into memory and `memcmp(sidecar.data()+offset, src, size) == 0` per node.

  **Classic-API run** (D-07): copy `runClassicSession`/named-I/O flow from Phase 6 (`input` count 1, `output` count 1; note output width is now 64); fill the session input with the deterministic LCG input used for the baseline; `runSession` → copy output → `checkVectorByRelativeError<float>` vs baseline.

  Register: `MNNTestSuiteRegister(SGFP4MultiTensorTest, "op/sgfp4/multi_tensor");`. Extend the cleanup guard to both niche dirs + all artifacts (Phase 6 lambda idiom). Implement with 2-3 files in the do/while(false)+break pattern per file style.
  </action>
  <verify>
    <automated>cd .build; cmake ..; cmake --build . --target run_test.out; .\run_test.out op/sgfp4/multi_tensor</automated>
    <!-- cmake .. re-globs test/CMakeLists.txt to pick up the new file; re-apply the FP4ModelTest.cpp vcxproj filter (Pitfall 3) between reconfigure and build -->
  </verify>
  <acceptance_criteria>
  - File compiles into `run_test.out` after the standard reconfigure (`cmake ..` — new file must be re-globbed) + re-applied `FP4ModelTest.cpp` vcxproj filter (Pitfall 3) + `cmake --build . --target run_test.out`, all BEFORE `run_test.out` is invoked — the suite must execute the freshly compiled test file, never a stale binary. `.\run_test.out op/sgfp4/multi_tensor` passes all cases.
  - Source contains `MNNTestSuiteRegister(SGFP4MultiTensorTest, "op/sgfp4/multi_tensor");` (exact string) and `sgfp4_inject::run(9,` and `kStructuredMixedData` and `kStructuredMixedCount`.
  - The suite asserts: 2 dequant ops; disjoint ranges; `offset % 16 == 0`; `memcmp` equality per range; `input`/`output` named I/O; relative-error parity over 64 outputs vs the pre-injection FP32 baseline.
  - No test reads gnus-poc at runtime; no env-var skips.
  </acceptance_criteria>
  <done>SGINJ-07 proven (2 containers → 1 artifact, collision-free byte-identical ranges, classic run parity) and the structured-half of SGINJ-08 proven (MIXED container decoded through inject→classic run) in one suite per D-04/D-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-11 atomicity regression + malformed-input probe suite (op/sgfp4/malformed_inputs)</name>
  <files>test/op/SGFP4MultiTensorTest.cpp</files>
  <read_first>
  - test/op/SGFP4MultiTensorTest.cpp (Task 1 state of the file — reuse its helpers)
  - test/op/SGFP4ClassicAPITest.cpp (SGFP4ClassicAPIMissingSidecarTest failure-probe shape, lines ~444-508; fileExists idiom)
  - tools/fp4/sgfp4_inject_core.hpp (loadNicheDir validation branches + post-D-11 failCleanup sites — probe expectations must map to these)
  - .planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md (Q4 probe table: construction + current behavior per probe)
  </read_first>
  <behavior>
  - Probe matrix (each: fresh temp paths, expect `sgfp4_inject::run != 0`, MNN_ERROR diagnostic emitted, then assert `!fileExists(outPath) && !fileExists(outPath + ".weight")`):
    1. Empty container (0-byte file, sha over empty bytes).
    2. Truncated container (first 15 bytes of the valid uniform container, sha recomputed).
    3. Bad sha256 (valid container, one hex digit of the manifest sha flipped).
    4. Legacy v1/magic: (a) valid container with `bytes[0] ^= 0xFF`, sha recomputed; (b) valid magic but version byte 0x01, sha recomputed.
    5. Manifest missing fields: omit `sha256`; omit `path`; omit `stats.shape`; `shape` rank-3; non-positive dim.
    6. Zero-match: single-niche run against the 2-weight base model with `stats.shape [256,256]` (matches nothing).
    7. Multi-match: base model with TWO [512,512] weights + one [512,512] niche → "found 2" hard fail (D-08 behavior lock).
    8. Garbage body bytes (D-10): valid uniform container with a nibble-payload byte of record 0 flipped AND sha256 recomputed + manifest rewritten (otherwise degrades into probe 3) — must not crash; exit ≠ 0; NO output files remain (the D-11 atomicity regression, per Plan 07-01's failCleanup).
    - Anti-case (dim-identity): a "dims disagreement" probe is UNREACHABLE by construction (pairing key and `dims` come from the same manifest `stats.shape` — D-14 cross-check); document this in a comment rather than inventing a fake case.
  - After every probe: cleanup removes all probe temp files/dirs.
  </behavior>
  <action>
  Add `SGFP4MalformedInputsTest` to the SAME file (D-12), registered exactly `MNNTestSuiteRegister(SGFP4MalformedInputsTest, "op/sgfp4/malformed_inputs");`. One looping test case over a probe table (per RESEARCH Q6 organization recommendation): each iteration = fresh `time+rand` output path + niche dir (Pitfall 6), build the probe's container/manifest per the behavior table (reusing Task 1's `writeNicheDir` with parameters + a mutation hook), run `sgfp4_inject::run` in-process (single-niche, the 2-weight base model from Task 1 except probe 7, which builds its own two-same-shape-weight model), assert exit ≠ 0, assert both output files absent via the `fileExists` idiom, cleanup. Per-probe failure diagnostics include the probe index/name.

  Key construction details: probe 2/4 mutations MUST recompute the manifest sha (`sgfp4::sha256_hex` over the mutated bytes) or they silently test probe 3 instead (RESEARCH anti-pattern); probe 8's garbage must stay within framing (payload byte, not header/magic bytes) so framing/sha/version gates pass and execution reaches sidecar-write/verify where D-11 cleanup must fire; "must not crash" = the run() returns (any int) — bounds-checked decode + error-code Execution guarantee this, the probe pins it (Critical Failure Mode 4).

  Reuse all Task 1 helpers (container builder, writeNicheDir, tempPath, makeDir, fileExists, cleanup guard). No new files.
  </action>
  <verify>
    <automated>cd .build; cmake --build . --target run_test.out; .\run_test.out op/sgfp4/malformed_inputs; .\run_test.out op/sgfp4</automated>
    <!-- same file as Task 1 — no re-glob needed, but run_test.out MUST be rebuilt before both filtered runs so they execute the new suite -->
  </verify>
  <acceptance_criteria>
  - `.\run_test.out op/sgfp4/malformed_inputs` passes; per-probe diagnostics name probe indices.
  - Source contains the exact string `MNNTestSuiteRegister(SGFP4MalformedInputsTest, "op/sgfp4/malformed_inputs");` and probes covering: empty, truncated, bad-sha, v1/magic (both byte-level variants), manifest missing each required field + rank-3 + non-positive shape, zero-match, multi-match (2× [512,512]), garbage-body with recomputed sha.
  - Every probe asserts `run() != 0` AND absence of both `out.mnn` and `out.mnn.weight` (D-11 regression proof against Plan 07-01's `failCleanup`).
  - File contains a comment noting the "dims disagreement" probe is unreachable by construction (collapses into zero-match; RESEARCH Q4 note 6).
  - `.\run_test.out op/sgfp4` full family green — final count 9 suites (classic_api, classic_api_missing_sidecar, inject, multi_tensor, malformed_inputs + pre-existing v1.0 sgfp4 family members `cpu`, `vulkan_uniform_parity`, etc. unchanged).
  </acceptance_criteria>
  <done>SGINJ-08's clean-failure criterion is enforced: the full malformed matrix exits non-zero with diagnostics, never crashes, and leaves zero partial artifacts (D-09/D-10/D-11); multi-match hard-fail behavior locked (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe bytes (container/manifest) → sgfp4_inject::run() | deliberately malformed untrusted inputs cross here — exactly what the suite weaponizes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | malformed containers/manifests | mitigate | probes 1-5 assert existing gates fire (sha256, byte-level version gate, manifest field validation) with non-zero exit |
| T-07-07 | Denial of Service | garbage payload reaching sidecar-write/verify → partial artifact | mitigate | probe 8 asserts no-crash + D-11 no-partial-output regression against failCleanup (Plan 07-01) |
| T-07-08 | Tampering | sidecar offset corruption between containers | mitigate | multi-tensor suite's disjoint-range + memcmp byte-identity assertions (Critical Failure Mode 2) |
</threat_model>

<verification>
- Per task: `.\run_test.out op/sgfp4/multi_tensor`, then `.\run_test.out op/sgfp4/malformed_inputs`.
- Phase gate: `.\run_test.out op/sgfp4` full family green; `cmake --build . --target sgfp4_inject.out` still green after the test addition (core-header edit + new include).
- Standard environment procedure: after any cmake reconfigure re-apply the `FP4ModelTest.cpp` vcxproj filter (Pitfall 3) — local-only, no tracked change.
</verification>

<success_criteria>
- Both suites green; family count grows by exactly 2.
- All Phase 7 success criteria observably true: (1) multi-container disjoint ranges + classic run (Criteria 1), (2) structured MIXED end-to-end via real-encoder fixture + clean malformed failures with no partial artifacts (Criteria 2/3).
- Phase 6 `classic_api*` suites still green after the core-header edit (Pitfall 1).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

- `test/op/SGFP4MultiTensorTest.cpp` — new compilation unit; symbols created: `SGFP4MultiTensorTest` (registered `"op/sgfp4/multi_tensor"`), `SGFP4MalformedInputsTest` (registered `"op/sgfp4/malformed_inputs"`), file-local helpers (generalized `buildContainerUniform64(dimO, dimI, out)`, parameterized `writeNicheDir`, probe table).
- New suite registration strings feeding filtered-suite verification: `op/sgfp4/multi_tensor`, `op/sgfp4/malformed_inputs`.
- Consumes (does not create): `kStructuredMixedData`/`kStructuredDimO`/`kStructuredDimI`/`kStructuredSize`/`kStructuredMixedCount` (07-02), `sgfp4_inject::run` + `failCleanup` behavior (07-01).
</artifacts_produced>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-03-SUMMARY.md` when done
</output>
