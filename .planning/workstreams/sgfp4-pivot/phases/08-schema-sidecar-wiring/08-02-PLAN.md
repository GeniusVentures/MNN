---
phase: 08-schema-sidecar-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/op/SGFP4TestUtil.hpp
  - test/op/SGFP4ClassicAPITest.cpp
  - test/op/SGFP4MultiTensorTest.cpp
  - test/op/SGFP4InjectTest.cpp
autonomous: true
requirements: [SGV2-22, SGV2-23]

must_haves:
  truths:
    - "One shared header SGFP4TestUtil.hpp holds the file-system, serialization, and container-builder helpers"
    - "The shared container builder uses the region-relative offset convention (SGFP4MultiTensorTest.cpp:169-215), not the absolute-offset variant"
    - "The three existing SGFP4 test files compile and pass after retrofit onto the shared helpers"
  artifacts:
    - path: "test/op/SGFP4TestUtil.hpp"
      provides: "shared inline helpers: tempPath, cwdPath, makeDir, removeDir, fileExists, writeU32Le, writeBytes, readBytes, buildContainerUniform64(dimO, dimI, out), writeNicheDir"
      contains: "buildContainerUniform64"
    - path: "test/op/SGFP4MultiTensorTest.cpp"
      provides: "source of truth for the generalized region-relative builder (retrofitted to call the shared one)"
      contains: "sgfp4_test::buildContainerUniform64"
    - path: "test/op/SGFP4ClassicAPITest.cpp"
      provides: "retrofitted to the shared region-relative builder (retires the absolute-offset copy)"
      contains: "sgfp4_test::buildContainerUniform64"
  key_links:
    - from: "test/op/SGFP4ClassicAPITest.cpp"
      to: "test/op/SGFP4TestUtil.hpp"
      via: "#include \"SGFP4TestUtil.hpp\""
      pattern: "buildContainerUniform64"
    - from: "test/op/SGFP4TestUtil.hpp"
      to: "MNN/SGFP4DequantUtils.hpp"
      via: "sgfp4_align16 / kSGFP4* framing constants"
      pattern: "sgfp4_align16"
---

<objective>
Extract the duplicated helpers from `SGFP4ClassicAPITest.cpp` / `SGFP4MultiTensorTest.cpp` / `SGFP4InjectTest.cpp` into a single header-only `test/op/SGFP4TestUtil.hpp`, using the generalized region-relative container builder from `SGFP4MultiTensorTest.cpp:169-215` as the canonical implementation, and retrofit the three files onto it. This retires the v2.0 audit's test-helper-dedup debt early (D-10) and guarantees the Phase 8 buffer-mode (08-05) and converter round-trip (08-06) tests are born on the correct offset convention.

Purpose: Eliminate the duplication that caused the W-1 offset-convention divergence (absolute vs region-relative record offsets) before any new tests copy it.

Output: New `test/op/SGFP4TestUtil.hpp`; three existing test files retrofitted; all three suites still green.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-PATTERNS.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-VALIDATION.md
@test/op/SGFP4MultiTensorTest.cpp
@test/op/SGFP4ClassicAPITest.cpp
@test/op/SGFP4InjectTest.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SGFP4TestUtil.hpp with the canonical region-relative builder</name>
  <files>test/op/SGFP4TestUtil.hpp</files>
  <read_first>
    - test/op/SGFP4MultiTensorTest.cpp (lines 100-215: the canonical filesystem helpers + generalized region-relative buildContainerUniform64(dimO, dimI, out) + writeNicheDir)
    - test/op/SGFP4ClassicAPITest.cpp (lines 100-170: the DUPLICATED helpers — note its buildContainerUniform64 writes ABSOLUTE offsets `kRecordRegionStart + b*kRecordSize`; do NOT copy this one)
    - test/op/SGFP4InjectTest.cpp (its tempPath/writeU32Le/etc. duplicates)
    - include/MNN/SGFP4DequantUtils.hpp (framing constants the builder depends on)
    - tools/fp4/sha256.hpp (sgfp4::sha256_hex used by writeNicheDir)
  </read_first>
  <action>
    Create `test/op/SGFP4TestUtil.hpp` (header-only, include guard `#ifndef SGFP4TestUtil_hpp`). Put every helper in `namespace sgfp4_test { ... }` and mark every function `inline` (C++11; header included by multiple TUs → no ODR violation). The functions to extract, with exact names/signatures: `std::string tempPath(const char* prefix, const char* suffix)`, `std::string cwdPath()`, `bool makeDir(const std::string&)`, `void removeDir(const std::string&)`, `bool fileExists(const std::string&)`, `void writeU32Le(std::vector<uint8_t>& out, size_t offset, uint32_t value)`, `bool writeBytes(const std::string& path, const uint8_t* data, size_t size)`, `bool readBytes(const std::string& path, std::vector<uint8_t>& out)`, `bool buildContainerUniform64(int dimO, int dimI, std::vector<uint8_t>& out)`, and `bool writeNicheDir(const std::vector<uint8_t>& containerBytes, const std::string& dir, const std::string& containerName, int dimO, int dimI)`. The `buildContainerUniform64` body is the GENERALIZED region-relative version from `SGFP4MultiTensorTest.cpp:169-215` (recordCount = (dimO/64)*(dimI/64); offset-table entries are `b * kRecordSize`, i.e. RELATIVE to the record-region start — NOT `kRecordRegionStart + b*kRecordSize`). Include headers `MNN/SGFP4DequantUtils.hpp` and `fp4/sha256.hpp`. Per D-13, this is ONLY the helper dedup — do NOT add the W-1 full classic_api behavior retrofit or W-2 failCleanup here.
  </action>
  <acceptance_criteria>
    - `test/op/SGFP4TestUtil.hpp` contains `namespace sgfp4_test` and all ten functions listed above, each prefixed `inline`.
    - The `buildContainerUniform64` body writes offset-table entries as `static_cast<uint32_t>(b * kRecordSize)` (region-relative), NOT `kRecordRegionStart + b * kRecordSize`.
    - The header includes `MNN/SGFP4DequantUtils.hpp` and `fp4/sha256.hpp`; it does NOT include `MNNTestSuite.h` (pure helpers, no test-suite dependency).
    - `grep -v '^//' test/op/SGFP4TestUtil.hpp | grep -c "inline"` is ≥ 10.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "b \* kRecordSize" test/op/SGFP4TestUtil.hpp</automated>
  </verify>
  <done>SGFP4TestUtil.hpp exists with inline sgfp4_test helpers; the region-relative builder is canonical; no test-suite dependency introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit the three test files onto SGFP4TestUtil.hpp and re-run their suites</name>
  <files>test/op/SGFP4ClassicAPITest.cpp, test/op/SGFP4MultiTensorTest.cpp, test/op/SGFP4InjectTest.cpp</files>
  <read_first>
    - test/op/SGFP4TestUtil.hpp (created in Task 1)
    - test/op/SGFP4MultiTensorTest.cpp (its local helper block to delete; lines 100-215)
    - test/op/SGFP4ClassicAPITest.cpp (its local helper block to delete; lines 100-170 — incl. the absolute-offset buildContainerUniform64 to be REPLACED)
    - test/op/SGFP4InjectTest.cpp (its local helper block to delete)
  </read_first>
  <action>
    In each of the three files: (1) add `#include "SGFP4TestUtil.hpp"` alongside the existing includes; (2) DELETE the now-duplicated local helpers (tempPath, cwdPath, makeDir, removeDir, fileExists, writeU32Le, writeBytes, readBytes, buildContainerUniform64, writeNicheDir) and their file-scope framing constants where they only served those helpers; (3) update call sites to `sgfp4_test::` qualified names. CRITICAL for `SGFP4ClassicAPITest.cpp`: replace its local ABSOLUTE-offset `buildContainerUniform64(std::vector<uint8_t>& out)` (which wrote `kRecordRegionStart + b*kRecordSize` and fixed `kRecordCount=64`/`kMatrixDim=512`) with the shared `sgfp4_test::buildContainerUniform64(kMatrixDim, kMatrixDim, out)` region-relative builder — this is the D-10 pull-forward of the W-1 fix. Keep every non-helper constant (`kMatrixDim`, `kLcg*`, `kParityRelativeTolerance`, `kRecordCount`) that the test logic still references, and keep `writeNicheDir` call sites but note `SGFP4ClassicAPITest`'s local `writeNicheDir` (fixed container name `phase6_fixture.sgfp4`) must be replaced by the shared `sgfp4_test::writeNicheDir(containerBytes, dir, "phase6_fixture.sgfp4", kMatrixDim, kMatrixDim)` to preserve its exact artifact layout. Rebuild and run all three suites.
  </action>
  <acceptance_criteria>
    - Each of the three files contains exactly one `#include "SGFP4TestUtil.hpp"` and NO remaining local definition of `buildContainerUniform64`, `tempPath`, or `writeU32Le`.
    - `SGFP4ClassicAPITest.cpp` contains a call `sgfp4_test::buildContainerUniform64(kMatrixDim, kMatrixDim, ...)` (or equivalent) — the absolute-offset local variant is gone.
    - `.\build\run_test.out op/sgfp4/classic_api`, `.\build\run_test.out op/sgfp4/multi_tensor`, `.\build\run_test.out op/sgfp4/inject`, and `.\build\run_test.out op/sgfp4/malformed_inputs` all exit 0.
    - `grep -c "static.*buildContainerUniform64\|bool buildContainerUniform64"` across the three files returns 0 (only the shared header defines it).
  </acceptance_criteria>
  <verify>
    <automated>.\build\run_test.out op/sgfp4/classic_api; .\build\run_test.out op/sgfp4/multi_tensor; .\build\run_test.out op/sgfp4/inject</automated>
  </verify>
  <done>Three files retrofitted onto SGFP4TestUtil.hpp; classic_api now uses the region-relative builder; all four affected suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan is test-infrastructure refactoring; no new untrusted input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | offset-convention divergence (W-1 bug class) | mitigate | The shared builder is the region-relative `SGFP4MultiTensorTest` variant (verified by the `b * kRecordSize` grep gate); the absolute-offset copy in classic_api is deleted, not carried into the shared header. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase. |
</threat_model>

<verification>
- `grep -n "b \* kRecordSize" test/op/SGFP4TestUtil.hpp` shows the region-relative entry (Task 1).
- All four suites (`classic_api`, `multi_tensor`, `inject`, `malformed_inputs`) exit 0 (Task 2).
</verification>

<success_criteria>
- One shared `SGFP4TestUtil.hpp` with the correct region-relative builder; three files retrofitted; all affected suites green; no absolute-offset builder remains.
</success_criteria>

## Artifacts this phase produces

- `test/op/SGFP4TestUtil.hpp` — new header with `namespace sgfp4_test` and inline functions: `tempPath`, `cwdPath`, `makeDir`, `removeDir`, `fileExists`, `writeU32Le`, `writeBytes`, `readBytes`, `buildContainerUniform64`, `writeNicheDir`.

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-02-SUMMARY.md` when done
</output>
