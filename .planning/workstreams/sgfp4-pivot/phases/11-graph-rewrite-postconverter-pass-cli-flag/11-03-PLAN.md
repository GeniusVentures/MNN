---
phase: "11-graph-rewrite-postconverter-pass-cli-flag"
plan: "03"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tools/converter/source/common/cli.cpp
  - tools/converter/source/MNNConverter.cpp
autonomous: true
requirements: [SGV2-29]
must_haves:
  truths:
    - "D-04: `--sgfp4` is a boolean cxxopts entry in cli.cpp's option table (no value argument) whose parse sets modelConfig::useSGFP4 = true — exact --hqq/--fp16 precedent shape; help text says \"SGFP4 v2\", never \"Ultra FP4\" (locked terminology)"
    - "D-05: at the end of initializeMNNConvertArgs (after all flag resolution, before `return true`), useSGFP4 combined with weightQuantBits != 0 OR useHQQ OR saveHalfFloat produces MNN_ERROR(\"--sgfp4 cannot be combined with --weightQuantBits, --hqq, or --fp16\") and returns false"
    - "OQ1 resolved: MNNConverter.cpp main returns 1 (not 0) when initializeMNNConvertArgs returns false, making the D-05 mutex's non-zero exit actually reachable"
    - "D-05 mutex is the ONLY new CLI behavior; every existing flag combination parses exactly as before (help/version paths, which also return false, now also exit 1 — a semantically correct side effect of the OQ1 fix)"
    - "pymnn/src/MNNTools.cc is NOT modified and remains correct (it calls initializeMNNConvertArgs directly and checks res itself)"
  artifacts:
    - path: "tools/converter/source/common/cli.cpp"
      provides: "--sgfp4 option-table entry + parse line + D-05 mutex block"
      contains: "--sgfp4 cannot be combined"
    - path: "tools/converter/source/MNNConverter.cpp"
      provides: "non-zero exit on parse failure"
      contains: "return 1;"
  key_links:
    - from: "tools/converter/source/common/cli.cpp"
      to: "tools/converter/include/config.hpp"
      via: "modelPath.useSGFP4 = true on --sgfp4 (field created in plan 11-01)"
      pattern: "useSGFP4 = true"
    - from: "tools/converter/source/MNNConverter.cpp"
      to: "tools/converter/source/common/cli.cpp"
      via: "!res → return 1 (exit-code propagation of the mutex's return false)"
      pattern: "return 1;"
---

<objective>
Add the user-facing trigger: boolean `--sgfp4` flag (D-04) wired to `modelConfig::useSGFP4` (field landed in plan 11-01), the D-05 hard mutex rejecting `--sgfp4` combined with `--weightQuantBits`/`--hqq`/`--fp16` with a clear MNN_ERROR and non-zero exit, and the OQ1 exit-code fix in `MNNConverter.cpp` (parse failure → `return 1`) so the mutex's non-zero exit is actually observable.

Purpose: SGV2-29 — the CLI flag trigger. Research KEY Q7 verified NO hard-flag-mutex precedent exists in cli.cpp (nearest is a soft `--hqq` downgrade D-05 rejects), and that `main()` returning 0 on parse failure makes any "non-zero exit" contract unreachable without the one-line fix.
Output: 2 small file edits; MNNConvert accepts `--sgfp4`, rejects conflicting combos with exit 1.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-PATTERNS.md

@tools/converter/source/common/cli.cpp
@tools/converter/source/MNNConverter.cpp
@tools/converter/include/config.hpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: --sgfp4 option + parse + D-05 mutex + exit-code fix</name>
  <files>tools/converter/source/common/cli.cpp, tools/converter/source/MNNConverter.cpp</files>
  <read_first>tools/converter/source/common/cli.cpp (option table :150-345 — the hqq entry :243-245 and fp16 :196-197 precedents; parse block :460-690 — fp16 :466-468, hqq :505-512, weightQuantBits :526-531, dumpPass :560-562 immediately before `return true` :564; MNN_ERROR existing uses e.g. :463), tools/converter/source/MNNConverter.cpp (whole file — 24 lines; the `if (!res) { return 0; }` block :15-18), tools/converter/include/config.hpp (useSGFP4 field from plan 11-01; weightQuantBits default 0 semantics :40)</read_first>
  <action>
Per D-04/D-05 + research KEY Q7 / OQ1:

1. `tools/converter/source/common/cli.cpp` option table — add adjacent to the `hqq` entry (:243-245) a boolean (no cxxopts::value) entry: name `"sgfp4"`, help text naming the format correctly, e.g. `"save conv-family weights as SGFP4 v2 (quadtree-adaptive FP4) via inserted SGFP4Dequant nodes"` — "SGFP4 v2", never "Ultra FP4" (locked terminology).
2. Parse block — near the hqq handling (:505-512 area): `if (result.count("sgfp4")) { modelPath.useSGFP4 = true; }` (no conditional downgrade — unlike hqq, this flag has no prerequisites).
3. D-05 mutex — at the END of `initializeMNNConvertArgs`, after the `dumpPass` block (:560-562) and immediately before `return true;` (:564), so every conflicting flag is already resolved:
   `if (modelPath.useSGFP4 && (modelPath.weightQuantBits != 0 || modelPath.useHQQ || modelPath.saveHalfFloat)) { MNN_ERROR("--sgfp4 cannot be combined with --weightQuantBits, --hqq, or --fp16 (conflicting weight transforms on the same tensors)\n"); return false; }`
   — collective enumeration (discretion: simplest message covering all three). `weightQuantBits` defaults to 0 = unset (config.hpp:40).
4. `tools/converter/source/MNNConverter.cpp` — change `if (!res) { return 0; }` (:16-18) to `return 1;` (OQ1, one line). `pymnn/src/MNNTools.cc:34` calls the function directly and checks `res` itself — verified unaffected; do NOT touch it.

5. Rebuild `MNNConvert` and behavior-verify all four cases:
   a. `MNNConvert --help` — the `--sgfp4` entry appears (proves table entry).
   b. Valid parse smoke: `MNNConvert -f ONNX --modelFile <corpus> --MNNModel tmp.mnn --sgfp4` reaches conversion (full pass-through correctness is plan 11-05's D-13 gate; here exit 0 + no parse error is enough) — the corpus `W:\gnus\models\alexnet_Opset16.onnx` may be used.
   c. Mutex: `MNNConvert -f ONNX --modelFile <any> --MNNModel tmp.mnn --sgfp4 --fp16` → MNN_ERROR text containing `--sgfp4 cannot be combined` AND process exit code 1 (post-fix) AND no output `.mnn` written.
   d. Spot-check `--sgfp4 --hqq` and `--sgfp4 --weightQuantBits 8` also rejected with exit 1.
  </action>
  <verify>Build MNNConvert; the four behavior cases above hold; source assertions: cli.cpp contains the sgfp4 option entry, `modelPath.useSGFP4 = true`, and the mutex condition `modelPath.useSGFP4 && (modelPath.weightQuantBits != 0 || modelPath.useHQQ || modelPath.saveHalfFloat)`; MNNConverter.cpp contains `return 1;` inside the `if (!res)` block</verify>
  <acceptance_criteria>
- `Select-String -Path tools\converter\source\common\cli.cpp -Pattern '"sgfp4"'` matches in BOTH the option table and the parse block; the help string contains `SGFP4 v2` and does NOT contain `Ultra FP4`
- `Select-String -Path tools\converter\source\common\cli.cpp -Pattern "weightQuantBits != 0 \|\| modelPath.useHQQ \|\| modelPath.saveHalfFloat"` matches exactly once (the D-05 mutex)
- `Select-String -Path tools\converter\source\MNNConverter.cpp -Pattern "return 1;"` matches inside the parse-failure block
- `MNNConvert -f ONNX --modelFile W:\gnus\models\alexnet_Opset16.onnx --MNNModel tmp\p11_mutex.mnn --sgfp4 --fp16` prints the MNN_ERROR mutex text and `$LASTEXITCODE` is 1; `tmp\p11_mutex.mnn` does not exist afterwards
- `--sgfp4` alone parses (no mutex error); `--help` output shows the new flag
  </acceptance_criteria>
  <done>SGV2-29 closed: `--sgfp4` parses to useSGFP4, conflicting combos hard-fail with MNN_ERROR + exit 1, and the exit code is genuinely non-zero (OQ1 fixed)</done>
</task>

</tasks>

## Artifacts this phase produces (plan 11-03)

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `--sgfp4` | CLI flag (boolean cxxopts option) | `tools/converter/source/common/cli.cpp` option table |
| `useSGFP4` parse assignment | code | cli.cpp parse block |
| D-05 mutex block | code (end of `initializeMNNConvertArgs`) | cli.cpp |
| parse-failure `return 1` | behavior change in `main` | `tools/converter/source/MNNConverter.cpp` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → converter config | user-supplied flags cross into modelConfig; the mutex is the new validation at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering (config confusion) | conflicting weight-transform flag combos | mitigate | D-05 hard mutex: parse-time MNN_ERROR + return false + (OQ1) exit 1 — fail fast instead of silently picking a winner; the D-02 topology guard (plan 11-01) remains the structural second line |
| T-11-08 | Repudiation | silent failure / exit-code-0 on error | mitigate | OQ1 `return 1` fix makes every parse failure (mutex included) observable to scripts checking ERRORLEVEL |
</threat_model>

<verification>
- `--help` shows `--sgfp4` with "SGFP4 v2" wording
- `--sgfp4` alone parses; each of the three conflicting flags triggers MNN_ERROR + exit 1 + no output file
- Existing flag combinations behave exactly as before (no parse regressions beyond the intended exit-code correction on help/version/parse-failure paths)
</verification>

<success_criteria>
- SGV2-29 closed at the CLI level; mutex observable as error text + exit code; pymnn path untouched and unaffected
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-03-SUMMARY.md` when done
</output>
