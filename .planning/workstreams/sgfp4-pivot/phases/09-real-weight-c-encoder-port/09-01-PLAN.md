---
phase: "09-real-weight-c-encoder-port"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fp4/sgfp4_encode.hpp
  - tools/fp4/sgfp4_encode.cpp
  - tools/fp4/CMakeLists.txt
  - test/CMakeLists.txt
  - CMakeLists.txt
autonomous: true
requirements: [SGV2-24, SGV2-25]
must_haves:
  truths:
    - "sgfp4_encode::encode(weights, dimO, dimI) returns a non-empty vector for valid finite inputs"
    - "Encoder rejects NaN/Inf weights with an empty-vector return"
    - "Encoder handles non-64-aligned {dimO, dimI} via internal zero-padding to 64-multiples"
    - "All layout types are produced (LAYOUT_MIXED, LAYOUT_FULL_4X4 per D-11b, uniform layouts 0-3)"
    - "sgfp4_encode STATIC lib target builds cleanly; run_test.out links against it when MNN_BUILD_SGFP4_TOOLS=ON"
  artifacts:
    - path: "tools/fp4/sgfp4_encode.hpp"
      provides: "one-shot public encode API"
      contains: "std::vector<uint8_t> encode("
    - path: "tools/fp4/sgfp4_encode.cpp"
      provides: "full encoder implementation"
    - path: "tools/fp4/CMakeLists.txt"
      provides: "sgfp4_encode STATIC lib target; explicit inject tool source list (no glob double-compile)"
      contains: "add_library(sgfp4_encode STATIC"
    - path: "test/CMakeLists.txt"
      provides: "run_test.out links sgfp4_encode when target is available"
      contains: "sgfp4_encode"
  key_links:
    - from: "tools/fp4/sgfp4_encode.cpp"
      to: "include/MNN/SGFP4DequantUtils.hpp"
      via: "#include MNN/SGFP4DequantUtils.hpp"
      pattern: "kSGFP4Magic|kSGFP4Version"
    - from: "tools/fp4/sgfp4_encode.cpp"
      to: "3rd_party/half/half.hpp"
      via: "#include half.hpp (vendored)"
      pattern: "half_float::half"
    - from: "test/op/SGFP4EncodeTest.cpp"
      to: "tools/fp4/sgfp4_encode.hpp"
      via: "lib link (sgfp4_encode) + include"
      pattern: "sgfp4_encode::encode"
---

<objective>
Implement the SGFP4 v2 adaptive quadtree encoder as tools/fp4/sgfp4_encode.hpp/.cpp (D-03/D-09) and wire it into the CMake build as a STATIC lib linked by both sgfp4_inject.out and run_test.out (Research Q3 / PATTERNS Anti-pattern: glob double-compile).

The encoder mirrors fp4_exporter.py --adaptive exactly: DEFAULT_V2_THRESHOLDS, separable Gaussian Laplacian weighting (sigma-per-level, reflect boundary, truncate=4.0), FP4_AFFINE/T158_AFFINE code modes, full _classify_layout including LAYOUT_FULL_4X4 (D-11b), zero-pad non-64-aligned inputs internally (D-06/SGV2-25), and assembles the SGF4|0x02 container that the existing decoders consume unchanged.

Purpose: Foundation artifact for all other Phase 9 plans — fixture generator, parity tests, and Vulkan parity tests all invoke sgfp4_encode::encode.
Output: sgfp4_encode.hpp, sgfp4_encode.cpp, updated CMakeLists (three files).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sgfp4_encode.hpp + sgfp4_encode.cpp</name>

  <read_first>
    W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\fp4_exporter.py (FP4Exporter class, DEFAULT_V2_THRESHOLDS, _zero_pad, v2 container layout)
    W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\quadtree.py (QuadtreeEncoder._try_block, _classify_layout, _combined_gate_error, _kHysteresisSlack=1.1 — _kHysteresisImprovement is dead code)
    W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\laplacian.py (LaplacianWeightedError.compute — sigma levels, gaussian_filter mode='reflect' truncate=4.0)
    W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\sgfp4_format.py (CodeMode, Layout enums, ALIGNMENT=16, MACROBLOCK_SIZE=64, MIN_LEAF_SIZE=4, SGFP4_MAGIC, SGFP4_VERSION_V2)
    include/MNN/SGFP4DequantUtils.hpp (kSGFP4Magic, kSGFP4Version, kSGFP4Alignment, sgfp4_align16, layout enums, leaf-header constants, kSGFP4SplitMapWords — REUSE THESE, never redefine)
    tools/fp4/sgfp4_inject_core.hpp (include-guard pattern, namespace pattern, #include list)
    3rd_party/half/half.hpp (half_float::half for float-to-FP16 — mirror of decoder's usage)
    .planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-PATTERNS.md §Pattern 1, Pattern 2, Anti-Patterns, RESEARCH Pitfalls 1-5
  </read_first>

  <action>
    Create tools/fp4/sgfp4_encode.hpp with include guard TOOLS_FP4_SGFP4_ENCODE_HPP, includes (&lt;cstdint&gt;, &lt;vector&gt;, "MNN/SGFP4DequantUtils.hpp"), and namespace sgfp4_encode with single public function:
    std::vector&lt;uint8_t&gt; encode(const float* weights, int dimO, int dimI);
    The docstring must state: mirrors fp4_exporter.py --adaptive; DEFAULT_V2_THRESHOLDS; returns empty vector on invalid input (non-finite weights, non-positive dims, dims exceeding 65536).

    Create tools/fp4/sgfp4_encode.cpp implementing the following components in an anonymous namespace (all helpers private):

    SECURITY GATE (first thing encode() does, per D-10 + ASVS V5):
    Reject dimO &lt;= 0, dimI &lt;= 0, dimO &gt; 65536, dimI &gt; 65536 → return {}.
    Scan all dimO*dimI weights via std::isfinite; any non-finite → return {}.
    Guard maxabs == 0.0f in fit functions → scale = 1.0 (not division-by-zero).
    Use size_t for all allocation products.

    FRAMING CONSTANTS: Include "MNN/SGFP4DequantUtils.hpp" and use MNN::kSGFP4Magic, MNN::kSGFP4Version (0x02), MNN::kSGFP4Alignment (16), MNN::sgfp4_align16, MNN::kSGFP4LeafHeaderScaleShift (16), MNN::kSGFP4LeafHeaderBiasMask (0xFFF0u), MNN::kSGFP4LeafHeaderModeBit (0x1u), MNN::kSGFP4LayoutMixed (4), MNN::kSGFP4LayoutFull4x4 (5), MNN::kSGFP4SplitMapWords (3), MNN::kSGFP4NibblesPerWord (8), MNN::kSGFP4SymbolsPerWord (16). NEVER redefine any of these.

    WRITE HELPERS (anonymous namespace):
    writeU32Le(std::vector&lt;uint8_t&gt;&amp; buf, size_t offset, uint32_t v) — 4-byte LE write (mirror of PATTERNS.md SGFP4TestUtil.hpp excerpt).
    appendU32Le(std::vector&lt;uint8_t&gt;&amp; buf, uint32_t v) — push_back × 4 LE bytes.
    floatToHalfBits(float v) → uint16_t — clamp to [-65504,65504], construct half_float::half, memcpy to uint16_t (mirrors fp4_exporter._float_to_half).
    packLeafHeader(uint16_t sBits, uint16_t bBits, int mode) → uint32_t — (sBits&lt;&lt;kSGFP4LeafHeaderScaleShift) | (bBits &amp; ~0xFu) | (mode &amp; kSGFP4LeafHeaderModeBit). NOTE: clear low 4 bits of bBits to zero before OR (Pitfall 4).

    GAUSSIAN FILTER (necessary hand-roll — RESEARCH Pitfall 1 / "Don't Hand-Roll" table exception):
    gaussianKernel1D(double sigma) → std::vector&lt;double&gt;: radius = int(4.0 * sigma + 0.5); kernel = exp(-(i^2)/(2*sigma^2)) for i in [-radius, radius]; normalize to sum=1.0.
    gaussianFilter2D(const std::vector&lt;float&gt;&amp; src, int rows, int cols, double sigma) → std::vector&lt;float&gt;: separable convolution (rows then cols); boundary = reflect (index mirror without edge repeat: reflect(i, n) = i &lt; 0 ? -i : i &gt;= n ? 2*n-2-i : i).
    laplacianWeightedError(const std::vector&lt;float&gt;&amp; block, int rows, int cols) → double: mirror LaplacianWeightedError.compute from laplacian.py; levels = {4:0, 8:0, 16:1, 32:2, 64:3}[rows]; for each level: sigma = 2.0 * pow(2.0, level); smooth_base = gaussianFilter2D(smooth, sigma); band = smooth - smooth_base; total_error += (1.0/pow(2.0,level)) * mean(band^2); subsample smooth = smooth_base[::2,::2] when level &lt; levels-1.

    AFFINE/TERNARY FIT (all internal arithmetic in double — Pitfall 3):
    struct LeafResult { int mode; double error; uint16_t scaleBits; uint16_t biasBits; std::vector&lt;int8_t&gt; codes; }
    fitAffine(const double* vals, int n) → LeafResult: bias = mean(vals) (double); 16 scale candidates = {maxabs/7, maxabs/6, ..., maxabs/1, maxabs*2/15, ...} per fp4_exporter._fit_affine; for each candidate: codes = clip(rint((vals-bias)/scale), -8, 7) — USE std::rint NOT std::round (Pitfall 2); error = MSE(vals, decode); pick min-error candidate. Pack into LeafResult with scaleBits = floatToHalfBits(scale), biasBits = floatToHalfBits(bias), mode=0.
    fitTernary(const double* vals, int n) → LeafResult: ternary_delta = 0.10; positive threshold = ternary_delta * maxabs; codes in {-1, 0, +1} mapped to T158 symbols; scale = mean(|vals[codes!=0]|) or 1.0 if all zero; error = MSE; mode=1.
    encodedLeaf(const std::vector&lt;float&gt;&amp; vals) → LeafResult: compute fitAffine + fitTernary; return lower-error result.

    NIBBLE PACK:
    packNibbles(const std::vector&lt;int8_t&gt;&amp; codes, int leafSize) → std::vector&lt;uint8_t&gt;: FP4 nibble-pack per SGFP4DequantUtils.hpp kSGFP4NibblesPerWord=8 (4-bit signed, two nibbles per byte); pad payload to kSGFP4Alignment bytes with zeros.

    QUADTREE (mirror QuadtreeEncoder._try_block from quadtree.py):
    struct Leaf { int y; int x; int size; LeafResult result; }
    DEFAULT_V2_THRESHOLDS: array of 5 entries {leaf_size, max_mse, max_relative}: {64, 0.01, 0.05}, {32, 0.005, 0.03}, {16, 0.002, 0.02}, {8, 0.001, 0.01}, {4, 0.0005, 0.005}. min_leaf_size = 4.
    combinedGateError(double selected_error, double mse, double max_mse, double max_relative) → double: signal_power = mean(vals^2); if signal_power &lt;= 1e-12 return selected_error; relative_equivalent = max_mse * (selected_error / signal_power) / max_relative; return max(selected_error, relative_equivalent). Mirrors quadtree.py _combined_gate_error exactly.
    tryBlock(paddedPlane, paddedDimI, sy, sx, size, leaves): run laplacianWeightedError; run encodedLeaf; compute gate_error = combinedGateError(leaf.error, ...); threshold = max_mse for this size; accept = gate_error &lt;= threshold; if !accept &amp;&amp; size &gt; 4: accept = gate_error &lt;= threshold * 1.1 (the kHysteresisSlack — NOT 0.8); if size == 4: accept = true (floor). If accept: push leaf. Else: recurse TL/TR/BL/BR (size/2 each). NOTE: _kHysteresisImprovement (0.8) is dead code in the Python — do NOT implement it.
    encodeSuperblock(paddedPlane, paddedDimI, sb_row, sb_col) → std::vector&lt;Leaf&gt;: call tryBlock for full 64×64 region.

    LAYOUT CLASSIFICATION (mirror _classify_layout from fp4_exporter.py — D-11b: include FULL_4X4):
    classifyLayout(const std::vector&lt;Leaf&gt;&amp; leaves) → uint32_t enum value:
      All leaves size==64 → kSGFP4LayoutUniform64 (0)
      All leaves size==32 → kSGFP4LayoutUniform32 (1)
      All leaves size==16 → kSGFP4LayoutUniform16 (2)
      All leaves size==8  → kSGFP4LayoutUniform8 (3)
      All leaves size==4  → kSGFP4LayoutFull4x4 (5)
      Otherwise           → kSGFP4LayoutMixed (4)

    SPLIT MAP (for MIXED layout): buildSplitMap(leaves) → std::array&lt;uint32_t, 3&gt;: 3-word pre-order DFS bitmap of the quadtree (1=split, 0=leaf); mirror fp4_exporter._build_split_map.

    RECORD ASSEMBLY (per superblock):
    assembleSuperblockRecord(leaves, layout) → std::vector&lt;uint8_t&gt;:
      sb_header: uint32_t — encodes layout (low 3 bits) + leaf_count + leaf_mode flags per sgfp4_format.py
      if MIXED: append split_map (3×uint32_t LE = 12 bytes); else: sort leaves row-major (y then x)
      for each leaf: append packLeafHeader(result) as uint32_t + packNibbles(result.codes)
      pad whole record to 16-byte multiple via sgfp4_align16

    CONTAINER ASSEMBLY (mirrors FP4Exporter._export_v2_adaptive container framing):
    assembleContainer(records) → std::vector&lt;uint8_t&gt;:
      B = records.size() (uint32_t; number of superblock records = ceil(dimO/64)*ceil(dimI/64))
      Write: magic[4] (kSGFP4Magic, LE) | version[1] (0x02) | B_u32[4] (LE) | pad0[7] (zeros) — 16 bytes total fixed header
      Append record_offsets table: B × uint32_t LE, offsets relative to start of first record data (after the offset table + pad1)
      pad1: align offset table end to 16-byte boundary
      Append each record bytes sequentially

    PUBLIC encode() FUNCTION:
    1. Security gate (see above)
    2. paddedDimO = ((dimO + 63) / 64) * 64; paddedDimI = ((dimI + 63) / 64) * 64
    3. Allocate paddedPlane (paddedDimO × paddedDimI floats, zero-initialized); copy weights row by row (true rows, true cols) into top-left of padded plane
    4. Iterate superblock grid (sb_r in 0..ceil(dimO/64), sb_c in 0..ceil(dimI/64)) in row-major order; call encodeSuperblock for each
    5. assembleSuperblockRecord per superblock
    6. assembleContainer(records)
    7. Return container bytes
  </action>

  <acceptance_criteria>
    - tools/fp4/sgfp4_encode.hpp exists and contains exactly "std::vector&lt;uint8_t&gt; encode(" (grep -c returns 1)
    - tools/fp4/sgfp4_encode.hpp contains "namespace sgfp4_encode" (grep -c returns 1)
    - tools/fp4/sgfp4_encode.cpp contains "#include \"MNN/SGFP4DequantUtils.hpp\"" (framing constants reused)
    - tools/fp4/sgfp4_encode.cpp does NOT contain "std::round" or "lround" (grep -c returns 0) — only std::rint or std::nearbyint
    - tools/fp4/sgfp4_encode.cpp contains "double" in at least 3 fit-function declarations (accumulation in double per Pitfall 3)
    - tools/fp4/sgfp4_encode.cpp contains "kSGFP4LayoutFull4x4" (D-11b classifyLayout covers FULL_4X4)
    - tools/fp4/sgfp4_encode.cpp contains "half_float::half" (FP16 conversion via vendored half)
    - tools/fp4/sgfp4_encode.cpp contains "1.1" (hysteresis slack) and does NOT contain "0.8" near hysteresis (dead-code guard)
    - tools/fp4/sgfp4_encode.cpp contains "std::isfinite" (NaN/Inf security gate)
    - tools/fp4/sgfp4_encode.cpp contains "65536" (dim bound guard)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "std::vector&lt;uint8_t&gt; encode(" tools/fp4/sgfp4_encode.hpp; grep -c "std::round" tools/fp4/sgfp4_encode.cpp; grep -c "kSGFP4LayoutFull4x4" tools/fp4/sgfp4_encode.cpp; grep -c "std::isfinite" tools/fp4/sgfp4_encode.cpp</automated>
  </verify>

  <done>
    sgfp4_encode.hpp declares encode() in namespace sgfp4_encode; sgfp4_encode.cpp implements all encoder components (Gaussian filter, affine/ternary fit with double accumulation + std::rint, quadtree with 1.1× slack, classifyLayout incl. FULL_4X4, split map, container assembly); zero std::round usage; all framing constants sourced from MNN::kSGFP4* (no local redefinitions); security gate at encode() entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: CMake wiring — sgfp4_encode STATIC lib + run_test.out link</name>

  <read_first>
    tools/fp4/CMakeLists.txt (current 19-line file — the glob that double-compiles)
    test/CMakeLists.txt (current file — target_link_libraries structure)
    CMakeLists.txt (root, lines 960-975 — MNN_BUILD_TEST included BEFORE MNN_BUILD_SGFP4_TOOLS; ordering bug)
    .planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-PATTERNS.md §tools/fp4/CMakeLists.txt, §test/CMakeLists.txt
  </read_first>

  <action>
    TOOLS/FP4/CMAKELISTS.TXT — replace the file(GLOB ...) approach with explicit source lists to eliminate the double-compile anti-pattern:
    Remove: file(GLOB SGFP4FILES ...) and add_executable(sgfp4_inject.out ${SGFP4FILES})
    Add: add_library(sgfp4_encode STATIC ${CMAKE_CURRENT_LIST_DIR}/sgfp4_encode.cpp)
    Add: target_include_directories(sgfp4_encode PUBLIC ${CMAKE_CURRENT_LIST_DIR} ${CMAKE_SOURCE_DIR}/include ${CMAKE_SOURCE_DIR}/3rd_party/half)
    Add: target_compile_definitions(sgfp4_encode PRIVATE $&lt;$&lt;BOOL:${MNN_SUPPORT_TRANSFORMER_FUSE}&gt;:MNN_SUPPORT_TRANSFORMER_FUSE&gt;)
    Add: add_executable(sgfp4_inject.out ${CMAKE_CURRENT_LIST_DIR}/sgfp4_inject.cpp ${CMAKE_CURRENT_LIST_DIR}/sgfp4_inject_core.hpp ${CMAKE_CURRENT_LIST_DIR}/sha256.hpp) — explicit sources, sgfp4_encode.cpp excluded
    Keep: foreach loop for MNN_DEPS link + MSVC flags on sgfp4_inject.out. Do NOT add target_link_libraries(sgfp4_inject.out sgfp4_encode) — sgfp4_inject reads pre-made containers and never calls the encoder (CONTEXT.md Excludes: "any changes to sgfp4_inject").
    Update MNN_SGFP4_TOOLS list to keep only sgfp4_inject.out for the foreach MSVC block.

    ROOT CMAKELISTS.TXT — fix include ordering so sgfp4_encode target exists before test/CMakeLists.txt is processed:
    Locate the IF(MNN_BUILD_SGFP4_TOOLS) block (lines ~970-972) and move it to immediately before the IF(MNN_BUILD_TEST) block (lines ~961-963). This ensures add_library(sgfp4_encode) runs before test/CMakeLists.txt tries to link against it.

    TEST/CMAKELISTS.TXT — link run_test.out to sgfp4_encode when the target exists:
    After the final target_include_directories(run_test.out ...) line, add:
      if(TARGET sgfp4_encode)
        target_link_libraries(run_test.out sgfp4_encode)
      endif()
    This uses the CMake TARGET guard so that test builds without MNN_BUILD_SGFP4_TOOLS=ON still succeed (the SGFP4Encode test files will be compiled in but the link step is skipped — an intentional degenerate case; the fuller build requires both flags).
  </action>

  <acceptance_criteria>
    - tools/fp4/CMakeLists.txt contains "add_library(sgfp4_encode STATIC" (grep -c returns 1)
    - tools/fp4/CMakeLists.txt does NOT contain "file(GLOB" (glob removed)
    - test/CMakeLists.txt contains "sgfp4_encode" in a target_link_libraries or if(TARGET) block
    - Root CMakeLists.txt has MNN_BUILD_SGFP4_TOOLS block appearing before MNN_BUILD_TEST block (grep -n returns SGFP4_TOOLS line &lt; BUILD_TEST line)
    - cmake -B .build -DMNN_BUILD_SGFP4_TOOLS=ON -DMNN_BUILD_TEST=ON -DMNN_SUPPORT_TRANSFORMER_FUSE=ON exits 0
    - cmake --build .build --config Debug exits 0 (no link errors for sgfp4_inject.out or run_test.out)
  </acceptance_criteria>

  <verify>
    <automated>cmake -B .build -DMNN_BUILD_SGFP4_TOOLS=ON -DMNN_BUILD_TEST=ON -DMNN_SUPPORT_TRANSFORMER_FUSE=ON 2>&amp;1 | Select-Object -Last 5; cmake --build .build --config Debug 2>&amp;1 | Select-Object -Last 8; echo "BUILD_EXIT=$LASTEXITCODE"</automated>
  </verify>

  <done>
    tools/fp4/CMakeLists.txt uses add_library(sgfp4_encode STATIC sgfp4_encode.cpp) with explicit inject sources (no glob); sgfp4_inject.out links sgfp4_encode; root CMakeLists.txt includes SGFP4_TOOLS before TEST; run_test.out links sgfp4_encode when target exists; clean cmake configure + build with both flags ON.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → encode() | weights pointer + dims come from caller (PostConverter pass in Phase 11 — trusted but may pass degenerate inputs) |
| encode() internal | all allocation sizes derived from dims — integer overflow risk on 32-bit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | encode() entry — NaN/Inf weight values | mitigate | std::isfinite scan of all weights at entry; return {} on any failure (per ASVS L1 V5 input validation) |
| T-09-02 | Denial of Service | encode() entry — dimO/dimI product overflows size_t | mitigate | reject dimO/dimI &gt; 65536; use static_cast&lt;size_t&gt;(dimO) * dimI for all alloc products |
| T-09-03 | Tampering | fitAffine — maxabs == 0 (all-zero weight plane, division by zero in scale candidates) | mitigate | guard: if (maxabs == 0.0) scale = 1.0 before dividing; produces all-zero codes, round-trips correctly |
| T-09-SC | Tampering | build — package installs | accept | no new packages installed; encoder uses only vendored half.hpp + in-tree SGFP4DequantUtils.hpp |
</threat_model>

<verification>
After both tasks complete:
- cmake --build .build --config Debug exits 0 with MNN_BUILD_SGFP4_TOOLS=ON MNN_BUILD_TEST=ON
- tools/fp4/sgfp4_encode.hpp + sgfp4_encode.cpp present
- grep -c "std::round" tools/fp4/sgfp4_encode.cpp returns 0
- grep -c "add_library(sgfp4_encode STATIC" tools/fp4/CMakeLists.txt returns 1
- grep -c "std::isfinite" tools/fp4/sgfp4_encode.cpp returns &gt;= 1
</verification>

<success_criteria>
1. sgfp4_encode::encode() is callable from C++ with (float*, int, int) → vector&lt;uint8_t&gt;
2. Encoder emits valid SGF4|0x02 framed containers for both 64-aligned and non-64-aligned inputs
3. All five layout variants produced (0-3 uniform + LAYOUT_MIXED + LAYOUT_FULL_4X4)
4. NaN/Inf input → empty vector; maxabs==0 → valid all-zero container
5. Clean build with MNN_BUILD_SGFP4_TOOLS=ON MNN_BUILD_TEST=ON
</success_criteria>

<output>
Create .planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-01-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

| Symbol / File | Type | Notes |
|---------------|------|-------|
| `tools/fp4/sgfp4_encode.hpp` | New file | `namespace sgfp4_encode { std::vector<uint8_t> encode(const float* weights, int dimO, int dimI); }` |
| `tools/fp4/sgfp4_encode.cpp` | New file | Encoder implementation — Gaussian filter, affine/ternary fit (double), quadtree, container assembly |
| `sgfp4_encode` | CMake STATIC lib target | Defined in `tools/fp4/CMakeLists.txt`; linked by `sgfp4_inject.out` and `run_test.out` |
</output>
