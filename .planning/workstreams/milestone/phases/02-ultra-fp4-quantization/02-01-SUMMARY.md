---
phase: 02-ultra-fp4-quantization
plan: 01
subsystem: backend
tags: [vulkan, fp4, glsl, e2m1, dequantization, shader]

# Dependency graph
requires:
  - phase: 01-vulkan-attention-correctness-llm-e2e
    provides: "makeshader.py pipeline, VulkanBasicExecution pattern, VulkanBackend addCreator registration, shader embedding infrastructure"
provides:
  - "FP4 E2M1 dequantization GLSL compute shader with FP16/FP32 dual output"
  - "VulkanFP4Dequant execution class registered under OpType_Dequantize"
  - "Autogenerated AllShader artifacts with fp4_dequant SPIR-V entries"
affects:
  - 02-02 (FP4 dequantization correctness tests)
  - Any FP4 downstream op that requires GPU-side dequant

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "VulkanBasicExecution subclass with per-op FP32/FP16 variant selection"
    - "E2M1 decode in GLSL compute shader with subnormal/normal/special case handling"
    - "2-per-byte FP4 nibble unpacking with little-endian swizzle for GPU coherence"

key-files:
  created:
    - source/backend/vulkan/buffer/execution/glsl/fp4_dequant.comp
    - source/backend/vulkan/buffer/execution/VulkanFP4Dequant.hpp
    - source/backend/vulkan/buffer/execution/VulkanFP4Dequant.cpp
  modified:
    - source/backend/vulkan/buffer/compiler/AllShader.cpp
    - source/backend/vulkan/buffer/shaders/AllShader.h
    - source/backend/vulkan/buffer/compiler/VulkanShaderMap.cpp
    - source/backend/vulkan/buffer/execution/glsl/macro.json

key-decisions:
  - "D-01: E2M1 encoding — 1 sign, 2 exponent, 1 mantissa with bias=1 applied as specified"
  - "D-02: Registered under OpType_Dequantize (existing op type) to reuse loaded shader infrastructure"
  - "D-03: 2 FP4 values packed per byte with little-endian nibble ordering (low nibble first)"
  - "D-04: FP16 default output via mUseFP32Output flag override; pipeline selects glsl_fp4_dequant_FP16_comp vs glsl_fp4_dequant_comp"
  - "D-08: Shader embedded via makeshader.py autogeneration pipeline into AllShader.cpp/h/VulkanShaderMap.cpp"

patterns-established:
  - "Pattern 1: E2M1 dequant in GLSL — FLOAT macro for dual-precision, no raw float types, no #version directive"
  - "Pattern 2: VulkanBasicExecution subclass — constructor selects pipeline variant per precision flag, const buffer via uniform binding 2"
  - "Pattern 3: Descriptor layout — binding 0 (r/o storage: SrcRaw), binding 1 (w/o storage: Dst), binding 2 (uniform: elementCount+srcBytes)"

requirements-completed: [FP4-01, FP4-02, FP4-03, FP4-04]

# Metrics
duration: 3min
completed: 2026-05-27
---

# Phase 2 Plan 1: FP4 Dequantization Shader and Vulkan Execution Class Summary

**E2M1 FP4 dequantization GLSL compute shader with 2-per-byte unpacking, dual FP16/FP32 output, and full Vulkan backend integration via VulkanBasicExecution subclass registered under OpType_Dequantize**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-27T23:20:47Z
- **Completed:** 2026-05-27T23:24:27Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments
- Created fp4_dequant.comp (87 lines) with E2M1 decode (subnormal, normal, Inf/NaN cases), 256-thread workgroup, 3-bindings layout matching VulkanSoftmax uniform buffer pattern
- Regenerated AllShader.cpp (4 fp4_dequant entries), AllShader.h (4 extern declarations), VulkanShaderMap.cpp (2 map insertions) via makeshader.py with FP32+FP16 SPIR-V variants
- Created VulkanFP4Dequant.hpp/.cpp as VulkanBasicExecution subclass with per-op FP32 output flag (D-04), pipeline variant selection, and static addCreator(OpType_Dequantize) registration

## Task Commits

Each task was committed atomically:

1. **Task 1: Create fp4_dequant.comp GLSL compute shader** - `42407e41` (feat)
2. **Task 2: Run makeshader.py to regenerate AllShader artifacts** - `f9ec8f59` (feat)
3. **Task 3: Create VulkanFP4Dequant execution class and register under OpType_Dequantize** - `73e155ea` (feat)

## Files Created/Modified
- `source/backend/vulkan/buffer/execution/glsl/fp4_dequant.comp` — E2M1 dequant compute shader; unpacks 2 FP4 nibbles per byte to FP16/FP32
- `source/backend/vulkan/buffer/execution/VulkanFP4Dequant.hpp` — VulkanBasicExecution subclass header with Creator inner class
- `source/backend/vulkan/buffer/execution/VulkanFP4Dequant.cpp` — Pipeline creation, descriptor binding, dispatch, OpType_Dequantize registration
- `source/backend/vulkan/buffer/compiler/AllShader.cpp` — Regenerated SPIR-V embedding for fp4_dequant (FP32 + FP16)
- `source/backend/vulkan/buffer/shaders/AllShader.h` — Regenerated extern declarations for fp4_dequant
- `source/backend/vulkan/buffer/compiler/VulkanShaderMap.cpp` — Regenerated shader name→binary map
- `source/backend/vulkan/buffer/execution/glsl/macro.json` — Added fp4_dequant.comp entry (useFP16=true)

## Decisions Made
- All implementation decisions followed 02-CONTEXT.md exactly: D-01 (E2M1), D-02 (OpType_Dequantize), D-03 (2-per-byte little-endian), D-04 (FP16 default, FP32 flag)
- Shader uses uniform buffer at binding 2 (matching VulkanSoftmax pattern) rather than push_constant — consistent with existing MNN Vulkan pipeline infrastructure
- Constructor takes explicit `bool useFP32Output` parameter per D-04 rather than deriving from backend global — gives per-op precision control

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — all three tasks executed cleanly with no errors.

## Known Stubs

- `VulkanFP4DequantCreator::onCreate()` defaults `useFP32Output = false` with a TODO comment for future FP32 flag reading from `op->main_as_QuantizedFloatParam()`. This is intentional per D-04 — FP16 is the default for Phase 2 initial implementation.

## Next Phase Readiness
- FP4 dequantization shader and execution class are complete
- Ready for Plan 02: FP4 dequantization correctness tests (VulkanFP4DequantTest)
- All shader artifacts embedded — test code can reference `glsl_fp4_dequant_comp` / `glsl_fp4_dequant_FP16_comp` pipeline keys

---
*Phase: 02-ultra-fp4-quantization*
*Completed: 2026-05-27*
