---
phase: 07-multi-tensor-hardening-structured-data-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fp4/sgfp4_inject_core.hpp
  - tools/fp4/README.md
autonomous: true
requirements: [SGINJ-07, SGINJ-08]
must_haves:
  truths:
    - "A failed sgfp4_inject run leaves NO out.mnn / out.mnn.weight behind (D-11)"
    - "A successful run produces byte-identical output to the pre-change tool (Phase 6 classic_api suites stay green)"
    - "The dims = {dimO, dimI} convention, niche-dir/manifest contract, CLI usage, and sidecar layout are documented in tools/fp4/README.md (D-13)"
  artifacts:
    - path: "tools/fp4/sgfp4_inject_core.hpp"
      provides: "failure-cleanup atomicity in run()"
      contains: "failCleanup"
    - path: "tools/fp4/README.md"
      provides: "tool documentation: dims convention, input contract, CLI, sidecar layout, failure behavior"
      contains: "dimO"
  key_links:
    - from: "tools/fp4/sgfp4_inject_core.hpp::run()"
      to: "std::remove(outputPath) + std::remove(sidecarPath)"
      via: "failCleanup lambda invoked on every failure return after arg validation"
      pattern: "failCleanup\\(\\);\\s*return 1;"
---

<objective>
Harden `sgfp4_inject` for clean failure (D-11 atomicity) and document the tool (D-13).

Purpose: A corrupt/partial artifact would crash the downstream unchecked-nullptr path in `SGProcessingManager` (SGINJ-08 clean-failure half). Today every failure `return 1` AFTER the sidecar `ofstream` opens (sidecar-write, save, and in-tool-verify stages) leaves `out.mnn` + `out.mnn.weight` fully or partially on disk. Fix via Option A failure-cleanup (RESEARCH Q1 recommendation — minimal diff, success path untouched, avoids Windows rename-over-existing portability issues of temp+rename). Also deliver `tools/fp4/README.md` covering exactly the four D-13 content areas plus post-fix failure semantics.

Output: modified `tools/fp4/sgfp4_inject_core.hpp`, new `tools/fp4/README.md`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-PATTERNS.md
@tools/fp4/sgfp4_inject_core.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11 atomicity — failure-cleanup in sgfp4_inject::run()</name>
  <files>tools/fp4/sgfp4_inject_core.hpp</files>
  <read_first>
  - tools/fp4/sgfp4_inject_core.hpp (entire run() — current write ordering, all failure returns)
  - .planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-PATTERNS.md (line anchors: sidecarPath ~line 296, sidecar open ~353-360, verify failures ~394-461, existing std::remove idiom ~line 422)
  </read_first>
  <action>
  Implement Option A failure-cleanup per D-11 and RESEARCH Q1 (planner-confirmed interpretation: cleanup satisfies D-11's "no partial files" outcome; chosen over temp+rename because `std::rename` over an existing destination is not portable on Windows).

  1. Immediately after `const std::string sidecarPath = outputPath + ".weight";` (~line 296), add a lambda `failCleanup` capturing by reference, body exactly: `std::remove(outputPath.c_str()); std::remove(sidecarPath.c_str());`. Match the file's existing `std::remove` idiom (already used for `.verify_N.mnn` temp files at ~line 422).
  2. Prepend `failCleanup();` to EVERY failure `return 1` in `run()` that occurs after that point — concretely the sites at approximately lines 304, 313, 341 (pre-write model/pairing errors), 360 (sidecar open failure), 377 (sidecar write failure), and 408, 424, 429, 435, 441, 447, 461 (save/reload/verify-stage failures). Do NOT touch the usage/arg-validation returns at ~289/294 (outputPath may be empty there; nothing has been decided about output paths yet).
  3. Chosen semantics (document in a short comment above the lambda): a failed run removes ANY files at `outputPath`/`sidecarPath`, including a stale artifact from a previous successful run — downstream consumers must never see an artifact that does not correspond to the inputs of the failed run. This deliberately satisfies D-11 literally: after a failed run, no `out.mnn`/`out.mnn.weight` remains.
  4. Pitfall A5 (Windows `std::remove` on open files): verify every `ofstream` is scope-closed before any cleanup can run — the sidecar write block (~353-375) is already braced; keep it that way; do not hoist the stream out of its scope.
  5. Success path must remain byte-identical (Pitfall 1 — `op/sgfp4/classic_api*` suites and op externalPath baking depend on it): no changes to write order, op construction, naming (`weight_sgfp4` suffix), or the unconditional in-tool verify chain.
  6. Style: Google variant, 4-space indent, 120-col, `MNN_ERROR` diagnostics + `return 1` (exceptions disabled). No change to `tools/fp4/sgfp4_inject.cpp` (thin shim keeps building unchanged).
  </action>
  <verify>
    <automated>cd .build; cmake --build . --target sgfp4_inject.out; cmake --build . --target run_test.out; .\run_test.out op/sgfp4</automated>
  </verify>
  <acceptance_criteria>
  - `tools/fp4/sgfp4_inject_core.hpp` contains the identifier `failCleanup` defined after `sidecarPath` and before the model-load section.
  - Every `return 1` in `run()` after the `sidecarPath` definition (grep line numbers ~304 through ~461) is preceded by a `failCleanup()` call; no `return 1` between ~304 and ~461 lacks it.
  - `cmake --build . --target sgfp4_inject.out` exits 0 (standalone tool still builds).
  - `cmake --build . --target run_test.out` exits 0 and runs BEFORE the test binary — the core-header edit is compiled into `run_test.out` so the classic_api regression executes the recompiled header, never a stale binary (no reconfigure needed: no CMake glob changes in this task).
  - `.\run_test.out op/sgfp4` family run is fully green, in particular `op/sgfp4/classic_api` and `op/sgfp4/classic_api_missing_sidecar` (success-path regression proof per Pitfall 1).
  - `tools/fp4/sgfp4_inject.cpp` is unmodified (git diff shows no change).
  </acceptance_criteria>
  <done>Failed runs can no longer leave partial or stale output artifacts; success path proven byte-identical via the classic_api family regression.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 — tools/fp4/README.md with the four required content areas</name>
  <files>tools/fp4/README.md</files>
  <read_first>
  - tools/fp4/sgfp4_inject_core.hpp (usage() ~147-151, loadNicheDir manifest parsing, exact-shape pairing loop, sidecar-merge block with sgfp4_align16 cursor, makeDequantOp ~261-275, verify chain)
  - include/MNN/SGFP4DequantUtils.hpp (framing constants, sgfp4_is_v2_container, sgfp4_align16)
  - tools/cv/README.md (tool-family README form: short intro → build/usage → parameter sections)
  - .planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-RESEARCH.md (Q7 — README content enumerated with code anchors)
  </read_first>
  <action>
  Create `tools/fp4/README.md` (D-13) covering EXACTLY these four areas, in this order, plus a short failure-behavior section. Structure: intro → CLI usage → niche-dir/manifest input contract → dims convention → sidecar layout → failure behavior. Facts must be taken from the current code, not invented:

  1. **CLI usage**: `sgfp4_inject --model <path> --niche-dir <dir> [--niche-dir <dir>...] --output <path>` (repeat `--niche-dir` once per target weight tensor); emits `<output>` plus `<output>.weight`; note the build target name `sgfp4_inject.out`.
  2. **Niche-dir/manifest input contract**: each `--niche-dir` is an unmodified `fp4_exporter.py --adaptive` output dir containing exactly one `*.sgfp4` plus `manifest.json`; required manifest fields are `fp4_binary.path` (basename cross-checked case-insensitively against the discovered `.sgfp4` — never resolved literally), `fp4_binary.sha256` (integrity over the container bytes), `fp4_binary.stats.shape` (exactly 2 positive ints); state explicitly that the version gate is byte-level (`SGF4` magic + version byte 0x02 via `sgfp4_is_v2_container`) and NEVER trusts the manifest's `fp4_binary.format` label — its `"fp4_ultra_v0.2"` value is a known terminology trap; the format is called **SGFP4 v2**.
  3. **dims convention** (`dims = {dimO, dimI}`, D-13/D-14): `dimO = shape[0]` (output rows), `dimI = shape[1]` (input columns); weights are 2-D `[out, in]` row-matrix ONLY; pairing is exact-shape match against non-input 2-D vars; 64-multiple dims only (non-64-multiple/padding is v3.0 Phase 10); NO transposed-pairing tolerance (rejected Phase 5); 2+ shape matches hard-fail (same-shape disambiguation via tensor-name keying is v3.0 Phase 11).
  4. **Sidecar layout**: single merged `<output>.weight`; per-op ranges non-overlapping; each container placed at a 16-byte-aligned offset (`sgfp4_align16`) and zero-padded to the next multiple; per-op descriptor is `SGFP4DequantParamT{magic = kSGFP4Magic, external = {offset, size}, dims = {dimO, dimI}}` with `op->externalPath` set literally on the op (documented gotcha: `createExecutionWithExternal` does NOT cover this op type); injected node naming `weight → weight_sgfp4`; every run performs an unconditional in-tool decode-vs-oracle verification (`dequant_sgfp4_container_cpu`).
  5. **Failure behavior (post-D-11)**: any failure exits non-zero with an `MNN_ERROR` diagnostic and leaves NO `<output>`/`<output>.weight` behind — including removal of a previous run's stale artifact at the same paths (Task 1 semantics).
  </action>
  <verify>
    <automated>Select-String -Path tools/fp4/README.md -Pattern '--niche-dir','dimO','dimI','externalPath','16-byte' | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
  - `tools/fp4/README.md` exists and contains each of: the literal CLI synopsis string `sgfp4_inject --model`, `--niche-dir`, `dimO`, `dimI`, `stats.shape`, `sha256`, `externalPath`, `16-byte`, `fp4_ultra_v0.2` (terminology-trap warning), `weight_sgfp4`.
  - The four D-13 areas are each a distinct section; no area is missing.
  - The failure-behavior section states non-zero exit + no partial outputs.
  </acceptance_criteria>
  <done>README documents the dims convention (SGINJ-07 "documented" clause, D-13), input contract, CLI, sidecar layout, and post-D-11 failure semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| niche-dir container/manifest bytes → tool | untrusted input files cross here (already guarded: sha256, byte-level v2 gate, bounds-checked decode) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | downstream `SGProcessingManager` unchecked-nullptr on corrupt artifact | mitigate | D-11 failure-cleanup (Task 1): failed runs remove ALL outputs so no corrupt/partial artifact can reach the consumer |
| T-07-02 | Tampering | container/manifest inputs | accept | existing guards (sha256 integrity, byte-level version gate, DoS-bounded decode) — unchanged this plan; asserted by Plan 07-03 probes |
| T-07-03 | Repudiation | tool output provenance | accept | local CLI tool, no audit surface; diagnostics via MNN_ERROR only |
</threat_model>

<verification>
- `cd .build; cmake --build . --target sgfp4_inject.out` — standalone build green.
- `.\run_test.out op/sgfp4` — full family green (classic_api regression after core-header edit; Pitfall 1).
- README grep assertions (Task 2 automated verify).
- Atomicity behavior itself (file-absence after failed runs) is asserted by Plan 07-03's `op/sgfp4/malformed_inputs` suite — this plan ships the mechanism.
</verification>

<success_criteria>
- No `return 1` after sidecar-path definition lacks `failCleanup()`.
- Family regression green with zero classic_api changes.
- `tools/fp4/README.md` present with all four D-13 areas + failure semantics.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

- `failCleanup` — lambda inside `sgfp4_inject::run()` (`tools/fp4/sgfp4_inject_core.hpp`)
- `tools/fp4/README.md` — new file (D-13)
- No new suite registrations, classes, or exported symbols.

Sibling plans add: `tools/fp4/author_structured_fixture.py`, `test/op/SGFP4StructuredFixtures.h` (`kStructuredMixedData`, `kStructuredDimO`, `kStructuredDimI`, `kStructuredSize`, `kStructuredMixedCount`), `test/op/SGFP4MultiTensorTest.cpp` (`SGFP4MultiTensorTest` @ `op/sgfp4/multi_tensor`, `SGFP4MalformedInputsTest` @ `op/sgfp4/malformed_inputs`).
</artifacts_produced>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/07-multi-tensor-hardening-structured-data-coverage/07-01-SUMMARY.md` when done
</output>
