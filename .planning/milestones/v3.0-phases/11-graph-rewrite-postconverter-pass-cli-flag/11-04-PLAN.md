---
phase: "11-graph-rewrite-postconverter-pass-cli-flag"
plan: "04"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tools/converter/source/TestSGFP4Converter.cpp
autonomous: true
requirements: [SGV2-28, SGV2-30]
must_haves:
  truths:
    - "D-12: a new PHASE C in TestSGFP4Converter.cpp drives the registered pass directly (Global<modelConfig>::Reset + RunNetPass({\"InsertSGFP4Dequant\"}, net)) and asserts: SGFP4Dequant node inserted, conv inputs[1] rewired to the new node's output index, conv param->weight empty, dequant buffer non-empty with external == {} and empty externalPath, tensorName grew by one entry named <conv>_sgfp4"
    - "Light-tier floor (D-07): a synthetic conv with dimO*dimI < 4096 or dimI == 1 is left fully FP32 (no dequant node, weight intact)"
    - "Subgraph coverage (D-03): a synthetic NetT whose conv lives inside subgraph->nodes gets rewritten, with the new index appended to subgraph->tensors (never renumbering existing entries)"
    - "Idempotency (research Pitfall 3): running the pass TWICE on the same net yields the identical node count and no double-append of tensorName"
    - "Encode-failure propagation (T-11-03 / VALIDATION TBD-01-01 / ROADMAP criterion 1): a NaN- or Inf-containing qualifying weight leaves the net untouched — no SGFP4Dequant node, param->weight intact, inputIndexes.size() == 1 — and the pass reports failure (observable via direct onExecute false or the untouched-net state); never a cleared-weight conv without its dequant node, never garbage bytes"
    - "Weight-spill path (research KEY Q3): a synthetic conv with external == {offset, wsize, bsize} and weight+bias spilled to a temp bin is reloaded (after an ofstream flush), bias restored into param->bias, external cleared, and the container decodes back to the original weights within the encoder's expected quantization tolerance"
    - "Flag-off dead code (D-14 unit leg): useSGFP4 = false → the pass mutates nothing (node count, weights, tensorName all unchanged)"
    - "D-02 (SGV2-30): a synthetic ALREADY-REWRITTEN conv (inputs.size() == 2) passed through WeightQuantAndCoding is a no-op — weight stays cleared, no quanParameter appears, no sparse transform touches it"
    - "Round-trip survival leg (research A2, Pitfall 4): the full serialize→flatbuffers→reload of a rewritten NetT preserves the SGFP4Dequant op (UnPack/Variable::save survival is the pre-D-13 mechanism check)"
    - "No new CMake surface (D-12): the extension lives entirely inside the existing TestSGFP4Converter target — the RunNetPass declaration from plan 11-01 is the only enablement"
  artifacts:
    - path: "tools/converter/source/TestSGFP4Converter.cpp"
      provides: "PHASE C pass-mechanics test section (extends the Phase 8 PHASE A/B scaffolding)"
      contains: "RunNetPass"
      min_lines: 400
  key_links:
    - from: "tools/converter/source/TestSGFP4Converter.cpp"
      to: "tools/converter/include/PostConverter.hpp"
      via: "RunNetPass({\"InsertSGFP4Dequant\"}, net) invocation (declaration from plan 11-01)"
      pattern: "RunNetPass"
    - from: "tools/converter/source/TestSGFP4Converter.cpp"
      to: "tools/converter/source/optimizer/Global.hpp"
      via: "Global<modelConfig>::Reset(&config) with config.useSGFP4 = true (precedent PostConverter.cpp:634)"
      pattern: "Global<modelConfig>::Reset"
    - from: "tools/converter/source/TestSGFP4Converter.cpp"
      to: "tools/converter/source/common/WeightQuantAndCoding.hpp (or its declaration)"
      via: "direct WeightQuantAndCoding(op, config, context) call on a rewritten conv (SGV2-30 no-op assertion)"
      pattern: "WeightQuantAndCoding"
---

<objective>
Extend the Phase 8 standalone `TestSGFP4Converter.cpp` with a PHASE C pass-mechanics section (D-12): synthetic NetTs driven through the registered `InsertSGFP4Dequant` pass, asserting node insertion, consumer rewiring, weight clearing, the Phase 8 D-11 buffer contract, light-tier skip, subgraph coverage, idempotency, the external-spilled reload path, flag-off dead code, WeightQuantAndCoding no-op on rewritten convs, encode-failure (NaN/Inf) propagation, and flatbuffers round-trip survival.

Purpose: the automated verification legs for SGV2-28 and SGV2-30 (VALIDATION.md rows TBD-01-01..06, TBD-03-01) — everything that can be unit-tested without the corpus; the corpus CLI run itself is plan 11-05's D-13.
Output: extended TestSGFP4Converter.cpp; `TestSGFP4Converter.exe` covers PHASE A + B + C.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-PATTERNS.md

@tools/converter/source/TestSGFP4Converter.cpp
@tools/converter/source/optimizer/PostConverter.cpp
@tools/converter/source/optimizer/PostTreatUtils.hpp
@tools/converter/source/common/WeightQuantAndCoding.cpp
@tools/converter/source/common/RemoveParams.cpp
@tools/converter/include/PostConverter.hpp
@tools/converter/include/config.hpp
@tools/fp4/sgfp4_encode.hpp
@test/op/SGFP4TestUtil.hpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PHASE C core — pass invocation, insertion, rewiring, buffer contract, light-tier, flag-off</name>
  <files>tools/converter/source/TestSGFP4Converter.cpp</files>
  <read_first>tools/converter/source/TestSGFP4Converter.cpp (whole file — CHECK macro :34-41, makeSgfp4Op :57-74, serializeNet :77-87, PHASE A/B structure :97-180, main() flow), tools/converter/source/optimizer/PostConverter.cpp (Global<modelConfig>::Reset precedent :634; RunNetPass :144-170 — what it logs when a pass name is missing), tools/converter/include/PostConverter.hpp (RunNetPass declaration from plan 11-01), tools/converter/include/config.hpp (useSGFP4 field), tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp (the pass as landed in 11-01 — its exact rewrite condition, naming, and ordering), test/op/SGFP4TestUtil.hpp (cwdPath/tempPath/readBytes helpers; buildContainerUniform64 if needed), tools/fp4/sgfp4_encode.hpp (encode overloads — the test can produce containers via the encoder itself for decode cross-checks)</read_first>
  <behavior>
    - Test 1 (insertion/rewiring/buffer contract): synthetic NetT with one Convolution op (inputIndexes = {0}, common->outputCount = 64, weight of 64×128 floats = 8192 elements ≥ 4096, dimI = 128 ≠ 1) → config.useSGFP4 = true, Global<modelConfig>::Reset(&config), RunNetPass({"InsertSGFP4Dequant"}, net) → net contains exactly one SGFP4Dequant op; conv inputIndexes.size() == 2 with inputs[1] == the dequant op's outputIndexes[0]; conv param->weight empty; dequant param->buffer non-empty, param->external empty, op->externalPath empty; param->dims == {64, 128}; magic == kSGFP4Magic; tensorName grew by exactly 1 with the `_sgfp4` suffix
    - Test 2 (light-tier floor D-07): a conv with outputCount×kernelSize < 4096 (e.g. 8×256) and a conv with dimI == 1 (e.g. 8192×1) both remain untouched: no SGFP4Dequant op, param->weight intact, inputIndexes.size() == 1
    - Test 3 (flag-off dead code D-14): same net as Test 1 but config.useSGFP4 = false → zero mutation (op count unchanged, weight intact, tensorName unchanged)
    - Test 4 (decode cross-check): decode the inserted op's buffer via MNN::dequant_sgfp4_container_cpu → output size == dimO*dimI, and the result approximates the original weights (the encoder is lossy: assert output size + finite values + a coarse error bound consistent with FP4 quantization, e.g. max-abs scaled below 1.0 on unit-range weights — not bit equality)
    - Test 5 (round-trip survival): serialize the rewritten net via the existing serializeNet helper, reload the flatbuffer (OpT::UnPack path / Interpreter-independent net inspection or the classic API as in PHASE B) → the reloaded net still contains the SGFP4Dequant op with its buffer intact
  </behavior>
  <action>
Extend TestSGFP4Converter.cpp with a PHASE C section in main() after PHASE B (keep PHASE A/B untouched — D-14: no edits to existing assertions):

1. Add includes: `PostConverter.hpp` (RunNetPass declaration), `config.hpp`, the optimizer `Global.hpp` (resolve the exact path from PostConverter.cpp's own includes), `MNN_generated.h` already present; keep using the existing `CHECK` macro and `sgfp4_test::` helpers (tempPath/cwdPath) for all transient files.
2. Build a small synthetic-net helper local to PHASE C (e.g. `makeConvOp(int oc, int kernelCount, float fill)` producing a Convolution OpT with inputIndexes = {0}, common->outputCount = oc, weight of oc*kernelCount floats — mirroring the PHASE A conv builder at :121-131) plus a fill pattern that is deterministic (ramp or seeded LCG — no rand()).
3. Implement the five tests above. For invocation: construct a stack `modelConfig config; config.useSGFP4 = true;` then `MNN::Express::Global<modelConfig>::Reset(&config);` (precedent PostConverter.cpp:634) then `RunNetPass({"InsertSGFP4Dequant"}, net);` — note RunNetPass reads Global<modelConfig>::Get() itself for dumpPass, and the pass reads useSGFP4 from the same global. IMPORTANT: after the PHASE C block, reset the global (Reset(nullptr) or scope-guard) so no state leaks across phases; the existing PHASE A/B calls (saveExternalData) read config too.
4. Guard against the "pass not registered" silent-skip (RunNetPass only LOGs a missing pass): Test 1's node-count assertion IS the can't-find-pass detector — a zero-insertion count fails loudly. Optionally first assert `PostConverter::get("InsertSGFP4Dequant") != nullptr` (PostTreatUtils.hpp base exposes the static getter).
5. For Test 4's decode: `MNN::dequant_sgfp4_container_cpu(buffer.data(), buffer.size(), out.data(), out.size())` — same oracle call shape as PHASE B (:167-169). Choose weights in [-1, 1] (e.g. ramp amplitude ≤ 1) and assert output size == dimO*dimI, all values finite, and a coarse max-abs bound appropriate to FP4 quantization of unit-range data.
6. For Test 5: reuse `serializeNet` (:77-87) then reload with `flatbuffers::FlatBufferBuilder`-inverse — simplest robust route: read the file bytes and `MNN::GetNet(...)` → `NetT::UnPack` (mirror how MNNRevert2Buffer-style code unpacks; keep it to header-level APIs already available in this TU's include set) and assert the SGFP4Dequant op survives with non-empty buffer. If a lighter route exists via the classic Interpreter as in PHASE B, prefer it only if it exercises the op; the goal is op-preservation, not execution.
7. Update the file-header comment block to describe PHASE C alongside A/B.
8. Build + run: `TestSGFP4Converter.exe` prints PASS covering all three phases. Then `git status test/` — still clean (no test-file edits; D-14).
  </action>
  <verify>Build TestSGFP4Converter; `TestSGFP4Converter.exe` exits 0 with PASS covering PHASE A+B+C; temporarily flipping any single PHASE C assertion's operands (mental or scratch check) is unnecessary — the assertions must be specific enough that a missing pass registration (node count 0) or a wrong splice (inputs[1] mismatch) fails the run</verify>
  <acceptance_criteria>
- `Select-String -Path tools\converter\source\TestSGFP4Converter.cpp -Pattern "PHASE C"` matches (comment + section exist)
- `Select-String -Path tools\converter\source\TestSGFP4Converter.cpp -Pattern 'RunNetPass\(\{"InsertSGFP4Dequant"\}'` matches; `Global<modelConfig>::Reset` matches
- The file contains assertions covering: inputIndexes.size() == 2 post-rewrite, inputs[1] == dequant output index, param->weight empty, buffer non-empty, external empty + externalPath empty, dims == {dimO, dimI}, light-tier skip (both `< 4096` and `dimI == 1` variants), flag-off zero-mutation, idempotent re-run (node count stable across a second RunNetPass), round-trip op survival
- `TestSGFP4Converter.exe` exits 0; `git status --porcelain test/` clean
- PHASE A/B output lines unchanged (layout + reload parity still pass — no regression to Phase 8 tests)
  </acceptance_criteria>
  <done>SGV2-28 unit legs green: insertion, rewiring, clearing, buffer contract, light-tier, flag-off, idempotency, round-trip survival — all inside the existing test executable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHASE C legs — subgraph coverage, spilled-weight reload, WeightQuantAndCoding no-op, idempotency, encode-failure propagation</name>
  <files>tools/converter/source/TestSGFP4Converter.cpp</files>
  <read_first>tools/converter/source/TestSGFP4Converter.cpp (as extended by Task 1), tools/converter/source/common/WeightQuantAndCoding.cpp (the D-02 guard from plan 11-01; its full signature WeightQuantAndCoding(op, config, context) and the PostTreatContext construction needed to call it — check writeFb.cpp:23-37 for the call shape), tools/converter/source/common/RemoveParams.cpp (loadExternalParam :180-215 — the {offset, wsize, bsize} layout the synthetic spill must mimic; ConvertMatmulToConv2D.cpp:270-279 spill producer), schema/current/MNN_generated.h (SubGraphProtoT: nodes + tensors fields), tools/converter/source/common/writeFb.cpp (context.subgraph usage if PostTreatContext needs it)</read_first>
  <behavior>
    - Test 6 (subgraph coverage D-03): NetT with a SubGraphProtoT whose nodes contain a qualifying conv (subgraph->tensors non-empty) → pass rewrites the subgraph node: dequant appended to subgraph->nodes, new index appended to subgraph->tensors (existing entries unrenumbered — assert their values are identical pre/post), conv inputs[1] == new index, main oplists untouched
    - Test 7 (spilled-weight reload KEY Q3): conv with empty param->weight, param->external = {0, wsize, bsize}, the weight+bias bytes pre-written to `.__convert_external_data.bin` in a temp CWD, and config.externalFile set to a flushed ofstream on that same file → pass rewrites it: param->bias restored (equals the original bias bytes), param->external empty, dequant node present, and the decoded container matches the original weight data within the Test-4-style FP4 tolerance
    - Test 8 (D-02 no-op, SGV2-30): an ALREADY-REWRITTEN conv (inputIndexes = {0, newIndex}, weight empty, a dequant op present) is passed through WeightQuantAndCoding directly (weightQuantBits = 0 default path — the one that reaches compressFloatWeightToSparse) → the op is unchanged: inputIndexes still size 2, weight still empty, no quanParameter set, type unchanged
    - Test 9 (idempotency — must_haves truth, ROADMAP criterion 4): run RunNetPass({"InsertSGFP4Dequant"}, net) a SECOND time on the already-rewritten Test-1 net → node count identical to post-first-run, conv inputIndexes still exactly 2 (no second append), tensorName unchanged (no double-append of the _sgfp4 entry)
    - Test 10 (encode-failure propagation — T-11-03, VALIDATION TBD-01-01, ROADMAP criterion 1): a qualifying conv (≥ 4096 elements, dimI != 1) whose weight fill contains NaN (and an Inf variant) → no SGFP4Dequant node, param->weight intact (memcmp against the saved fill — element-wise == is NaN-unreliable), inputIndexes.size() == 1, and the pass reports failure (direct PostConverter::get("InsertSGFP4Dequant")->onExecute(net) returns false; RunNetPass itself only logs)
  </behavior>
  <action>
Add three more PHASE C legs after Task 1's block (same helpers/CHECK style):

1. Test 6 — subgraph: build `MNN::SubGraphProtoT` with `name = "body"`, `tensors = {"x"}`, `nodes` containing the Task-1-style qualifying conv whose inputIndexes reference an existing tensor index (e.g. {0}); attach via `net->subgraphs.push_back(...)`. Run the pass with useSGFP4 = true. Assert: exactly one SGFP4Dequant op inside `subgraph->nodes`; the conv node's inputIndexes.size() == 2; the new index equals the pre-push `subgraph->tensors.size()` and the appended name carries the `_sgfp4` suffix; the pre-existing tensors entries are byte-identical (no renumber — copy them before the run and compare after); the root `net->oplists` count is unchanged.
2. Test 7 — spill: in a temp dir (sgfp4_test::tempPath), write weight (dimO*dimI floats, deterministic fill) + bias (oc floats) bytes to `.__convert_external_data.bin` at offset 0 via a real `std::ofstream` kept OPEN (this is the production condition — the pass must flush it); set `config.externalFile = &thatOfstream;` before Reset. Build the conv with empty weight, `param->external = {0, (int64_t)(dimO*dimI*4), (int64_t)(oc*4)}`, common->outputCount = dimO. Run the pass. Assert: dequant node present; conv param->external empty; param->bias restored and element-wise equal to the written bias; decoded buffer (Test-4 route) approximates the written weights; conv inputIndexes.size() == 2. Close/remove the ofstream + temp file afterwards. NOTE: FileLoader resolves the bin relative to CWD — chdir to the temp dir for this leg or place the bin next to the executable's CWD as the converter does; prefer running the whole executable with CWD already at a temp base (the existing sgfp4_test::cwdPath pattern) and cleaning up `.__convert_external_data.bin` by name.
3. Test 8 — WeightQuantAndCoding no-op: construct the rewritten conv (inputs {0, 1}, weight empty, plus a minimal dequant sibling op — the guard only reads inputIndexes, but build the realistic shape), then call `WeightQuantAndCoding(op, config, context)` with a minimal PostTreatContext (mirror writeFb.cpp's construction; if the context is awkward to build, construct the lightest valid one — the D-02 return happens before any context use). Assert afterwards: op->inputIndexes.size() == 2, `op->main.AsConvolution2D()->weight` empty, `quanParameter` null, op->type unchanged. The header declaring WeightQuantAndCoding is already in MNNConvertDeps' include set (common/PostTreatUtils.hpp or its own header — resolve from writeFb.cpp's include list).
4. Test 9 — idempotency: keep the Test-1 net (already rewritten), snapshot node count + the conv's inputIndexes + tensorName, run `RunNetPass({"InsertSGFP4Dequant"}, net)` a SECOND time, assert all three snapshots are identical — the `inputIndexes.size() == 1` rewrite condition is the guard being proven.
5. Test 10 — encode-failure propagation: build the Test-1-style qualifying conv but seed the weight fill with a NaN (first element) — plus a second variant with +Inf — run the pass (useSGFP4 = true): assert zero SGFP4Dequant ops, `param->weight` unchanged (compare byte-wise/memcpy against the saved fill — element-wise == is unreliable with NaN), `inputIndexes.size() == 1`. For the failure-report leg, invoke the pass object directly: `auto pass = PostConverter::get("InsertSGFP4Dequant"); CHECK(pass != nullptr); CHECK(!pass->onExecute(net));` (`onExecute` is the public virtual RunNetPass dispatches to — PostTreatUtils.hpp). If direct invocation does not compile as written, the untouched-net assertions remain the required leg; record the substitution in the SUMMARY.
6. Extend the file-header comment and the final PASS line to enumerate all PHASE C legs (now Tests 1-10). Build + run; `run_test.out op/sgfp4` still 13/13 (D-14 — this plan touches only the converter-side test, never test/).
  </action>
  <verify>Build + run TestSGFP4Converter.exe → exit 0 with PHASE A+B+C PASS; `git status test/` clean; `run_test.out op/sgfp4` green</verify>
  <acceptance_criteria>
- TestSGFP4Converter.cpp contains a subgraph fixture (SubGraphProtoT with nodes+tensors) with pre/post tensor-stability assertions, a spilled-weight fixture (open ofstream + external == {0, wsize, bsize}) with bias-restore assertion, and a direct WeightQuantAndCoding invocation asserting the no-op on an inputs>1 conv
- `Select-String -Path tools\converter\source\TestSGFP4Converter.cpp -Pattern "WeightQuantAndCoding"` matches (the SGV2-30 leg drives the hook itself)
- The file contains a NaN/Inf encode-failure leg (untouched conv + failure report — TBD-01-01 / ROADMAP criterion 1) and a double-RunNetPass idempotency leg (stable node count + no tensorName double-append — ROADMAP criterion 4)
- `TestSGFP4Converter.exe` exits 0; `.__convert_external_data.bin` (if created in CWD) removed by the test
- `git status --porcelain test/` clean; `run_test.out op/sgfp4` 13/13 green
  </acceptance_criteria>
  <done>SGV2-28 remaining legs (subgraph, spill, idempotency, encode-failure propagation) + SGV2-30 unit leg (skip-guard no-op) green — D-12 complete, no new CMake surface</done>
</task>

</tasks>

## Artifacts this phase produces (plan 11-04)

| Symbol / Artifact | Kind | Location |
|---|---|---|
| PHASE C test section | test code (extends existing executable; no new target) | `tools/converter/source/TestSGFP4Converter.cpp` |
| PHASE C local helpers (e.g. `makeConvOp`, spill/subgraph fixture builders) | file-local test helpers | same file |

(No new production symbols — this plan verifies plan 11-01's artifacts.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → pass under test | synthetic (trusted, self-generated) inputs — no untrusted boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering (test hygiene) | temp `.__convert_external_data.bin` left in CWD | mitigate | test removes its temp bin (and all temp artifacts) via the existing sgfp4_test cleanup pattern; acceptance criterion enforces it |
| T-11-10 | Tampering (regression) | unintended edits to runtime test suites | mitigate | plan touches ONLY tools/converter test code; `git status test/` clean + 13/13 suites green are hard acceptance criteria (D-14) |
</threat_model>

<verification>
- `TestSGFP4Converter.exe` exit 0 covering PHASE A+B+C
- All D-12 assertion families present: insertion/rewire/clear/buffer/light-tier/subgraph/idempotency/spill/flag-off/no-op/encode-failure-propagation/round-trip
- `run_test.out op/sgfp4` 13/13; `git status test/` clean
</verification>

<success_criteria>
- D-12 fully delivered inside the existing executable; SGV2-28/SGV2-30 unit legs closed; Phase 8 PHASE A/B unregressed
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-04-SUMMARY.md` when done
</output>
