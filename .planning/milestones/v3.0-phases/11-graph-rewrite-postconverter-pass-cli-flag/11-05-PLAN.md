---
phase: "11-graph-rewrite-postconverter-pass-cli-flag"
plan: "05"
type: execute
wave: 3
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - tools/fp4/README.md
autonomous: true
requirements: [SGV2-28, SGV2-29, SGV2-30]
must_haves:
  truths:
    - "D-13: a real `MNNConvert -f ONNX --modelFile W:\\gnus\\models\\alexnet_Opset16.onnx --MNNModel <out>.mnn --sgfp4` run completes with exit 0 and the emitted .mnn contains SGFP4Dequant nodes — count = 8 candidates (5 feature convs + 3 classifier FC→convs) minus light-tier skips, asserted against the pass's own output, with optional --dumpPass showing InsertSGFP4Dequant ops N -> N+K"
    - "D-13 decode leg: the converted artifact loads via the classic Interpreter/Session API and one SGFP4 node's decode is verified (spot-check vs expectation) — proving the full flag→pass→buffer→artifact→runtime chain"
    - "D-13 mutex leg: the --sgfp4 --fp16 combination fails with the MNN_ERROR mutex text and exit 1 (re-verified at smoke level, formal assertions landed in 11-03)"
    - "OQ3 contingency (pre-authorized, NOT the primary path): if the smoke shows SGFP4 node loss through the Express round-trip (research A2/Pitfall 4), the fallback is invoking the pass post-optimizeNet from cli.cpp before writeFb — a documented D-01 deviation recorded in the SUMMARY and STATE, not a silent re-placement"
    - "D-14: with the flag OFF, all 13 op/sgfp4 suites + the existing converter tests are green with zero test-file edits (`git status test/` clean); the pass is dead code"
    - "D-13 documentation: the smoke procedure (exact commands, expected node counts, corpus provenance sha256 4bc388cc…, mutex shape) is committed in tools/fp4/README.md as a repeatable manual gate — corpus is a test-time dependency, NOT an always-on CI gate"
  artifacts:
    - path: "tools/fp4/README.md"
      provides: "mnnconvert --sgfp4 smoke procedure (commands, assertions, provenance, contingency note)"
      contains: "--sgfp4"
  key_links:
    - from: "the smoke procedure in tools/fp4/README.md"
      to: "MNNConvert.exe --sgfp4"
      via: "documented command with exit-code + node-count + decode assertions"
      pattern: "--sgfp4"
    - from: "the smoke's classic-API decode step"
      to: "the emitted .mnn"
      via: "Interpreter::createFromFile + createSession + runSession (PHASE B pattern)"
      pattern: "Interpreter"
---

<objective>
Prove the flag→artifact path on the real approved corpus (D-13), run the flag-OFF no-regression gate (D-14), and commit the smoke procedure to `tools/fp4/README.md`. This plan consumes everything: the pass (11-01), the flag + mutex + exit code (11-03), the unit tests (11-04), and the tooling hygiene (11-02) — and produces the phase-level acceptance evidence.

Purpose: closes SGV2-28/29/30 at the integration level and de-risks the single biggest unknown (Express round-trip survival of inserted nodes, research A2) with the pre-authorized OQ3 contingency as fallback.
Output: executed + documented D-13 smoke, green D-14 gate, README section.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-RESEARCH.md

@tools/fp4/README.md
@tools/converter/source/TestSGFP4Converter.cpp
@.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-13 corpus smoke — convert, count nodes, decode, mutex re-check</name>
  <files>tools/fp4/README.md</files>
  <read_first>tools/fp4/README.md (existing ## Usage / ## Failure behavior sections — the smoke section slots alongside), tools/converter/source/TestSGFP4Converter.cpp (PHASE B classic-API reload pattern :143-180 — the decode step reuses this shape), .planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-VERIFICATION.md (corpus provenance: sha256 4bc388cc…, 16 tensors, tier split 10 full / 6 light), 11-RESEARCH.md §Code Examples (the D-13 smoke assertion shape)</read_first>
  <action>
Per D-13 / research Pitfall 4:

1. Build MNNConvert (with converter ON — plans 11-01/11-03 outputs). Run: `MNNConvert -f ONNX --modelFile W:\gnus\models\alexnet_Opset16.onnx --MNNModel tmp\p11_smoke.mnn --sgfp4` (optionally `--dumpPass` for the `[DumpPass] PostConvert::InsertSGFP4Dequant: ops N -> N+K` line). Assert: exit 0; output .mnn exists.
2. Node-count assertion: inspect the emitted artifact for SGFP4Dequant ops. Preferred route: MNNDump2Json (`MNNConvert -f MNN ... --JsonFile` exists in the option table) or a flatbuffers GetNet scan; assert count == expected = 8 candidates (5 feature convs + 3 classifier FC→convs via ConvertMatmulToConv2D) MINUS light-tier skips (per Phase 10: all 10 full-tier weights encode — expect 8 non-bias weight tensors... reconcile against the Phase 10 report's tier table: features.0/3/6/8/10 (5 convs) + classifier.1/4/6 (3 FCs) = 8 full-tier weights, none < 4096 elements, none dimI == 1 → expected K = 8; biases are not conv weights and are never targeted). Record the observed count; a mismatch is a finding to investigate (light-tier floor firing unexpectedly, or a conv not matching the type gate), not a silent pass.
3. Decode spot-check: load `tmp\p11_smoke.mnn` via the classic API (Interpreter::createFromFile → createSession → resizeSession → runSession, PHASE B pattern) — successful session construction + run is the decode leg (the artifact's buffers were already decode-verified at unit level in 11-04; here we prove the converted artifact loads and executes end-to-end). If a more explicit single-node decode check is cheap (e.g. via MNNDump2Json + TestSGFP4Converter-style reload), do it; tolerance wording: any comparison vs FP32 is informational only — Phase 12 owns formal accuracy gates.
4. Mutex leg: `--sgfp4 --fp16` → MNN_ERROR text + exit 1 + no output written (re-verification; assertions already landed in 11-03).
5. **OQ3 contingency (pre-authorized, NOT primary):** IF step 2 shows node loss (K < expected — the Express round-trip folded/replaced the inserted ops, research A2): do NOT silently re-place the pass. Execute the fallback: invoke the pass post-`optimizeNet` from `cli.cpp` (between optimizeNet and writeFb), record it in the SUMMARY as an explicit D-01 deviation with the evidence (node counts before/after), and update STATE. Only that fallback is authorized without re-planning.
6. Write the `## mnnconvert --sgfp4 smoke` section into `tools/fp4/README.md`: exact commands (convert + count + decode + mutex), expected results (exit 0, K = 8 SGFP4Dequant nodes, session runs, mutex exit 1), corpus provenance (path + sha256 `4bc388cc…`, Phase 10 D-01/D-02 approval), the note that this is a manual/test-time gate (not always-on CI), and — if triggered — the OQ3 deviation note. Clean up `tmp\p11_smoke*` afterwards.
  </action>
  <verify>The smoke run's transcript shows: exit 0, K SGFP4Dequant nodes in the artifact (expected 8), classic-API session constructs and runs, mutex exits 1; tools/fp4/README.md contains the `--sgfp4` smoke section with the corpus sha256</verify>
  <acceptance_criteria>
- `MNNConvert -f ONNX --modelFile W:\gnus\models\alexnet_Opset16.onnx --MNNModel <tmp>.mnn --sgfp4` exits 0 and the artifact contains SGFP4Dequant ops (observed count recorded; expected 8 — any deviation explained in the SUMMARY)
- A classic-API `Interpreter` session on the converted artifact constructs, resizes, and runs (`runSession` returns NO_ERROR)
- `MNNConvert ... --sgfp4 --fp16` exits 1 with the mutex MNN_ERROR and writes no output
- `Select-String -Path tools\fp4\README.md -Pattern "--sgfp4"` matches within a smoke section that includes the corpus sha256 `4bc388cc`
- If node loss occurred: the cli.cpp fallback applied + deviation documented (SUMMARY + STATE); otherwise: no deviation, primary D-01 placement confirmed
  </acceptance_criteria>
  <done>D-13 closed: real corpus flag-ON conversion produces a loadable artifact with the expected SGFP4 nodes; procedure committed as a repeatable manual gate</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-14 flag-OFF no-regression gate + phase wrap-up</name>
  <files>tools/fp4/README.md</files>
  <read_first>tools/fp4/README.md (as extended by Task 1), 11-VALIDATION.md (Sampling Rate — per-wave gate: run_test.out op/sgfp4 13/13 + full TestSGFP4Converter.exe + git status test/ clean)</read_first>
  <action>
Per D-14 (the phase's final gate — everything before it must already be merged):

1. `git status test/` — must be clean (zero test-file edits across the whole phase; any test/ change is a D-14 violation to revert).
2. Run `run_test.out op/sgfp4` — all 13 suites green.
3. Run full `TestSGFP4Converter.exe` — PHASE A+B+C green.
4. Flag-off conversion equivalence spot-check: convert the corpus WITHOUT `--sgfp4` into a second output; assert the conversion succeeds (behavioral no-change proof at CLI level; the unit-level zero-mutation proof landed in 11-04 Test 3).
5. In `tools/fp4/README.md`, append the flag-OFF statement to the smoke section: `--sgfp4` absent ⇒ the pass is dead code; all suites green without test modification.
6. Record the phase-level evidence bundle in the SUMMARY: smoke transcript facts, suite results, node count, any deviation (OQ3), and the W-1/W-2/W-3 closure pointers from 11-02 — this is what `/gsd-verify-work` will audit against the ROADMAP success criteria.
  </action>
  <verify>`git status --porcelain test/` empty; `run_test.out op/sgfp4` 13/13; `TestSGFP4Converter.exe` exit 0; flag-off corpus conversion succeeds; README contains the flag-OFF note</verify>
  <acceptance_criteria>
- `git status --porcelain test/` output is EMPTY
- `run_test.out op/sgfp4` reports all 13 suites passing
- `TestSGFP4Converter.exe` exits 0 (PHASE A+B+C)
- A corpus conversion without `--sgfp4` exits 0 (no flag-off behavior change at CLI level)
- 11-05-SUMMARY.md records: smoke facts (exit/node-count/decode/mutex), suite results, W-debt closures, and either "no deviation" or the OQ3 deviation record
  </acceptance_criteria>
  <done>D-14 closed: zero-regression proof complete; phase evidence bundle assembled for verification</done>
</task>

</tasks>

## Artifacts this phase produces (plan 11-05)

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `## mnnconvert --sgfp4 smoke` section | documentation | `tools/fp4/README.md` |
| D-13 evidence (transcript facts: node count, decode, mutex) | documentation (SUMMARY) | `11-05-SUMMARY.md` |
| OQ3 contingency record (only if triggered) | documented deviation | SUMMARY + STATE |

(No new production symbols; if the OQ3 fallback triggers, the invocation-site change in cli.cpp would be a deviation artifact — document, don't hide.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus file → converter | same trusted-user-model boundary as plan 11-01, exercised end-to-end here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering (validation integrity) | smoke evidence quality | mitigate | node-count assertion reconciled against the Phase 10 tier table (expected 8); mismatches are investigated findings, not silent passes; transcript facts recorded in the SUMMARY |
| T-11-12 | Tampering (regression drift) | flag-off no-change guarantee | mitigate | D-14 hard gate: `git status test/` clean + 13/13 suites + flag-off conversion equivalence; any test/ edit fails the phase |
| T-11-04 | Tampering | `.__convert_external_data.bin` temp path | accept | (carried from 11-01) pre-existing behavior, unchanged; smoke runs in a temp CWD and cleans up |
</threat_model>

<verification>
- D-13: smoke exit 0 + expected node count + classic-API run + mutex exit 1 + README section with provenance
- D-14: test/ clean, 13/13 suites, TestSGFP4Converter green, flag-off conversion succeeds
- OQ3: either confirmed unnecessary or executed as the documented deviation
</verification>

<success_criteria>
- Phase 11's integration evidence complete: the flag produces a real loadable SGFP4 artifact on the approved corpus, the mutex holds, and flag-off is provably inert
- All documentation committed for `/gsd-verify-work` to audit
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-05-SUMMARY.md` when done
</output>
