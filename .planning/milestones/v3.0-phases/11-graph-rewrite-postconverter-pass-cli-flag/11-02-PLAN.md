---
phase: "11-graph-rewrite-postconverter-pass-cli-flag"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fp4/sgfp4_inject_core.hpp
  - tools/fp4/author_structured_fixture.py
  - tools/fp4/author_real_shape_fixture.py
  - tools/fp4/validate_real_weights.py
autonomous: true
requirements: [SGV2-30]
must_haves:
  truths:
    - "D-09 (W-1): verified-closed, NOT re-implemented — SGFP4ClassicAPITest.cpp already calls sgfp4_test::buildContainerUniform64 (region-relative offsets) since commit 1df51b7e; this plan proves it and records the closure, with no edit to the offset logic itself"
    - "D-10 (W-2): sgfp4_inject's failCleanup lambda is defined ABOVE the arg-validation returns and is a no-op while outputPath is empty (guards [&outputPath] only, computes outputPath + \".weight\" internally) — usage()-exit paths now remove stale output artifacts, matching the README's full-removal promise"
    - "D-11 (W-3): author_structured_fixture.py and author_real_shape_fixture.py read GNUST_POC_ROOT from env var SGFP4_GNUS_POC_ROOT with the current hard-coded W:/ path as fallback; validate_real_weights.py gains the same env-var as the default fallback for its --gnus-poc-root argparse option"
    - "W-2 verification surface: a scripted stale-file probe (create out.mnn + out.weight, run sgfp4_inject with a bad arg targeting that --output, assert both files removed) is committed under tools/fp4/ or documented in the README — cheap and proportionate (audit item is WARNING-level)"
  artifacts:
    - path: "tools/fp4/sgfp4_inject_core.hpp"
      provides: "failCleanup hoisted above arg validation (W-2 fix)"
      contains: "if (!outputPath.empty())"
    - path: "tools/fp4/author_structured_fixture.py"
      provides: "env-var gnus-poc root override (W-3 fix)"
      contains: "SGFP4_GNUS_POC_ROOT"
    - path: "tools/fp4/author_real_shape_fixture.py"
      provides: "env-var gnus-poc root override (W-3 fix)"
      contains: "SGFP4_GNUS_POC_ROOT"
    - path: "tools/fp4/validate_real_weights.py"
      provides: "env-var default fallback for --gnus-poc-root (uniformity)"
      contains: "SGFP4_GNUS_POC_ROOT"
  key_links:
    - from: "tools/fp4/sgfp4_inject_core.hpp"
      to: "the arg-validation return paths (:288-289 unknown arg, :292-294 missing required args)"
      via: "failCleanup() invoked (or cleanup semantics guaranteed by hoist placement) on those paths"
      pattern: "failCleanup"
    - from: "tools/fp4/author_structured_fixture.py"
      to: "os.environ"
      via: "GNUST_POC_ROOT = Path(os.environ.get(\"SGFP4_GNUS_POC_ROOT\", <current default>))"
      pattern: "os\.environ\.get\\(\"SGFP4_GNUS_POC_ROOT\""
---

<objective>
Retire the v2.0 milestone-audit tech debt assigned to Phase 11: W-1 (verify-and-close — research proved it already fixed at commit 1df51b7e), W-2 (hoist `sgfp4_inject`'s `failCleanup` lambda above the arg-validation returns so usage()-exit paths also remove stale output artifacts, per D-10), W-3 (env-var override for the hard-coded gnus-poc root in the authoring scripts, per D-11), plus a W-2 stale-file probe script.

Purpose: closes the audit's actionable items alongside the converter work without touching any file plan 11-01/11-03/11-04 modify (fully parallel-safe, Wave 1). W-2/W-3 are prerequisites for SGV2-30's "tooling hygiene" acceptance row in the milestone audit and keep the injection tool's failure behavior honest while the converter path supersedes it.
Output: 4 small file edits + 1 probe script + verified-green classic_api suite.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-RESEARCH.md
@.planning/milestones/v2.0-MILESTONE-AUDIT.md

@tools/fp4/sgfp4_inject_core.hpp
@tools/fp4/author_structured_fixture.py
@tools/fp4/author_real_shape_fixture.py
@tools/fp4/validate_real_weights.py
@tools/fp4/README.md
@test/op/SGFP4ClassicAPITest.cpp
@test/op/SGFP4TestUtil.hpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: W-2 failCleanup hoist in sgfp4_inject_core.hpp</name>
  <files>tools/fp4/sgfp4_inject_core.hpp</files>
  <read_first>tools/fp4/sgfp4_inject_core.hpp (lines ~270-320: the run() entry — declarations + arg parse loop :278-288, usage() returns :288-289 and :292-294, sidecarPath :296, the failCleanup lambda :304-310, and the D-11 failure-cleanup comment block above it; also grep all 12 later failCleanup() call sites to confirm they stay valid), tools/fp4/README.md (## Failure behavior — the full-removal promise W-2 must honor)</read_first>
  <action>
Per D-10 / research KEY Q5:

1. Move the `failCleanup` lambda definition from its current position AFTER `sidecarPath` derivation (:304-310) to ABOVE the arg parse loop (:278 area), restructured to capture ONLY `&outputPath` and to be a safe no-op while `outputPath` is still empty:

   - Lambda body: `if (!outputPath.empty()) { std::remove(outputPath.c_str()); std::remove((outputPath + ".weight").c_str()); }` — computes the sidecar path inside, so it never `std::remove`s a bare `.weight` in the CWD when `--output` was not yet parsed.
2. The two arg-validation returns (`usage(); return 1;` on unknown arg :288-289 and on missing required args :292-294) gain a `failCleanup();` call immediately before `return 1;` — now reachable since the lambda exists above them.
3. `sidecarPath` stays derived at :296 for the success path; all 12 later failure sites keep calling `failCleanup()` unchanged (they still compile — the lambda captures what it needs).
4. Update the D-11 failure-cleanup comment block to note the W-2 hoist: cleanup now covers ALL failure paths including arg validation.

Rebuild `sgfp4_inject.out` (MNN_BUILD_SGFP4_TOOLS=ON build) and run the probe: create stale `out.mnn` + `out.weight` in a temp dir, invoke `sgfp4_inject.out` with a bad/unknown argument and `--output` pointing at them, assert BOTH files are gone and exit code is 1.

   Execution note (wave-1 build serialization): plan 11-01 runs in the same wave and re-configures CMake in the SAME build directory (its CMakeLists edits trigger a re-configure) — if the two wave-1 plans execute concurrently, serialize the `cmake` configure/build invocations (or build this plan's tool targets in a separate build dir); 11-01's re-configure must not race this rebuild.
  </action>
  <verify>Build sgfp4_inject.out; stale-file probe passes (both files removed, exit 1); existing `run_test.out op/sgfp4` quick subset still green (inject tool unchanged functionally on success paths); source assertion: the `failCleanup` lambda's line number is ABOVE the arg parse loop, and both arg-validation returns contain `failCleanup()`</verify>
  <acceptance_criteria>
- `Select-String -Path tools\fp4\sgfp4_inject_core.hpp -Pattern "failCleanup"` — the lambda DEFINITION's line number is LOWER than the arg parse loop's line, and both `usage(); return 1;` sites have a preceding `failCleanup();` on an adjacent line
- The lambda body contains `if (!outputPath.empty())` and computes `outputPath + ".weight"` internally (no `sidecarPath` capture)
- Probe transcript (or committed script output) shows: pre-existing `out.mnn` + `out.weight` both removed after a bad-arg run, process exit code 1
- `sgfp4_inject.out` builds clean under MSVC (no C2065 undeclared identifier — hoist correctness proof)
  </acceptance_criteria>
  <done>W-2 closed: every failure path (arg-stage included) removes stale output artifacts; audit item text satisfied</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: W-3 env-var root override in the three Python scripts + W-2 probe script commit</name>
  <files>tools/fp4/author_structured_fixture.py, tools/fp4/author_real_shape_fixture.py, tools/fp4/validate_real_weights.py</files>
  <read_first>tools/fp4/author_structured_fixture.py (lines ~20-30: the GNUST_POC_ROOT hard-code + sys.path.insert), tools/fp4/author_real_shape_fixture.py (lines ~20-35: same pattern at :27-29), tools/fp4/validate_real_weights.py (lines ~45-55: DEFAULT_GNUS_POC_ROOT + the --gnus-poc-root argparse option; :757 if the literal reappears), tools/fp4/README.md (any documented env vars / root instructions to extend)</read_first>
  <action>
Per D-11 / research KEY Q6:

1. `tools/fp4/author_structured_fixture.py` (:25 area): replace the hard-coded `GNUST_POC_ROOT = Path("W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/gnus-poc")` with `GNUST_POC_ROOT = Path(os.environ.get("SGFP4_GNUS_POC_ROOT", "W:/gnus/GeniusCognitiveSystem/GNUS-NEO-SWARM/gnus-poc"))`, adding `import os` if absent. Update the adjacent comment: root overridable via SGFP4_GNUS_POC_ROOT (W-3, Phase 11 D-11); no gnus-poc-side convention exists — name is MNN-side.
2. `tools/fp4/author_real_shape_fixture.py` (:28 area): identical edit (same env name, same fallback).
3. `tools/fp4/validate_real_weights.py`: keep `--gnus-poc-root` argparse authoritative; change its DEFAULT to read the env var with the same literal fallback (`default=os.environ.get("SGFP4_GNUS_POC_ROOT", "W:/...")`), and if the literal reappears at :757, route it through the same default. Uniformity fix — behavior identical when the env var is unset.
4. Commit the W-2 probe from Task 1 as a tiny script under `tools/fp4/` (e.g. `w2_failcleanup_probe.ps1` or `.sh` — either is fine, it is a documented manual step not a CI gate): creates the stale pair, runs the bad-arg invocation, asserts removal + exit 1; OR document the probe verbatim in `tools/fp4/README.md` under Failure behavior. Choose one; the README already promises the behavior being proven.
5. Manual W-3 verification (authoring-time only, per VALIDATION.md): with `SGFP4_GNUS_POC_ROOT` set to the real root, `python tools/fp4/author_structured_fixture.py --help` (or an import smoke) resolves imports; with it unset, behavior is the current default. Record the check in the summary.

No C++ build impact — Python + script/doc only.
  </action>
  <verify>Source assertions: all three scripts contain `SGFP4_GNUS_POC_ROOT` inside an `os.environ.get(...)` with the W:/ literal as fallback; `python -c "import ast; ast.parse(open('tools/fp4/author_structured_fixture.py').read())"` (and the other two) parse clean; with env var set, author_structured_fixture.py's GNUST_POC_ROOT resolves to the override (print/smoke)</verify>
  <acceptance_criteria>
- `Select-String -Path tools\fp4\author_structured_fixture.py, tools\fp4\author_real_shape_fixture.py, tools\fp4\validate_real_weights.py -Pattern 'os\.environ\.get\("SGFP4_GNUS_POC_ROOT"'` matches in ALL THREE files
- All three scripts parse (`ast.parse`) without error
- Env-var resolution demonstrated: setting `SGFP4_GNUS_POC_ROOT` changes the effective root (print statement or import smoke recorded in the summary)
- W-2 probe is either committed as a script or documented step-by-step in tools/fp4/README.md (one of the two, referenced from the summary)
  </acceptance_criteria>
  <done>W-3 closed: portable root override across the authoring/validation scripts; W-2 probe preserved as a repeatable step</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: W-1 verify-and-close (no code edit) + audit annotation</name>
  <files>.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-02-SUMMARY.md</files>
  <read_first>test/op/SGFP4ClassicAPITest.cpp (lines ~80-100: the Plan 08-02 comment block documenting the swap to sgfp4_test::buildContainerUniform64; lines ~160-175: the call sites), test/op/SGFP4TestUtil.hpp (lines ~130-190: the region-relative offset builder — the encoder-conformant convention), git log for commit 1df51b7e (the D-10 pull-forward that fixed W-1)</read_first>
  <action>
Per D-09 / research KEY Q4 — W-1 is ALREADY FIXED. This task VERIFIES and RECORDS the closure; it does not touch the code:

1. Run the classic_api suite: `run_test.out op/sgfp4/classic_api` (build with MNN_BUILD_TEST=ON as usual) — must be green.
2. Confirm by source inspection that `SGFP4ClassicAPITest.cpp` contains NO local absolute-offset container builder: its container construction routes through `sgfp4_test::buildContainerUniform64` from `SGFP4TestUtil.hpp` (region-relative convention, anchored on `MNN::kSGFP4RecordOffsetTableStart` / `sgfp4_align16`). Record the git SHA `1df51b7e` as the fixing commit.
3. Record the closure in the 11-02 SUMMARY.md: "W-1 retired by 1df51b7e (Phase 8 plan 08-02, D-10 pull-forward); verified green this phase; no edit made" — this is the annotation the milestone audit's W-1 row needs. Do NOT edit `.planning/milestones/v2.0-MILESTONE-AUDIT.md` itself unless the executor's convention elsewhere has been to annotate in place; prefer the SUMMARY as the record and let `/gsd-verify-work` fold it into the phase verification.
4. Explicitly do NOT modify `test/op/SGFP4ClassicAPITest.cpp` or `test/op/SGFP4TestUtil.hpp` — any drift there would be a defect, not work.
  </action>
  <verify>`run_test.out op/sgfp4/classic_api` exits green; `git status test/op/` clean after this task (no edits); summary records SHA 1df51b7e</verify>
  <acceptance_criteria>
- `run_test.out op/sgfp4/classic_api` passes (exit 0, suite green)
- `git status --porcelain test/op/` shows NO modifications attributable to this task
- 11-02-SUMMARY.md contains a W-1 closure note citing commit `1df51b7e`
  </acceptance_criteria>
  <done>W-1 verified-and-closed with recorded evidence; zero code churn</done>
</task>

</tasks>

## Artifacts this phase produces (plan 11-02)

| Symbol / Artifact | Kind | Location |
|---|---|---|
| hoisted `failCleanup` lambda | code relocation (no new symbol) | `tools/fp4/sgfp4_inject_core.hpp` |
| `SGFP4_GNUS_POC_ROOT` | environment variable contract (consumed, not defined) | `tools/fp4/author_structured_fixture.py`, `author_real_shape_fixture.py`, `validate_real_weights.py` |
| W-2 probe script (name executor-chosen, e.g. `w2_failcleanup_probe.ps1`) OR README probe section | script/doc | `tools/fp4/` |
| W-1 closure record | documentation (SUMMARY) | `11-02-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → Python scripts | `SGFP4_GNUS_POC_ROOT` env var now influences import paths (developer machine, not runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | `SGFP4_GNUS_POC_ROOT` env-var influence | accept | authoring-time developer convenience only (same trust level as the `--gnus-poc-root` CLI override that already exists); no build/CI/runtime surface consumes it |
| T-11-06 | Tampering | stale-artifact removal in sgfp4_inject | mitigate (by W-2 itself) | the W-2 hoist IS the mitigation — failed runs can no longer leave a stale `.mnn`/`.weight` pair that a downstream consumer would mistake for the failed run's output |
</threat_model>

<verification>
- `run_test.out op/sgfp4/classic_api` green (W-1 leg)
- Stale-file probe passes (W-2 leg)
- Env-var override demonstrated on at least one script (W-3 leg)
- No edits to `test/op/` source files
</verification>

<success_criteria>
- W-1: verified-and-closed, annotated with fixing SHA `1df51b7e`, classic_api green
- W-2: failCleanup covers arg-stage failures; README promise honored; probe repeatable
- W-3: all three scripts honor `SGFP4_GNUS_POC_ROOT` with identical fallbacks
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-02-SUMMARY.md` when done
</output>
