---
phase: "11-graph-rewrite-postconverter-pass-cli-flag"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - tools/fp4/CMakeLists.txt
  - tools/converter/CMakeLists.txt
  - tools/converter/include/config.hpp
  - tools/converter/include/PostConverter.hpp
  - tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp
  - tools/converter/source/optimizer/PostConverter.cpp
  - tools/converter/source/common/WeightQuantAndCoding.cpp
autonomous: true
requirements: [SGV2-28, SGV2-30]
must_haves:
  truths:
    - "D-01: a registered PostConverter pass named \"InsertSGFP4Dequant\" runs in the final pass batch BEFORE ReIndexTensor — PostConverter.cpp tail reads RunNetPass({\"InsertSGFP4Dequant\", \"ReIndexTensor\"}, newNet) (research KEY Q2 order lock)"
    - "D-14 (unit-level leg): with modelConfig::useSGFP4 == false (the default), the pass returns true having mutated nothing — dead code when the flag is absent"
    - "D-06: the pass rewrites exactly Convolution, ConvolutionDepthwise, Deconvolution, DeconvolutionDepthwise ops; dims arithmetic mirrors WeightQuantAndCoding.cpp:126-131 (oc = common->outputCount; kernelSize = weightSize / oc; dimO = oc; dimI = kernelSize)"
    - "Idempotency (research Pitfall 3): the rewrite condition is strictly conv-family AND inputIndexes.size() == 1 AND quanParameter == nullptr, so the double-RunOptimize final-batch execution (PostConverter.cpp:649 + :685) and per-subgraph round-trips never double-rewrite"
    - "D-07 light-tier floor: elements < 4096 OR dimI == 1 → conv left fully FP32 and untouched"
    - "D-08: encoding goes through a greppable file-scope constant kSGFP4ConverterEncodeConfig aliasing sgfp4_encode::kDefaultEncodeConfig (config-carrying encode overload), with a comment naming tools/fp4/real_weight_validation_report.json as where the validated (unused-by-default) delta lives"
    - "Phase 8 D-11 buffer contract: every inserted SGFP4Dequant op carries param->buffer populated, external == {} and empty externalPath; the pass never writes external/externalPath itself"
    - "Weight-spill path (research KEY Q3): convs arriving with param->external.size() == 3 are reloaded from .__convert_external_data.bin via FileLoader after flushing config->externalFile (null-checked), bias restored into param->bias, param->external cleared"
    - "Transactional failure safety (T-11-03): a conv is only mutated (weight cleared, input index pushed) AFTER its container encoded successfully; on encode/reload failure the conv is skipped untouched, MNN_ERROR names the op, and the pass returns false"
    - "D-02 (SGV2-30): WeightQuantAndCoding returns early for any conv op with inputIndexes.size() > 1, inserted after the existing quanParameter early-return and BEFORE the weightQuantBits==0 compressFloatWeightToSparse path"
    - "OQ2 resolved (Option A): the sgfp4_encode static lib is defined at root CMakeLists.txt above add_subdirectory(tools/converter) whenever MNN_BUILD_CONVERTER is on, so --sgfp4 always works in a converter build"
  artifacts:
    - path: "tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp"
      provides: "the registered graph-rewrite pass (auto-globbed into MNNConverterOpt)"
      contains: "PostConverterRegister<InsertSGFP4Dequant> __l(\"InsertSGFP4Dequant\")"
      min_lines: 180
    - path: "tools/converter/include/config.hpp"
      provides: "modelConfig::useSGFP4 flag field"
      contains: "bool useSGFP4 = false;"
    - path: "tools/converter/include/PostConverter.hpp"
      provides: "RunNetPass declaration (symbol already has external linkage in PostConverter.cpp:144; needed by plan 11-04 tests)"
      contains: "void RunNetPass"
    - path: "CMakeLists.txt"
      provides: "sgfp4_encode lib hoisted above the converter subdirectory (defined exactly once across the build)"
      contains: "add_library(sgfp4_encode STATIC"
    - path: "tools/converter/CMakeLists.txt"
      provides: "MNNConvertDeps links the encoder (both SHARED and STATIC branches, via a single post-IF target_link_libraries line)"
      contains: "sgfp4_encode"
  key_links:
    - from: "tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp"
      to: "tools/fp4/sgfp4_encode.hpp"
      via: "sgfp4_encode::encode(weights, dimO, dimI, kSGFP4ConverterEncodeConfig) — config-carrying overload"
      pattern: "sgfp4_encode::encode"
    - from: "tools/converter/source/optimizer/PostConverter.cpp"
      to: "tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp"
      via: "final-batch RunNetPass entry, pass before ReIndexTensor"
      pattern: "InsertSGFP4Dequant\", \"ReIndexTensor"
    - from: "tools/converter/source/common/WeightQuantAndCoding.cpp"
      to: "the pass's rewrite fingerprint"
      via: "inputIndexes.size() > 1 early return (same fingerprint as RemoveParams.cpp:70-73, inverted in the pass)"
      pattern: "inputIndexes.size\\(\\) > 1"
    - from: "tools/converter/CMakeLists.txt"
      to: "CMakeLists.txt (root)"
      via: "target_link_libraries(MNNConvertDeps PUBLIC sgfp4_encode) resolving against the hoisted target"
      pattern: "target_link_libraries\\(MNNConvertDeps PUBLIC sgfp4_encode\\)"
---

<objective>
Make the SGFP4 v2 graph-rewrite pass exist, compile, and be invoked: hoist the `sgfp4_encode` encoder library above the converter subdirectory (OQ2 Option A), add the `modelConfig::useSGFP4` field and the `RunNetPass` declaration, write the `InsertSGFP4Dequant` PostConverter pass (D-01/D-03/D-06/D-07/D-08 + Phase 8 D-11 buffer contract + spilled-weight reload), register it in the final pass batch BEFORE `ReIndexTensor` (research KEY Q2 order lock), and add the D-02 `WeightQuantAndCoding` topology skip-guard.

Purpose: SGV2-28 (the pass) + SGV2-30 (the skip-guard) — the converter-side machinery the CLI flag (plan 11-03) triggers and the tests (plan 11-04) verify. With the flag defaulting false, this plan lands as dead code per D-14.
Output: new pass file + 7 edited files; converter builds with the encoder linked; existing suites stay green.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/ROADMAP.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-PATTERNS.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-CONTEXT.md

@tools/converter/source/optimizer/PostTreatUtils.hpp
@tools/converter/source/optimizer/PostConverter.cpp
@tools/converter/source/optimizer/postconvert/SplitBlockQuantConvolution.cpp
@tools/converter/source/optimizer/postconvert/ReIndexTensor.cpp
@tools/converter/source/common/WeightQuantAndCoding.cpp
@tools/converter/source/common/RemoveParams.cpp
@tools/converter/source/common/writeFb.cpp
@tools/converter/include/config.hpp
@tools/converter/include/PostConverter.hpp
@tools/converter/CMakeLists.txt
@tools/fp4/CMakeLists.txt
@tools/fp4/sgfp4_encode.hpp
@CMakeLists.txt
@tools/converter/source/TestSGFP4Converter.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hoist sgfp4_encode above the converter, link it into MNNConvertDeps, add useSGFP4 field + RunNetPass declaration</name>
  <files>CMakeLists.txt, tools/fp4/CMakeLists.txt, tools/converter/CMakeLists.txt, tools/converter/include/config.hpp, tools/converter/include/PostConverter.hpp</files>
  <read_first>CMakeLists.txt (lines ~905-925: the `if (NOT MNN_SKIPBUILD_GEOMETRY)` block containing add_subdirectory(tools/converter) at :913-916, and the MNN_BUILD_SGFP4_TOOLS include at :960-962), tools/fp4/CMakeLists.txt (whole file — the sgfp4_encode definition being hoisted at :7-8), tools/converter/CMakeLists.txt (lines 40-105: MNNConvertDeps SHARED/STATIC branches + TestSGFP4Converter wiring), tools/converter/include/config.hpp (fields :38-78, useHQQ slot), tools/converter/include/PostConverter.hpp (whole file), tools/converter/source/optimizer/PostConverter.cpp (RunNetPass definition at :144 — declaration must match exactly)</read_first>
  <action>
Per D-01/OQ2 (Option A — encoder always available in converter builds; research KEY Q1):

1. Root `CMakeLists.txt`: inside the existing `if (NOT MNN_SKIPBUILD_GEOMETRY)` block, immediately ABOVE `add_subdirectory(${CMAKE_CURRENT_LIST_DIR}/tools/converter)` (:913-916), add a guarded hoist: `IF(MNN_BUILD_CONVERTER)` → `add_library(sgfp4_encode STATIC ${CMAKE_CURRENT_LIST_DIR}/tools/fp4/sgfp4_encode.cpp)` + `target_include_directories(sgfp4_encode PUBLIC ${CMAKE_CURRENT_LIST_DIR}/tools/fp4 ${CMAKE_CURRENT_LIST_DIR}/include ${CMAKE_CURRENT_LIST_DIR}/3rd_party/half)` + `set_target_properties(sgfp4_encode PROPERTIES POSITION_INDEPENDENT_CODE ON)` (Linux SHARED MNNConvertDeps needs PIC; harmless under MSVC — research A3) → `ENDIF()`. Add a short comment: hoisted so the target exists when tools/converter configures (Phase 11, plan 11-01).
2. `tools/fp4/CMakeLists.txt`: wrap the existing `add_library(sgfp4_encode STATIC ...)` + `target_include_directories(sgfp4_encode ...)` pair (:7-8) in `if(NOT TARGET sgfp4_encode)` / `endif()` so the lib is defined exactly once when both MNN_BUILD_CONVERTER and MNN_BUILD_SGFP4_TOOLS are ON (research Pitfall 1). Leave the tools executables untouched.
3. `tools/converter/CMakeLists.txt`: after the MNNConvertDeps IF/ELSE block (i.e. after the SHARED/STATIC branch closes, before the `IF(NOT MNN_BUILD_SHARED_LIBS)` block at ~:60), add ONE line: `target_link_libraries(MNNConvertDeps PUBLIC sgfp4_encode)` — applies to both branches; after the hoist the target exists at configure time. This CMakeLists edit also re-triggers the configure that picks up the new pass .cpp glob (research Pitfall 7).
4. `tools/converter/include/config.hpp`: add `bool useSGFP4 = false;` adjacent to `bool useHQQ = false;` (:44 area). In-place `= false` default like useHQQ — no constructor-init-list edit (D-04 field; the CLI parse lands in plan 11-03).
5. `tools/converter/include/PostConverter.hpp`: add alongside the existing `optimizeNet` declaration: `void RunNetPass(const std::vector<std::string>& passes, std::unique_ptr<MNN::NetT>& originNet);` — signature character-for-character matching the definition at PostConverter.cpp:144 (plan 11-04's tests call it; only the declaration is missing today).

Then re-configure and build: `cmake --build . --target MNNConvertDeps TestSGFP4Converter` (existing build dir) — proves target ordering and the unchanged test executable still links (PHASE A/B of TestSGFP4Converter.exe must still pass: flag-off invariance).

   Execution note (wave-1 build serialization): plan 11-02 runs in the same wave and rebuilds `sgfp4_inject.out` from the SAME build directory — if the two wave-1 plans execute concurrently, serialize the `cmake --build` invocations (or configure 11-02's tool build in a separate build dir); this task's CMake re-configure must not race a parallel 11-02 target build.
  </action>
  <verify>Re-configure + build MNNConvertDeps and TestSGFP4Converter succeed; `TestSGFP4Converter.exe` still prints "PASS (layout + reload parity)"; source assertions: root CMakeLists.txt contains `add_library(sgfp4_encode STATIC` at a line number BELOW the `add_subdirectory(...tools/converter)` line; tools/fp4/CMakeLists.txt contains `if(NOT TARGET sgfp4_encode)`; tools/converter/CMakeLists.txt contains `target_link_libraries(MNNConvertDeps PUBLIC sgfp4_encode)`; config.hpp contains `bool useSGFP4 = false;`; PostConverter.hpp contains `void RunNetPass`</verify>
  <acceptance_criteria>
- `Select-String -Path CMakeLists.txt -Pattern "add_library\(sgfp4_encode STATIC"` returns a line number LOWER than the `add_subdirectory(...tools/converter)` match
- `Select-String -Path tools\fp4\CMakeLists.txt -Pattern "NOT TARGET sgfp4_encode"` matches
- `Select-String -Path tools\converter\CMakeLists.txt -Pattern "target_link_libraries\(MNNConvertDeps PUBLIC sgfp4_encode\)"` matches exactly once
- `Select-String -Path tools\converter\include\config.hpp -Pattern "bool useSGFP4 = false;"` matches
- `Select-String -Path tools\converter\include\PostConverter.hpp -Pattern "void RunNetPass"` matches
- Configure with BOTH `MNN_BUILD_SGFP4_TOOLS=ON` and `MNN_BUILD_CONVERTER=ON` does NOT error with a duplicate-target message (single-definition proof)
- `TestSGFP4Converter.exe` exits 0 unchanged
  </acceptance_criteria>
  <done>CMake target graph correct in all option combinations; useSGFP4 field and RunNetPass declaration present; converter + existing SGFP4 converter test build and pass</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the InsertSGFP4Dequant pass, register it before ReIndexTensor, add the D-02 skip-guard</name>
  <files>tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp, tools/converter/source/optimizer/PostConverter.cpp, tools/converter/source/common/WeightQuantAndCoding.cpp</files>
  <read_first>tools/converter/source/optimizer/PostTreatUtils.hpp (PostConverter base + PostConverterRegister<T> macro :20-41), tools/converter/source/optimizer/postconvert/SplitBlockQuantConvolution.cpp (pass skeleton, config access :26, tensorName growth :95-97, includes :7-17), tools/converter/source/common/RemoveParams.cpp (inputs>1 early-break :70-73; loadExternalParam :180-215; storeWeight swap-empty idiom :26-29; saveExternalData net+subgraph walk :145-177), tools/converter/source/common/WeightQuantAndCoding.cpp (op-type gate :52-58, quanParameter return :60-63, dims arithmetic :126-131), tools/converter/source/optimizer/PostConverter.cpp (final batch :393-394; optimizeNet double-RunOptimize :631-716; temp-bin ofstream :639-647), tools/converter/source/TestSGFP4Converter.cpp (makeSgfp4Op buffer-staged OpT builder :57-74 — the reference OpT construction), tools/fp4/sgfp4_encode.hpp (encode overloads + kDefaultEncodeConfig extern; MSVC C2086 warning — never redefine), tools/fp4/sgfp4_inject_core.hpp (node naming `_sgfp4` + inputs[1] splice conventions), tools/converter/source/common/writeFb.cpp (_postTreatOp order :23-37 — proof the pass finishes before RemoveAndStoreParam)</read_first>
  <action>
Per D-01/D-02/D-03/D-06/D-07/D-08 + Phase 8 D-11 contract + research KEY Q2/Q3:

1. NEW FILE `tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp` — class `InsertSGFP4Dequant : public PostConverter` with `bool onExecute(std::unique_ptr<MNN::NetT>& net) const override`. File header comment cites plan 11-01 and the D-NN decisions implemented. Structure:

   - Top guard (D-14): `auto config = Global<modelConfig>::Get(); if (nullptr == config || !config->useSGFP4) { return true; }`
   - D-08 constant, file-scope in this .cpp ONLY (never a header — MSVC C2086 pitfall): `static const sgfp4_encode::EncodeConfig& kSGFP4ConverterEncodeConfig = sgfp4_encode::kDefaultEncodeConfig;` with a comment: Python-identical defaults by design (cross-repo parity outranks one-sided promotion, Phase 10 D-09); the validated delta lives in `tools/fp4/real_weight_validation_report.json` and this alias is the single edit point when adopted upstream. No CLI threshold override (D-08).
   - Shared per-op processing (a helper taking the op plus a lambda/namespace-appender for tensor-name growth, applied to BOTH `net->oplists` and every `net->subgraphs` → `subgraph->nodes` — D-03, exact `saveExternalData` walk shape from RemoveParams.cpp:145-177; subgraph branch appends to `subgraph->tensors` and uses its pre-push size as the new index; NEVER renumber existing indices — research KEY Q8):
     * Type gate (D-06): only `OpType_Convolution`, `OpType_ConvolutionDepthwise`, `OpType_Deconvolution`, `OpType_DeconvolutionDepthwise`; else skip.
     * Rewrite condition (idempotency + D-02 fingerprint): skip unless `op->inputIndexes.size() == 1`; skip if `param->quanParameter.get() != nullptr` (int8 weights are not FP32 encode targets — mirrors WeightQuantAndCoding.cpp:60-62).
     * Weight acquisition (research KEY Q3): `param->weight` non-empty → use directly. Else if `param->external.size() == 3` → first `if (config->externalFile) { config->externalFile->flush(); }` (ofstream still open+buffered — Pitfall 5; null when open failed, PostConverter.cpp:645-647), then `FileLoader` on `.__convert_external_data.bin`, `offset(external[0])`, read `external[1]` bytes into a weight vector and `external[2]` bytes into `param->bias` (bias RESTORED — the dequant node carries only the container; semantics per loadExternalParam, RemoveParams.cpp:180-215). FileLoader open/read failure or short read → MNN_ERROR naming the op, skip this conv untouched, mark failed (T-11-01/T-11-03 transactional rule: never mutate a conv whose weights could not be read).
     * Dims (Pitfall 8 — mirror WeightQuantAndCoding.cpp:126-131 exactly): `oc = common->outputCount; kernelSize = (int)(weight.size() / oc); dimO = oc; dimI = kernelSize;` Element count computed in `size_t` (T-11-02); pre-encode assertion `weight.size() == (size_t)dimO * dimI` — mismatch → MNN_ERROR + skip + failed.
     * Light-tier floor (D-07): `if ((size_t)dimO * dimI < 4096 || dimI == 1) { continue; }` — leave FP32 untouched.
     * Encode: `auto container = sgfp4_encode::encode(weight.data(), dimO, dimI, kSGFP4ConverterEncodeConfig);` Empty container (encoder's invalid-input contract: NaN/Inf, bad dims) → MNN_ERROR naming the op, skip untouched, mark failed — never encode garbage (V5 / T-11-03).
     * Node construction (Phase 8 D-11 buffer contract — shape from TestSGFP4Converter.cpp makeSgfp4Op :57-74): new `MNN::OpT` with `type = OpType_SGFP4Dequant`, `main.type = OpParameter_SGFP4DequantParam`, `SGFP4DequantParamT{ magic = MNN::kSGFP4Magic, dims = {dimO, dimI}, buffer = container bytes (vector<int8_t>, memcpy) }`; `external` stays `{}`, `externalPath` stays empty — externalization rides RemoveAndStoreParam/storeSGFP4Container untouched. New output index: `newIndex = (int)namespaceSize` (tensorName.size() / subgraph->tensors.size(), pre-push), push name `<convName>_sgfp4` (fallback `sgfp4_weight_<newIndex>` when the op name is empty) — SplitBlockQuantConvolution.cpp:95-97 precedent; op `name` mirrors the same string (injection-tool naming convention, CONTEXT specifics).
     * Splice: `dequantOp->outputIndexes = {newIndex};` insert the dequant op into the oplist IMMEDIATELY BEFORE the conv op (producer precedes consumer); `conv->inputIndexes.push_back(newIndex);` (becomes inputs[1]; conv op type unchanged; `common` untouched).
     * Clear (after successful insertion only): clear `param->weight` via the swap-empty idiom (RemoveParams.cpp:26-29); `param->external.clear()`; keep `param->bias` (restored above if spilled).
   - Return `failed ? false : true` (RunNetPass only logs false — research §Error reporting; the artifact stays safe because every failure path skipped its conv transactionally).
   - Registration (D-01): `static PostConverterRegister<InsertSGFP4Dequant> __l("InsertSGFP4Dequant");`
   - Includes following SplitBlockQuantConvolution.cpp:7-17, plus `MNN/SGFP4DequantUtils.hpp`, `sgfp4_encode.hpp`, `core/FileLoader.hpp`, `<cstring>`, `<vector>`.

2. `tools/converter/source/optimizer/PostConverter.cpp` — change the final batch tail (:393-394) from `RunNetPass({"ReIndexTensor"}, newNet);` to `RunNetPass({"InsertSGFP4Dequant", "ReIndexTensor"}, newNet);` (D-01; BEFORE ReIndexTensor per research KEY Q2 order lock: ReIndexTensor then compacts/dedups the pass's tensorName additions for free; `ReIndexOnnxIfAlias` stays last and unchanged).

3. `tools/converter/source/common/WeightQuantAndCoding.cpp` — immediately after the `quanParameter != nullptr` early-return (:60-62) and BEFORE the `useHqq` read / the `weightQuantBits == 0` → `compressFloatWeightToSparse` path, insert (D-02, SGV2-30): early `return` when `op->inputIndexes.size() > 1`, with comment: SGFP4-rewritten convs carry their weight as a second input tensor; an original converter conv has only its activation index. This guard MUST fire before compressFloatWeightToSparse, which would otherwise run on a cleared-weight rewritten conv.

4. Re-configure (the tools/converter/CMakeLists.txt edit from Task 1 already forces it) and build `MNNConvert` + `TestSGFP4Converter`; confirm the pass file was globbed (a "Can't find pass of InsertSGFP4Dequant" log during any conversion would indicate it was NOT — research Pitfall 7). Run `TestSGFP4Converter.exe` (PHASE A/B green) and `run_test.out` quick `op/sgfp4` subset — flag-off invariance (D-14).
  </action>
  <verify>Build succeeds for MNNConvert + TestSGFP4Converter; `TestSGFP4Converter.exe` exits 0; `run_test.out op/sgfp4` green (flag OFF ⇒ dead code); source assertions: InsertSGFP4Dequant.cpp contains `PostConverterRegister<InsertSGFP4Dequant> __l("InsertSGFP4Dequant")` and `kSGFP4ConverterEncodeConfig` and `useSGFP4` and `inputIndexes.size() == 1` and `externalFile` flush and `< 4096`; PostConverter.cpp contains `RunNetPass({"InsertSGFP4Dequant", "ReIndexTensor"}`; WeightQuantAndCoding.cpp contains an `inputIndexes.size() > 1` return positioned between the quanParameter return and the weightQuantBits==0 block</verify>
  <acceptance_criteria>
- `InsertSGFP4Dequant.cpp` exists and contains, verbatim: `PostConverterRegister<InsertSGFP4Dequant> __l("InsertSGFP4Dequant");`, `static const sgfp4_encode::EncodeConfig& kSGFP4ConverterEncodeConfig = sgfp4_encode::kDefaultEncodeConfig;`, the `!config->useSGFP4` dead-code guard, the `< 4096` / `dimI == 1` light-tier floor, `config->externalFile` flush before FileLoader reads, and bias-restore handling for `external.size() == 3`
- `Select-String -Path tools\converter\source\optimizer\PostConverter.cpp -Pattern 'InsertSGFP4Dequant", "ReIndexTensor'` matches (pass BEFORE ReIndexTensor in one RunNetPass call)
- `Select-String -Path tools\converter\source\common\WeightQuantAndCoding.cpp -Pattern "inputIndexes\.size\(\) > 1"` matches, and the match line number is BETWEEN the `quanParameter.get() != nullptr` return and the `weightQuantBits == 0` block
- `MNNConvert.exe` builds and a flag-OFF conversion (any small model, no `--sgfp4`) still succeeds — zero behavior change (D-14)
- `TestSGFP4Converter.exe` exits 0; `run_test.out op/sgfp4` 13/13 green
  </acceptance_criteria>
  <done>The pass is registered, invoked before ReIndexTensor, idempotent by construction, dead when the flag is off, and the WeightQuantAndCoding skip-guard is in place — SGV2-28 machinery + SGV2-30 closed at the source level</done>
</task>

</tasks>

## Artifacts this phase produces (plan 11-01)

| Symbol / Artifact | Kind | Location |
|---|---|---|
| `InsertSGFP4Dequant` | class (file-local) | `tools/converter/source/optimizer/postconvert/InsertSGFP4Dequant.cpp` |
| `"InsertSGFP4Dequant"` | PostConverter pass registration string | same file (invoked from `PostConverter.cpp` final batch) |
| `kSGFP4ConverterEncodeConfig` | file-static const-reference alias of `sgfp4_encode::kDefaultEncodeConfig` | same file (D-08 swap point) |
| `modelConfig::useSGFP4` | config field (`bool`, default false) | `tools/converter/include/config.hpp` |
| `RunNetPass` declaration | header declaration of an existing symbol | `tools/converter/include/PostConverter.hpp` |
| sgfp4_encode hoist | CMake target relocation (no new target; definition moved to root, guarded `if(NOT TARGET ...)` in tools/fp4) | `CMakeLists.txt`, `tools/fp4/CMakeLists.txt`, `tools/converter/CMakeLists.txt` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user model file → converter pass | untrusted model data (weights, dims, external descriptors) crosses into the new pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | spilled-weight reload in `InsertSGFP4Dequant.cpp` | mitigate | FileLoader open/read failures checked; `external` sizes treated as untrusted — short read ⇒ MNN_ERROR + transactional skip (conv untouched) |
| T-11-02 | DoS | dims arithmetic (`dimO*dimI`) | mitigate | element count in `size_t`; pre-encode assertion `weight.size() == (size_t)dimO*dimI`; encoder's own dims>65536 rejection (sgfp4_encode.hpp contract) |
| T-11-03 | Tampering (integrity) | encode failure propagation | mitigate | empty-container return ⇒ MNN_ERROR naming op + conv skipped transactionally + pass returns false — never a cleared-weight conv without its dequant node, never garbage bytes |
| T-11-04 | Tampering | `.__convert_external_data.bin` temp-file path (CWD) | accept | pre-existing converter behavior, unchanged this phase (deleted at writeFb.cpp:170); no new exposure added |
</threat_model>

<verification>
- CMake configure + build green in all relevant option combos (converter on; converter+SGFP4 tools on — single target definition)
- `TestSGFP4Converter.exe` PASS; `run_test.out op/sgfp4` 13/13 (flag-off invariance, D-14)
- All source assertions in Task 2 acceptance criteria hold
- A flag-off conversion of any model is byte-behavior unchanged (pass never runs its body)
</verification>

<success_criteria>
- SGV2-28 machinery exists: registered, ordered, idempotent, light-tier-floored, buffer-staging pass handling BOTH in-param and external-spilled weights
- SGV2-30 closed: `WeightQuantAndCoding` skips `inputs>1` convs before any weight transform (incl. the sparse path)
- D-08: greppable `kSGFP4ConverterEncodeConfig` swap point with validated-delta documentation comment
- Zero regression with the flag off (D-14)
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/11-graph-rewrite-postconverter-pass-cli-flag/11-01-SUMMARY.md` when done
</output>
