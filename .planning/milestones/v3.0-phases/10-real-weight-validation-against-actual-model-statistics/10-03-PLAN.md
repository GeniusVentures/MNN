---
phase: "10-real-weight-validation-against-actual-model-statistics"
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - tools/fp4/sgfp4_encode.hpp
  - tools/fp4/sgfp4_encode.cpp
  - tools/fp4/validate_real_weights.py
  - tools/fp4/real_weight_validation_report.md
  - tools/fp4/real_weight_validation_report.json
autonomous: true
requirements: [SGV2-26, SGV2-27]
must_haves:
  truths:
    - "D-08: sgfp4_encode.hpp grows an EncodeConfig struct (thresholds only, per D-10) whose defaults are the Python-identical DEFAULT_V2_THRESHOLDS values; a new overload encode(weights, dimO, dimI, const EncodeConfig&) exists alongside the UNCHANGED knob-less one-shot encode(weights, dimO, dimI)"
    - "Behavior preservation: with defaults, the config-carrying path is bit-identical to the shipped path — proven by the existing 13/13 green suites (incl. op/sgfp4/encode byte-exactness fixtures) passing WITHOUT modification"
    - "D-11 wiring complete: validate_real_weights.py --encode-dump leg drives sgfp4_encode_dump.out on sampled real layers; sampled containers are byte-compared vs fp4_exporter --adaptive output (rtol 1e-4 decode-vs-decode as contractual fallback, recorded if hit); C++ decode-error stats match the Python reference within rtol 1e-4"
    - "Sampling covers: every non-64-aligned AlexNet tensor (features.0.weight 64×363, classifier.6.weight 1000×4096), the largest aligned plane (classifier.1.weight), one aligned conv kernel, and ≥2 light-tier tensors — with every layer still passing/failing D-07 via the Python reference regardless of sampling"
    - "D-07/D-08 revision loop (data-driven, NOT a user gate): if any layer failed the gate in 10-01/10-03 runs, a minimal data-justified threshold delta is derived from the failing (layer, size, error) tuples, applied via --thresholds re-run until all layers pass, and shipped as default values in kDefaultEncodeConfig ONLY if the evidence justifies promoting them; if no layer fails, defaults stand and the report states 'no data-justified revision' explicitly"
    - "D-09: any divergence from gnus-poc defaults is rendered in the final report as a documented-delta block (size / old / new / motivating statistic) — a proposal for the sibling repo, with no gnus-poc-side code changes"
  artifacts:
    - path: "tools/fp4/sgfp4_encode.hpp"
      provides: "EncodeConfig struct + config-carrying encode overload; one-shot overload unchanged"
      contains: "EncodeConfig"
    - path: "tools/fp4/sgfp4_encode.cpp"
      provides: "config-threaded quadtree gate (kDefaultV2Thresholds lookup replaced by config-carried array; one-shot forwards with defaults)"
    - path: "tools/fp4/real_weight_validation_report.md"
      provides: "FINAL committed validation report: stats + gate + parity tables + delta block (phase acceptance evidence per D-05)"
  key_links:
    - from: "tools/fp4/sgfp4_encode.cpp"
      to: "tools/fp4/sgfp4_encode.hpp"
      via: "one-shot encode() forwards to encode(..., kDefaultEncodeConfig)"
      pattern: "kDefaultEncodeConfig"
    - from: "tools/fp4/validate_real_weights.py"
      to: "tools/fp4/sgfp4_encode_dump.out"
      via: "subprocess per sampled layer (dump → encode → byte-compare vs exporter)"
      pattern: "sgfp4_encode_dump"
---

<objective>
Close the phase: (a) grow the encoder API with the D-08 `EncodeConfig` struct (Phase 9 D-10 deferral resolved) with Python-identical defaults and overload-based wiring; (b) wire the C++ parity-sampling leg into `validate_real_weights.py` using 10-02's harness and run it on the approved AlexNet corpus; (c) run the data-driven threshold revision loop if — and only if — gate failures demand it, landing any delta as a documented, data-justified change; (d) finalize the committed report artifact (stats + parity + delta) that is the phase's acceptance evidence.

Purpose: retires the synthetic-fixture-tuned-assumptions risk (SGV2-26/27) and hands Phase 11 a validated, tunable encoder.
Output: extended `sgfp4_encode.hpp/.cpp`, parity-wired driver, final `real_weight_validation_report.md`/`.json`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/STATE.md
@.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-01-SUMMARY.md
@.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-02-SUMMARY.md
@.planning/workstreams/sgfp4-pivot/phases/09-real-weight-c-encoder-port/09-CONTEXT.md

@tools/fp4/sgfp4_encode.hpp
@tools/fp4/sgfp4_encode.cpp
@tools/fp4/sgfp4_encode_dump.cpp
@tools/fp4/validate_real_weights.py
@tools/fp4/CMakeLists.txt
@tools/fp4/README.md
@include/MNN/SGFP4DequantUtils.hpp
@W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\fp4_exporter.py
@W:\gnus\GeniusCognitiveSystem\GNUS-NEO-SWARM\gnus-poc\quantize\sgfp4_decoder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EncodeConfig struct + config-carrying overload (behavior-preserving)</name>
  <files>tools/fp4/sgfp4_encode.hpp, tools/fp4/sgfp4_encode.cpp</files>
  <behavior>
    - Existing suites green unchanged: with no config argument, every op/sgfp4/* suite (13/13 incl. op/sgfp4/encode byte-exactness fixtures) passes without any test-file modification — this IS the overload-compatibility proof
    - encode(w, dimO, dimI, EncodeConfig{values == kDefaultV2Thresholds}) produces a container byte-identical to encode(w, dimO, dimI) on any input
    - encode(w, dimO, dimI, configWithDifferentGates) produces a measurably different container on at least one outlier-heavy input (proves the knob actually threads through to the quadtree gate)
    - Invalid-input contract preserved on BOTH overloads (empty vector on NaN/Inf/bad dims)
  </behavior>
  <action>
    Per 10-RESEARCH §6 (structure at planner discretion, per D-08): in `sgfp4_encode.hpp`, add inside namespace sgfp4_encode a struct `EncodeConfig` with `struct Gate { double maxMse; double maxRelative; };` and `Gate leafGates[5];` indexed in kDefaultV2Thresholds order (64/32/16/8/4), plus `static const EncodeConfig kDefaultEncodeConfig;` whose values equal the Python `DEFAULT_V2_THRESHOLDS` exactly ({64: 0.01/0.05, 32: 0.005/0.03, 16: 0.002/0.02, 8: 0.001/0.01, 4: 0.0005/0.005}). Declare the new overload `std::vector<uint8_t> encode(const float* weights, int dimO, int dimI, const EncodeConfig& config);` and leave the existing one-shot overload's signature and docstring EXACTLY as shipped (D-08: knob-less overload unchanged — overload, NOT a default argument, so "which call site uses tuned values" stays greppable). Future knobs deliberately absent (D-10: thresholds only).

    In `sgfp4_encode.cpp`: define `kDefaultEncodeConfig` with the values above; thread the quadtree context's gate lookup (the hard-coded `kDefaultV2Thresholds[5]` use, ~line 491 area) through a config-carried `Gate[5]`; the one-shot overload becomes a one-line forward to the config overload with `kDefaultEncodeConfig`. No numerical-path changes whatsoever when defaults are used — identical comparisons, identical order.

    Verify behavior via the existing suites (build + run `run_test.out` gating `MNN_BUILD_SGFP4_TOOLS=ON`, Phase 9 verified build lines: `cmake --build build --config Release --target run_test.out` then run with the sgfp4 filter, matching the 09-04 suite invocation). For the different-config probe, a throwaway standalone check linking `sgfp4_encode` (or a temporary addition LATER removed) may assert divergence; do NOT add permanent tests beyond the existing suites (D-05: no new test gates this phase — the existing suites are the compatibility proof).

    Rebuild `sgfp4_encode_dump.out` still clean (its one-shot call site is untouched by the overload).
  </action>
  <verify>cmake --build build --config Release --target run_test.out then run_test.out with the Phase 9 sgfp4 suite invocation — 13/13 suites green, zero test files modified; git status shows no changes under test/</verify>
  <acceptance_criteria>EncodeConfig + both overloads compile; defaults bit-equal to gnus-poc table; all existing suites pass unmodified; a config delta demonstrably changes output (probe evidence in summary); sgfp4_encode_dump.out still builds</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire C++ parity-sampling leg into the driver + run on AlexNet</name>
  <files>tools/fp4/validate_real_weights.py, tools/fp4/real_weight_validation_report.md, tools/fp4/real_weight_validation_report.json</files>
  <action>
    Activate the `--encode-dump` / `--sample` leg stubbed in 10-01. Per sampled layer (subprocess pattern from `quantize_fp4.py`, which already shells out to MNN binaries):

    1. Write the layer's FP32 weights as little-endian raw dump under `--workdir` (`astype('<f4').tofile()`).
    2. Invoke `sgfp4_encode_dump.out --weights <dump> --dimO N --dimI M --out <container>` (nonzero exit → parity FAIL with the exit code surfaced; exit 2 additionally names the layer as encoder-rejected).
    3. Encode the same layer via gnus-poc `export_weights(..., adaptive=True, thresholds=effective_table)` and byte-compare (`filecmp.cmp(..., shallow=False)`). Assert byte-exact as primary (Phase 9's achieved bar per 09-01/09-04); if a platform rounding case surfaces, fall back to the contractual decode-vs-decode rtol 1e-4 (decode via `dequant_sgfp4_container_cpu`-equivalent Python `decode_v2`; pattern from `test/op/SGFP4EncodeTest.cpp`, kEncodeRelTol = 1e-4) and RECORD the divergence as expected-behavior in the report.
    4. Compute C++ decode-error stats from the sampled container (Python-side decode of the C++-produced bytes) and check they match the Python-computed reference stats within rtol 1e-4.
    5. Delete the transient dump (keep ~160 MB peak, one layer at a time).

    Default sampling rule (discretion per CONTEXT, encoded not hardcoded): every non-64-aligned tensor (features.0.weight, classifier.6.weight), the largest aligned plane (classifier.1.weight, 37.7M elems — minutes not hours per 10-RESEARCH timing), one aligned conv kernel, and ≥2 light-tier tensors. `--sample` overrides. Report gains the parity table (layer, byte-exact y/n or rtol fallback note, decode-stats rtol, PASS/FAIL); any parity mismatch → exit 4.

    Run the full driver with `--encode-dump` on the corpus and commit the regenerated report + sidecar (parity tables now real).
  </action>
  <verify>python tools/fp4\validate_real_weights.py --encode-dump build\Release\sgfp4_encode_dump.out completes with exit 0 (or 3 if the 10-01 gate state has failures — those feed Task 3); report parity table has ≥6 sampled real layers, primarily byte-exact PASS rows</verify>
  <acceptance_criteria>Both non-64-aligned tensors byte-exact (or documented rtol fallback) vs the exporter; classifier.1.weight sampled and parity-passing (byte-exact expected, minutes runtime); exit 4 wired for parity mismatch; transient dumps cleaned; committed report shows the parity table (D-11 leg of SGV2-27 evidence complete)</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Data-driven threshold decision + final report (delta or explicit none)</name>
  <files>tools/fp4/validate_real_weights.py, tools/fp4/real_weight_validation_report.md, tools/fp4/real_weight_validation_report.json, tools/fp4/sgfp4_encode.hpp, tools/fp4/sgfp4_encode.cpp</files>
  <action>
    Two branches by data — this is D-08's revision policy executing, NOT a user decision:

    **Branch A — all layers passed the D-07 gate (exit 0):** Defaults stand. The report's summary states "no data-justified revision" explicitly (never silently). EncodeConfig (Task 1) already landed with Python-identical defaults; no threshold edits. Proceed to finalization.

    **Branch B — one or more layers failed (exit 3):** The quadtree force-accepts 4×4 leaves regardless of error (verified: `quadtree.py` `if size <= min_block_size: accept = True`), so failures are possible by construction — typically forced-4×4 leaves exceeding `max_relative 0.005` on outlier-heavy planes. Execute the locked loop:
    1. Read the failing `(layer, size, error)` tuples from the sidecar JSON (recorded by 10-01's gate evaluation).
    2. Derive a MINIMAL delta table — touch only the leaf sizes that actually failed, raising `max_mse`/`max_relative` to the smallest value that clears the observed worst error with ≤10% headroom (smallest-possible principled relaxation; document the arithmetic).
    3. Write `delta.json` (exact DEFAULT_V2_THRESHOLDS key shape) and re-run the driver with `--thresholds delta.json` (first-class `export_weights(thresholds=...)` parameter — already plumbed in 10-01). Budget one revision iteration for this phase.
    4. All layers green under the delta → land the revised values as the `kDefaultEncodeConfig` defaults in `sgfp4_encode.hpp/.cpp` (documented rationale comment citing the motivating statistics), re-verify existing suites per Task 1's compatibility proof — if a fixture suite breaks under new defaults, keep defaults Python-identical instead and record the delta as report-only (byte-exactness fixtures outrank the promotion; D-09's upstream proposal path exists precisely for this), stating so in the report.
    5. Either way, render the D-09 documented-delta block in the report: three-column diff (size / old / new / motivating statistic) as the gnus-poc-side proposal — NO gnus-poc code changes.
    6. Annotate rather than fail any ε-escape layers (signal_power ≤ 1e-12) per 10-01's risk-R3 handling.

    **Finalization (both branches):** regenerate the report so its summary carries the final gate state, the parity table, the threshold decision with rationale, and the corpus provenance block (model sha256, versions). Report the effective final threshold table verbatim. Commit report + sidecar (+ encoder files if Branch B promoted defaults). This report is the phase's acceptance evidence (D-05).
  </action>
  <verify>Final driver invocation exits 0; report summary block names the branch taken, lists the final effective threshold table, and contains either the D-09 delta diff or the explicit "no data-justified revision" statement</verify>
  <acceptance_criteria>Every full-tier layer of the corpus meets the (possibly revised) thresholds' own targets — D-07 fully green, zero tolerated failing tail; if revised: delta is minimal, evidence-linked, suites still green or promotion explicitly declined with rationale; report + sidecar committed in final form; no gnus-poc-side changes; no test/op/ changes (D-05)</acceptance_criteria>
</task>

</tasks>

<verification>
- Behavior preservation: `git diff` shows zero changes under `test/`; all 13 sgfp4 suites green via the Phase 9 run_test.out invocation
- Full driver run with parity: exit 0; report contains 16-tensor stats table, pad-overhead rows for the two non-aligned tensors, ≥6-layer parity table, threshold-decision block
- If Branch B: `delta.json` exists under the workdir or report annex; re-run under delta is green; C++ defaults promotion consistent with the fixture-suite outcome
- `tools/fp4/README.md` updated (if conventions require): one paragraph on the driver + harness usage for future retuning
</verification>

<success_criteria>
- SGV2-26 closed: committed report demonstrates encoder parameters validated against the approved real corpus (AlexNet) with distribution evidence and all-layer gate pass
- SGV2-27 closed: revision loop executed to a documented conclusion (delta promoted or defaults affirmed), config struct shipped (Phase 9 D-10 deferral resolved), C++-vs-Python parity sampled and passing on the risky paths (non-aligned, largest plane)
- Phase 11 unblocked: encoder callable with defaults or explicit config, report is the hand-off artifact, gnus-poc delta documented for upstream (D-09)
</success_criteria>

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/10-real-weight-validation-against-actual-model-statistics/10-03-SUMMARY.md` when done
</output>
