---
phase: 08-schema-sidecar-wiring
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - source/backend/cpu/CPUSGFP4Dequant.cpp
  - source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp
  - source/core/OpCommonUtils.cpp
autonomous: true
requirements: [SGV2-22]

must_haves:
  truths:
    - "CPU and Vulkan decoders dispatch buffer-first: non-empty param->buffer() decodes from inline bytes with no FileLoader"
    - "Empty buffer falls back to the existing external-sidecar path with all current validation (T-01-04 file-size bounds, loader validity) preserved"
    - "Buffer-mode retains equivalent entry validation (magic/version gate + dims-consistency) and never writes partial output on malformed input"
  artifacts:
    - path: "source/backend/cpu/CPUSGFP4Dequant.cpp"
      provides: "buffer-first dispatch in onResize before the sidecar gate"
      contains: "sgfp4_is_v2_container"
    - path: "source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp"
      provides: "buffer-first dispatch in the creator before the sidecar gate"
      contains: "param->buffer()"
    - path: "source/core/OpCommonUtils.cpp"
      provides: "D-12 non-interception comment at createExecutionWithExternal (line ~665)"
      contains: "SGFP4Dequant"
  key_links:
    - from: "source/backend/cpu/CPUSGFP4Dequant.cpp"
      to: "param->buffer()"
      via: "mOp->main_as_SGFP4DequantParam() accessor"
      pattern: "param->buffer\\(\\)"
    - from: "source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp"
      to: "param->buffer()"
      via: "op->main_as_SGFP4DequantParam() accessor"
      pattern: "param->buffer\\(\\)"
---

<objective>
Add buffer-first dispatch to both SGFP4 decode Executions (CPU `onResize` and Vulkan creator) so a non-empty `param->buffer()` decodes directly from inline bytes, and an empty buffer falls through to the existing external-sidecar path unchanged. Also add the D-12 non-interception comment at `OpCommonUtils.cpp`'s `createExecutionWithExternal` switch.

Purpose: Implement D-01/D-02/D-03 — one unified decode convention across both data placements, unlocking single-file `.mnn` artifacts while keeping sidecar mode the original supported path (D-04).

Output: CPU and Vulkan decoders with buffer-first dispatch; D-12 comment; existing sidecar-path suites still green.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-PATTERNS.md
@source/backend/cpu/CPUSGFP4Dequant.cpp
@source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp
@source/core/OpCommonUtils.cpp
@include/MNN/SGFP4DequantUtils.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buffer-first dispatch in CPUSGFP4Dequant::onResize</name>
  <files>source/backend/cpu/CPUSGFP4Dequant.cpp</files>
  <read_first>
    - source/backend/cpu/CPUSGFP4Dequant.cpp (full: onResize lines 42-88, onExecute 90-108, queryFileSize 24-41)
    - include/MNN/SGFP4DequantUtils.hpp (sgfp4_is_v2_container at ~119-126, dequant_sgfp4_container_cpu signature)
    - 08-PATTERNS.md §"CPUSGFP4Dequant.cpp" (the dispatch shape to insert)
    - 08-RESEARCH.md §"Pattern 4" + §"Pitfall 4" + Open Question Q2 (validation strength decision)
  </read_first>
  <action>
    In `CPUSGFP4Dequant::onResize`, after the existing `nullptr == param` guard and BEFORE the current hard gate `if (!USE_EXTERNAL_DATA(param) || nullptr == mOp->externalPath()) { return NOT_SUPPORT; }`, insert the buffer-first branch (per D-01/D-02): read `const auto* buf = param->buffer();` (accessor form — a `const flatbuffers::Vector<uint8_t>*`, Pitfall 6). If `buf != nullptr && buf->size() > 0`, then `mContainer.assign(buf->data(), buf->data() + buf->size());` (copy into mContainer for safety — RESEARCH A2), then gate with `if (!MNN::sgfp4_is_v2_container(mContainer.data(), mContainer.size())) { return INVALID_VALUE; }` (magic/version entry check — D-02), then perform the dims-consistency check per Q2: allocate `std::vector<float> scratch(outputs[0]->elementSize());` and `if (!MNN::dequant_sgfp4_container_cpu(mContainer.data(), mContainer.size(), scratch.data(), outputs[0]->elementSize())) { return INVALID_VALUE; }`, then `return NO_ERROR;`. The existing sidecar block (offset/size sanity, T-01-04 `queryFileSize` probe, `FileLoader` valid() + bounded read) remains VERBATIM and unchanged below the new branch — it is the empty-buffer fallback. `onExecute` is unchanged (it already guards `mContainer.empty()` and returns `INVALID_VALUE` on oracle false). Do NOT reimplement T-01-04 in buffer mode (Q2 decision: eager oracle is the replacement bound).
  </action>
  <acceptance_criteria>
    - `CPUSGFP4Dequant::onResize` contains `const auto* buf = param->buffer();` before the `USE_EXTERNAL_DATA` gate.
    - The buffer branch calls `mContainer.assign(buf->data(), buf->data() + buf->size())` and `MNN::sgfp4_is_v2_container(...)` and `MNN::dequant_sgfp4_container_cpu(...)` for dims-consistency, returning `INVALID_VALUE` on failure.
    - The existing `if (!USE_EXTERNAL_DATA(param) || nullptr == mOp->externalPath()) { return NOT_SUPPORT; }` block and the subsequent T-01-04 / FileLoader code are still present (sidecar fallback unchanged).
    - `grep -n "param->buffer()" source/backend/cpu/CPUSGFP4Dequant.cpp` returns ≥ 1 hit.
  </acceptance_criteria>
  <verify>
    <automated>.\build\run_test.out op/sgfp4/uniform_decode; .\build\run_test.out op/sgfp4/mixed_decode</automated>
  </verify>
  <done>CPU onResize dispatches buffer-first (magic + eager-oracle dims check), falls back to the unchanged sidecar path; existing CPU SGFP4 suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Buffer-first dispatch in VulkanSGFP4Dequant creator</name>
  <files>source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp</files>
  <read_first>
    - source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp (full: creator lines 126-210 — the sidecar gate + host pre-validation)
    - source/backend/cpu/CPUSGFP4Dequant.cpp (Task 1 result — the dispatch convention to mirror)
    - 08-PATTERNS.md §"VulkanSGFP4Dequant.cpp" (buffer-first branch to insert + host pre-validation to reuse)
  </read_first>
  <action>
    In `VulkanSGFP4DequantCreator::onCreate`, restructure so `std::vector<uint8_t> container;` is declared BEFORE the dispatch. After the `nullptr == param` guard and BEFORE the current gate `if (!USE_EXTERNAL_DATA(param) || nullptr == op->externalPath())`, insert the buffer-first branch (per D-01/D-03): `const auto* buf = param->buffer();` and `if (buf != nullptr && buf->size() > 0) { container.assign(buf->data(), buf->data() + buf->size()); if (!MNN::sgfp4_is_v2_container(container.data(), container.size())) { MNN_ERROR("VulkanSGFP4Dequant: inline buffer failed v2 version gate\n"); return nullptr; } }`. The existing sidecar block (gate + T-03-02 `queryFileSize` + FileLoader read into `container`) becomes the `else` path, unchanged in behavior — it must still fill `container` exactly as today. Both branches then funnel into the EXISTING host pre-validation `dequant_sgfp4_container_cpu(container.data(), container.size(), scratch.data(), elementCount)` (Q2: buffer mode mirrors the sidecar mode's eager host pre-validation; no new validation code). The `elementCount`/`outputs[0]` checks and `return new VulkanSGFP4Dequant(backend, std::move(container), ...)` stay unchanged. Do NOT edit GLSL or the shader pipeline.
  </action>
  <acceptance_criteria>
    - `VulkanSGFP4DequantCreator::onCreate` contains `const auto* buf = param->buffer();` and a `buf != nullptr && buf->size() > 0` branch calling `container.assign(buf->data(), buf->data() + buf->size())` and `MNN::sgfp4_is_v2_container(...)`.
    - The existing `!USE_EXTERNAL_DATA(param) || nullptr == op->externalPath()` gate and T-03-02 `queryFileSize`/FileLoader read remain present as the empty-buffer path.
    - The host pre-validation `dequant_sgfp4_container_cpu(...)` is executed for BOTH branches (single code path, no duplicate).
    - `grep -n "param->buffer()" source/backend/vulkan/buffer/execution/VulkanSGFP4Dequant.cpp` returns ≥ 1 hit.
  </acceptance_criteria>
  <verify>
    <automated>.\build\run_test.out op/sgfp4/vulkan_uniform_parity</automated>
  </verify>
  <done>Vulkan creator dispatches buffer-first and reuses the shared host pre-validation; existing Vulkan parity suite green (pass-skip with no device).</done>
</task>

<task type="auto">
  <name>Task 3: D-12 non-interception comment at createExecutionWithExternal</name>
  <files>source/core/OpCommonUtils.cpp</files>
  <read_first>
    - source/core/OpCommonUtils.cpp (lines 665-680: the `switch (op->main_type())` in createExecutionWithExternal with Convolution2D/Scale/LayerNorm cases)
  </read_first>
  <action>
    Add a comment block immediately ABOVE the `switch (op->main_type())` in `OpCommonUtils::createExecutionWithExternal` (line ~665) documenting D-12: `OpType_SGFP4Dequant` is INTENTIONALLY absent from this switch. The decoder (`CPUSGFP4Dequant`/`VulkanSGFP4Dequant`) owns its own data-placement dispatch (buffer-first vs external-sidecar), so SGFP4 ops flow to `backend->onCreate` unmodified and must NOT be auto-rewritten with a session-derived externalPath like `Convolution2D`/`Scale`/`LayerNorm`. No code change — comment only. This stops the Phase 11 planner from hunting for a case that deliberately does not exist.
  </action>
  <acceptance_criteria>
    - `source/core/OpCommonUtils.cpp` contains the literal `OpType_SGFP4Dequant` within a comment at/above the `switch (op->main_type())` in `createExecutionWithExternal` (line ~665).
    - The comment states SGFP4 is intentionally NOT auto-rewritten and that the decoder owns dispatch.
    - `git diff` for this task shows ONLY a comment addition (no executable code change in this file).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "OpType_SGFP4Dequant" source/core/OpCommonUtils.cpp</automated>
  </verify>
  <done>D-12 non-interception documented at createExecutionWithExternal; no functional change to runtime-core dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model file (.mnn) | Inline `buffer` bytes cross into decoders untrusted |
| Sidecar file (.weight) | External container bytes read by decoders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | buffer-mode decode (CPU+Vulkan) | mitigate | `sgfp4_is_v2_container` magic/version gate at entry + `dequant_sgfp4_container_cpu` bounds-checked decode (returns false → INVALID_VALUE/nullptr, no partial output). |
| T-08-05 | DoS | sidecar `external()[1]` oversized allocation | mitigate | Unchanged T-01-04 (CPU) / T-03-02 (Vulkan) `queryFileSize` probe still bounds the read before allocation in the sidecar path. |
| T-08-06 | Tampering | inline buffer truncated/garbage | mitigate | Entry gate rejects < 16-byte or wrong-magic/version buffers before any decode; oracle fails closed on short/oversized decode. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase. |
</threat_model>

<verification>
- `.\build\run_test.out op/sgfp4/uniform_decode` and `op/sgfp4/mixed_decode` exit 0 (CPU sidecar path unchanged).
- `.\build\run_test.out op/sgfp4/vulkan_uniform_parity` exits 0 (pass-skip without device).
- `grep -n "param->buffer()"` returns hits in both backend files; `grep -n "OpType_SGFP4Dequant" source/core/OpCommonUtils.cpp` returns the D-12 comment.
</verification>

<success_criteria>
- Both decoders dispatch buffer-first with magic + dims-consistency entry checks; empty buffer keeps the exact sidecar path; D-12 comment present; existing sidecar suites green.
</success_criteria>

## Artifacts this phase produces

- Buffer-first dispatch branch in `CPUSGFP4Dequant::onResize` (uses `param->buffer()`, `mContainer.assign`, `sgfp4_is_v2_container`, eager `dequant_sgfp4_container_cpu` dims check).
- Buffer-first dispatch branch in `VulkanSGFP4DequantCreator::onCreate` (uses `param->buffer()`, `container.assign`, `sgfp4_is_v2_container`, shared host pre-validation).
- D-12 documentation comment at `source/core/OpCommonUtils.cpp` `createExecutionWithExternal` (no new symbol — comment only).

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-03-SUMMARY.md` when done
</output>
