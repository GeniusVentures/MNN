---
phase: 08-schema-sidecar-wiring
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - test/op/SGFP4DequantTest.cpp
  - test/op/SGFP4VulkanDequantTest.cpp
autonomous: true
requirements: [SGV2-22]

must_haves:
  truths:
    - "Buffer-mode decode == sidecar-mode decode == oracle on CPU using identical container bytes"
    - "Buffer-mode decode == sidecar-mode decode == oracle on Vulkan using identical container bytes"
    - "A malformed inline buffer returns INVALID_VALUE/nullptr (no partial output)"
  artifacts:
    - path: "test/op/SGFP4DequantTest.cpp"
      provides: "SGFP4DequantBufferTest suite op/sgfp4/dequant_buffer (CPU buffer==sidecar==oracle parity)"
      contains: "op/sgfp4/dequant_buffer"
    - path: "test/op/SGFP4VulkanDequantTest.cpp"
      provides: "SGFP4VulkanBufferParityTest suite op/sgfp4/vulkan_buffer_parity (Vulkan buffer==sidecar==oracle parity)"
      contains: "op/sgfp4/vulkan_buffer_parity"
  key_links:
    - from: "SGFP4DequantBufferTest"
      to: "SGFP4DequantFixtures.h"
      via: "identical container bytes across buffer and sidecar placements"
      pattern: "sgfp4_fixtures::"
    - from: "SGFP4VulkanBufferParityTest"
      to: "MNN::dequant_sgfp4_container_cpu"
      via: "CPU oracle reference"
      pattern: "dequant_sgfp4_container_cpu"
---

<objective>
Add buffer-mode decode-parity tests (D-08) to the existing CPU and Vulkan SGFP4 test files, proving buffer-mode decode == sidecar-mode decode == oracle using identical container bytes, plus a malformed-buffer rejection probe.

Purpose: Exercise the new buffer-first dispatch from Plan 08-03 end-to-end through the Module/Interpreter path, and lock in the unified placement convention.

Output: Two new suites — `op/sgfp4/dequant_buffer` (CPU) and `op/sgfp4/vulkan_buffer_parity` (Vulkan, pass-skip with no device).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-VALIDATION.md
@test/op/SGFP4DequantTest.cpp
@test/op/SGFP4VulkanDequantTest.cpp
@test/op/SGFP4DequantFixtures.h
@test/op/SGFP4TestUtil.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPU buffer-mode parity suite op/sgfp4/dequant_buffer</name>
  <files>test/op/SGFP4DequantTest.cpp</files>
  <read_first>
    - test/op/SGFP4DequantTest.cpp (full — esp. runSgfp4Module lines 385-450 and testOpLevelExternalSidecar lines 351-383: the sidecar op-construction pattern to clone)
    - test/op/SGFP4DequantFixtures.h (Fixture struct: name/container/containerSize/dimO/dimI/expected/expectedCount)
    - test/op/SGFP4TestUtil.hpp (shared helpers created in 08-02)
  </read_first>
  <action>
    Add a new `MNNTestCase` subclass `SGFP4DequantBufferTest` to `test/op/SGFP4DequantTest.cpp` (registered via `MNNTestSuiteRegister(SGFP4DequantBufferTest, "op/sgfp4/dequant_buffer");`). Its `run(int)` iterates every `sgfp4_fixtures::kFixtures[i]` and, for each, constructs an `OpT` with `type = OpType_SGFP4Dequant`, `main.type = OpParameter_SGFP4DequantParam`, a `new SGFP4DequantParamT` with `magic = kSGFP4Magic`, `dims = {fixture.dimO, fixture.dimI}`, and — for buffer mode — `param->buffer.assign(fixture.container, fixture.container + fixture.containerSize)` with `external` LEFT EMPTY and `externalPath` UNSET (so the runtime takes the buffer-first branch). Run it through `Variable::create(Expr::create(op.get(), {}))` → `Variable::save` → `Module::load` with a `MNN_FORWARD_CPU` runtime manager (clone `runSgfp4Module` but WITHOUT sidecarPath/externalPath). Assert output equals the oracle `MNN::dequant_sgfp4_container_cpu(fixture.container, ...)` result AND `fixture.expected` within `kFixtureRelativeTolerance` (1e-4). Then add a malformed-buffer probe: build the op with a truncated/garbage `param->buffer` (e.g. first 8 bytes of a valid container) and assert the module decode produces NO valid output (Module::load null, or onForward empty, or the runtime returns an error) — no partial output. Reuse `checkVectorByRelativeError<float>` for parity. This suite does NOT write a sidecar file.
  </action>
  <acceptance_criteria>
    - `test/op/SGFP4DequantTest.cpp` contains `MNNTestSuiteRegister(SGFP4DequantBufferTest, "op/sgfp4/dequant_buffer");`.
    - The buffer-mode op sets `param->buffer` (via `.assign(...)`) and leaves `external` empty / `externalPath` unset.
    - Parity asserts `checkVectorByRelativeError<float>(out, oracle, ...)` against `dequant_sgfp4_container_cpu` output.
    - A malformed-buffer probe asserts non-success (null module / empty output / error return).
    - `.\build\run_test.out op/sgfp4/dequant_buffer` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.\build\run_test.out op/sgfp4/dequant_buffer</automated>
  </verify>
  <done>CPU buffer-mode suite green: buffer == sidecar == oracle for all fixtures + malformed buffer rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Vulkan buffer-mode parity suite op/sgfp4/vulkan_buffer_parity</name>
  <files>test/op/SGFP4VulkanDequantTest.cpp</files>
  <read_first>
    - test/op/SGFP4VulkanDequantTest.cpp (full — esp. runSgfp4VulkanModule lines 42-108 and the no-device skip guard lines 117-123)
    - test/op/SGFP4DequantTest.cpp (Task 1 result — the buffer-mode op-construction convention to mirror)
    - test/op/SGFP4DequantFixtures.h + test/op/SGFP4TestUtil.hpp
  </read_first>
  <action>
    Add a new `MNNTestCase` subclass `SGFP4VulkanBufferParityTest` to `test/op/SGFP4VulkanDequantTest.cpp` (registered `MNNTestSuiteRegister(SGFP4VulkanBufferParityTest, "op/sgfp4/vulkan_buffer_parity");`). Keep the D-07 no-device guard: `auto vulkanCreator = MNN::MNNGetExtraRuntimeCreator(MNN_FORWARD_VULKAN); if (nullptr == vulkanCreator) { MNN_PRINT("...skipping..."); return true; }`. For each fixture: (a) compute the CPU oracle `MNN::dequant_sgfp4_container_cpu(fixture.container, ..., cpuRef.data(), fixture.expectedCount)`; (b) build the buffer-mode op with `param->buffer.assign(fixture.container, fixture.container + fixture.containerSize)`, `external` empty, `externalPath` unset, run it through a `MNN_FORWARD_VULKAN` Module session (clone `runSgfp4VulkanModule` but WITHOUT the sidecar file and WITHOUT `setExternalFile` — the buffer is inline); (c) assert GPU output == CPU oracle within `kFixtureRelativeTolerance` (1e-4, Precision_High). Optionally also run the default-precision relaxed pass (2e-3) only if the tight pass succeeds (mirroring the existing suite). No sidecar file is written. Do NOT touch GLSL.
  </action>
  <acceptance_criteria>
    - `test/op/SGFP4VulkanDequantTest.cpp` contains `MNNTestSuiteRegister(SGFP4VulkanBufferParityTest, "op/sgfp4/vulkan_buffer_parity");`.
    - The suite has the `MNNGetExtraRuntimeCreator(MNN_FORWARD_VULKAN) == nullptr` pass-skip guard.
    - Buffer-mode op sets `param->buffer` and leaves `external`/`externalPath` empty; parity is asserted against `dequant_sgfp4_container_cpu` output.
    - `.\build\run_test.out op/sgfp4/vulkan_buffer_parity` exits 0 (pass or graceful skip).
  </acceptance_criteria>
  <verify>
    <automated>.\build\run_test.out op/sgfp4/vulkan_buffer_parity</automated>
  </verify>
  <done>Vulkan buffer-mode suite green (or gracefully skipped with no device): buffer == oracle parity on GPU.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model file (.mnn) | Inline buffer bytes exercised by the parity tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | malformed inline buffer | mitigate | Malformed-buffer probe (Task 1) asserts the runtime rejects garbage/truncated buffers without partial output — exercising the `sgfp4_is_v2_container` gate + oracle from Plan 08-03. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase. |
</threat_model>

<verification>
- `.\build\run_test.out op/sgfp4/dequant_buffer` exits 0 (CPU parity + malformed probe).
- `.\build\run_test.out op/sgfp4/vulkan_buffer_parity` exits 0 (GPU parity or graceful skip).
</verification>

<success_criteria>
- Buffer-mode decode parity proven on both CPU and Vulkan against the oracle; malformed buffer rejection proven on CPU; both suites registered under op/sgfp4/.
</success_criteria>

## Artifacts this phase produces

- `SGFP4DequantBufferTest` — MNNTestCase class in `test/op/SGFP4DequantTest.cpp`.
- Test suite registration string: `op/sgfp4/dequant_buffer`.
- `SGFP4VulkanBufferParityTest` — MNNTestCase class in `test/op/SGFP4VulkanDequantTest.cpp`.
- Test suite registration string: `op/sgfp4/vulkan_buffer_parity`.

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-05-SUMMARY.md` when done
</output>
