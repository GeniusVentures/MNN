---
phase: 08-schema-sidecar-wiring
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-04]
files_modified:
  - tools/converter/source/TestSGFP4Converter.cpp
  - tools/converter/CMakeLists.txt
autonomous: true
requirements: [SGV2-23]

must_haves:
  truths:
    - "A converter-path test drives saveExternalData/RemoveAndStoreParam on a synthetic NetT with populated SGFP4 buffers"
    - "Emitted sidecar layout is 16-byte aligned, monotonic, and non-overlapping; external == {offset, true-size}; buffer cleared in the serialized op"
    - "Reload+decode parity: the externalized sidecar region decodes identically to the oracle"
  artifacts:
    - path: "tools/converter/source/TestSGFP4Converter.cpp"
      provides: "converter round-trip test executable (main) asserting layout + reload parity"
      contains: "saveExternalData"
    - path: "tools/converter/CMakeLists.txt"
      provides: "TestSGFP4Converter executable target linking MNNConvertDeps + MNN_DEPS"
      contains: "TestSGFP4Converter"
  key_links:
    - from: "tools/converter/source/TestSGFP4Converter.cpp"
      to: "saveExternalData"
      via: "#include \"common/CommonUtils.hpp\""
      pattern: "saveExternalData"
    - from: "tools/converter/source/TestSGFP4Converter.cpp"
      to: "MNN::dequant_sgfp4_container_cpu"
      via: "reload parity oracle"
      pattern: "dequant_sgfp4_container_cpu"
---

<objective>
Build the D-09 converter round-trip test and wire it into the converter build. This resolves RESEARCH Q1 (converter-test placement): a new `TestSGFP4Converter` executable under `tools/converter/source/` linking `MNNConvertDeps` + `MNN_DEPS` (static build), because `RemoveAndStoreParam`/`saveExternalData` live in `MNNConvertDeps` (built only under MNN_BUILD_CONVERTER=ON) and `run_test.out` links only `MNN_DEPS` — the `TestPassManager`/`TestConvertResult` precedents are gated behind MNN_BUILD_SHARED_LIBS=ON and are NOT built in this static workspace.

Purpose: Prove the aligned storeWeight SGFP4 case (08-04) emits a 16-byte-aligned, monotonic, non-overlapping sidecar with `external == {offset, true-size}`, buffer cleared, and that a reloaded op decodes identically to the oracle.

Output: `tools/converter/source/TestSGFP4Converter.cpp` + CMake target; `.\build\TestSGFP4Converter` exits 0.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-PATTERNS.md
@tools/converter/source/common/RemoveParams.cpp
@tools/converter/source/common/CommonUtils.hpp
@tools/converter/CMakeLists.txt
@test/op/SGFP4TestUtil.hpp
@include/MNN/SGFP4DequantUtils.hpp
@tools/fp4/sgfp4_inject_core.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write TestSGFP4Converter.cpp (layout + reload parity assertions)</name>
  <files>tools/converter/source/TestSGFP4Converter.cpp</files>
  <read_first>
    - tools/converter/source/common/CommonUtils.hpp (declarations: `void RemoveAndStoreParam(std::unique_ptr<MNN::OpT>&, std::ofstream*, int64_t&)` and `bool saveExternalData(std::unique_ptr<MNN::NetT>&, const std::string&)`)
    - tools/converter/source/common/RemoveParams.cpp (the storeWeight/RemoveAndStoreParam/saveExternalData behavior being verified)
    - test/op/SGFP4TestUtil.hpp (buildContainerUniform64, readBytes, fileExists)
    - include/MNN/SGFP4DequantUtils.hpp (sgfp4_align16, dequant_sgfp4_container_cpu, kSGFP4Magic)
    - test/op/SGFP4ClassicAPITest.cpp (lines 442-508: the Interpreter::createFromFile→createSession→runSession reload pattern to reuse)
    - 08-RESEARCH.md §"Open Questions" Q1 + §"Pitfall 2" and §"Pitfall 3" (layout assertions + converter-linking rationale)
  </read_first>
  <action>
    Create `tools/converter/source/TestSGFP4Converter.cpp` with a `main(int argc, const char* argv[])` returning 0 on success, non-zero on any failed assertion. Includes: `"common/CommonUtils.hpp"` (relative — the file sits in `source/`, next to `common/`), `"MNN_generated.h"`, `"MNN/SGFP4DequantUtils.hpp"`, `"MNN/Interpreter.hpp"`, `"SGFP4TestUtil.hpp"` (via the include dir added in Task 2). PHASE A (layout): build a `NetT` with TWO SGFP4 ops and ONE trailing Convolution2D, all with empty `externalPath` (so `RemoveAndStoreParam` processes them). SGFP4 op 0: `op->type = OpType_SGFP4Dequant`, `op->main.type = OpParameter_SGFP4DequantParam`, `param = new SGFP4DequantParamT` with `param->magic = kSGFP4Magic`, `param->dims = {64,64}`, `param->buffer` = `sgfp4_test::buildContainerUniform64(64, 64, bytes)` result. SGFP4 op 1: same shape `{64,128}` (different byte size). Convolution2D: `op->main.type = OpParameter_Convolution2D`, `inputIndexes = {0}` (size 1 so the Convolution2D case runs), `param->weight` = a 16-element float vector, `param->common` present. Call `saveExternalData(netT, sidecarPath)` (sidecarPath = a `tempPath("sgfp4_conv_", ".weight")` path). Assert: op0 `external == {0, trueSize0}`; op1 `external == {sgfp4_align16(trueSize0), trueSize1}`; Convolution2D `external[0] == sgfp4_align16(trueSize0) + sgfp4_align16(trueSize1)` (monotonic, non-overlapping); both SGFP4 `param->buffer.empty()`; sidecar file size (`readBytes`) == `sgfp4_align16(trueSize0) + sgfp4_align16(trueSize1) + 16*sizeof(float)` (the Convolution2D weight is not padded). PHASE B (reload+decode parity): build a fresh single-op `NetT` (one SGFP4 op, `dims={64,64}`, `buffer` populated, `tensorName={"output"}`, `outputName={"output"}`, `tensorNumber=1`, `op->outputIndexes={0}`), call `saveExternalData`, then set `op->externalPath = sidecarPath` (literal per-op path the runtime reads — D-12 non-interception means this is the ONLY way SGFP4 resolves the sidecar), serialize via `flatbuffers::FlatBufferBuilder` + `MNN::Net::Pack(builder, netT.get())` + `builder.Finish(...)`, write `builder.GetBufferPointer()/GetSize()` to a temp `.mnn` file, reload via `Interpreter::createFromFile(mnnPath)` + `createSession` + `resizeSession` + `runSession`, read the output tensor's host float data, and assert `checkVectorByRelativeError`-style parity against `MNN::dequant_sgfp4_container_cpu(bytes.data(), bytes.size(), oracle.data(), 64*64)`. Clean up temp files before returning. No fenced code blocks — implement from the named identifiers above.
  </action>
  <acceptance_criteria>
    - `tools/converter/source/TestSGFP4Converter.cpp` exists and calls `saveExternalData` (via `common/CommonUtils.hpp`) and `MNN::dequant_sgfp4_container_cpu`.
    - Phase A asserts: op0 offset == 0, op1 offset == `sgfp4_align16(trueSize0)`, Convolution2D offset == sum of the two aligned SGFP4 sizes; `param->buffer.empty()` for both SGFP4 ops; sidecar length matches the expected aligned total.
    - Phase B reloads a serialized `.mnn` via `Interpreter::createFromFile` and asserts decode parity with the oracle.
    - `main` returns 0 only when all assertions pass; any `MNN_ERROR` is paired with a non-zero exit.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build .build --target TestSGFP4Converter</automated>
  </verify>
  <done>TestSGFP4Converter.cpp compiles and encodes the D-09 layout + reload assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TestSGFP4Converter target into tools/converter/CMakeLists.txt and run it</name>
  <files>tools/converter/CMakeLists.txt</files>
  <read_first>
    - tools/converter/CMakeLists.txt (full — the MNNConvert static-branch linking block lines 40-66 is the template; the ELSE shared-branch block lines 67-82)
    - 08-RESEARCH.md §"Pitfall 3" (why run_test.out cannot link MNNConvertDeps)
  </read_first>
  <action>
    In `tools/converter/CMakeLists.txt`, inside the `IF(NOT MNN_BUILD_SHARED_LIBS)` block (after the existing MNNConvert linking), add `add_executable(TestSGFP4Converter ${CMAKE_CURRENT_LIST_DIR}/source/TestSGFP4Converter.cpp)` plus `target_include_directories(TestSGFP4Converter PRIVATE ${CMAKE_CURRENT_LIST_DIR}/../../test/op/)` (so `#include "SGFP4TestUtil.hpp"` resolves), and link it by MIRRORING the MNNConvert static-branch link block exactly — `target_link_libraries(TestSGFP4Converter MNNConvertDeps)` then, for MSVC: `target_link_options(TestSGFP4Converter PRIVATE /WHOLEARCHIVE:$<TARGET_FILE:MNNConvertDeps>)` + a `foreach (DEPEND ${MNN_DEPS})` loop linking `${DEPEND}` with `/WHOLEARCHIVE:$<TARGET_FILE:${DEPEND}>`; for GNU/Clang: `target_link_libraries(TestSGFP4Converter -Wl,--whole-archive MNNConvertDeps ${MNN_DEPS} -lpthread -Wl,--no-whole-archive)`; for Apple: `-Wl,-all_load MNNConvertDeps ${MNN_DEPS}`. Also add the mirrored target in the `ELSE()` shared-libs block: `add_executable(TestSGFP4Converter ...)` + `target_include_directories(... ../../test/op/)` + `target_link_libraries(TestSGFP4Converter MNNConvertDeps)` (MNNConvertDeps already links `${MNN_DEPS}` PUBLIC there). Re-run cmake configure + build the target, then execute `.\build\TestSGFP4Converter` (the converter CMakeLists sets `CMAKE_RUNTIME_OUTPUT_DIRECTORY` to the build root).
  </action>
  <acceptance_criteria>
    - `tools/converter/CMakeLists.txt` contains `add_executable(TestSGFP4Converter ...)` in BOTH the static and shared branches, and `target_include_directories(TestSGFP4Converter PRIVATE .../test/op/)`.
    - `cmake --build .build --target TestSGFP4Converter` completes with zero errors (no `undefined reference to RemoveAndStoreParam/saveExternalData`).
    - `.\build\TestSGFP4Converter` exits 0 (all layout + reload assertions pass).
    - `.\build\run_test.out op/sgfp4/` still exits 0 (the converter test does not pollute run_test.out).
  </acceptance_criteria>
  <verify>
    <automated>.\build\TestSGFP4Converter</automated>
  </verify>
  <done>TestSGFP4Converter target builds and links MNNConvertDeps + MNN_DEPS; the round-trip test exits 0; run_test.out unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model file (.mnn) / op params | Synthetic NetT exercises the converter externalization path |
| Sidecar file (.weight) | Shared external weight byte stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | sidecar offset overlap across mixed op types | mitigate | Phase A asserts op1 offset == sgfp4_align16(trueSize0) and Convolution2D offset == sum of aligned SGFP4 sizes (monotonic, non-overlapping) — the Pitfall 2 mixed-type guard. |
| T-08-11 | Tampering | external == {offset, true-size} + buffer cleared | mitigate | Phase A asserts `external == {offset, true-size}` and `param->buffer.empty()`; Phase B proves the externalized region reloads and decodes identically to the oracle. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase. |
</threat_model>

<verification>
- `cmake --build .build --target TestSGFP4Converter` succeeds.
- `.\build\TestSGFP4Converter` exits 0.
- `.\build\run_test.out op/sgfp4/` exits 0 (no regression).
</verification>

<success_criteria>
- Converter round-trip test linked and passing: 16-byte aligned/monotonic/non-overlapping sidecar, external == {offset, true-size}, buffer cleared, reload+decode parity.
</success_criteria>

## Artifacts this phase produces

- `tools/converter/source/TestSGFP4Converter.cpp` — new converter-side test executable source (`main`).
- CMake executable target `TestSGFP4Converter` (registered in `tools/converter/CMakeLists.txt`, both static and shared branches).
- Build output binary `TestSGFP4Converter` (lands in the build root, e.g. `.build/TestSGFP4Converter`).

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/08-schema-sidecar-wiring/08-06-SUMMARY.md` when done
</output>
