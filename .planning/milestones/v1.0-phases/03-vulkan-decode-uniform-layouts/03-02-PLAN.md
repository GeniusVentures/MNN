---
phase: 03-vulkan-decode-uniform-layouts
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - source/backend/vulkan/buffer/execution/glsl/sgfp4_dequant.comp
  - source/backend/vulkan/buffer/execution/glsl/macro.json
  - source/backend/vulkan/buffer/compiler/AllShader.cpp
  - source/backend/vulkan/buffer/shaders/AllShader.h
  - source/backend/vulkan/buffer/compiler/VulkanShaderMap.cpp
autonomous: true
requirements: [SGV2-12]

must_haves:
  truths:
    - "A GLSL compute shader decodes uniform-layout SGFP4 v2 containers (both code modes, all 5 uniform layouts) via shift-mask-FMA affine reconstruction"
    - "The shader is embedded through the makeshader.py pipeline with regenerated AllShader.cpp / AllShader.h / VulkanShaderMap.cpp committed — never hand-edited"
    - "Both FP32 and FP16 output variants exist as shader keys glsl_sgfp4_dequant_comp and glsl_sgfp4_dequant_FP16_comp"
  artifacts:
    - path: "source/backend/vulkan/buffer/execution/glsl/sgfp4_dequant.comp"
      provides: "Uniform-layout SGFP4 v2 decode (framing re-walk + leaf-header unpack + dual-mode affine decode), thread-per-weight per D-03"
      min_lines: 90
    - path: "source/backend/vulkan/buffer/compiler/AllShader.cpp"
      provides: "Regenerated SPIR-V embedding"
      contains: "sgfp4_dequant"
    - path: "source/backend/vulkan/buffer/shaders/AllShader.h"
      provides: "Regenerated extern declarations"
      contains: "glsl_sgfp4_dequant"
    - path: "source/backend/vulkan/buffer/compiler/VulkanShaderMap.cpp"
      provides: "Regenerated shader-map entries"
      contains: "sgfp4_dequant"
  key_links:
    - from: "sgfp4_dequant.comp"
      to: "SGFP4DequantUtils.hpp constants"
      via: "1:1 constant port (kSGFP4RecordCountOffset=5, kSGFP4Alignment=16, kSGFP4LayoutEnumMask=0x7, kSGFP4LeafHeaderBiasMask=0xFFF0, ...)"
      pattern: "read_u32_le|unpackHalf2x16"
    - from: "macro.json"
      to: "glsl_sgfp4_dequant_FP16_comp"
      via: "makeshader.py genShaderFileObjs useFP16 variant generation"
      pattern: "sgfp4_dequant.comp"
---

<objective>
Write the uniform-layout SGFP4 v2 GLSL decode shader (`sgfp4_dequant.comp`) porting `dequant_sgfp4_container_cpu()`'s uniform path, register its FP16 variant in `macro.json`, and regenerate + commit the embedded shader artifacts via `makeshader.py` (per D-03, D-04, D-06 and locked roadmap note 5).

Purpose: SGV2-12 — the GPU half of the decode port. All framing constants and decode math are normative from `include/MNN/SGFP4DequantUtils.hpp` (Phase-1/2-tested).
Output: One new `.comp` shader, a one-entry `macro.json` edit, and three regenerated committed artifacts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md
@.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-PATTERNS.md
@include/MNN/SGFP4DequantUtils.hpp
@source/backend/vulkan/buffer/execution/glsl/fp4_dequant.comp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author sgfp4_dequant.comp (uniform-layout GLSL decode)</name>
  <files>source/backend/vulkan/buffer/execution/glsl/sgfp4_dequant.comp</files>
  <read_first>
    - include/MNN/SGFP4DequantUtils.hpp (normative constants: kSGFP4* block at lines 22-100; the uniform record walk in dequant_sgfp4_container_cpu; sgfp4_read_u32_le)
    - source/backend/vulkan/buffer/execution/glsl/fp4_dequant.comp (GLSL conventions: no #version, FLOAT macro from makeshader.py, local_size_x=256, binding layout, early-return bounds guard, header comment discipline)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md ("Code Examples" — the GLSL skeleton derived from the verified constants; note its `kMagic` literal example is WRONG: use the self-documenting assembly form below)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-PATTERNS.md (sgfp4_dequant.comp section)
  </read_first>
  <action>
    Create `sgfp4_dequant.comp` following the `fp4_dequant.comp` conventions exactly:
    <!-- planner-discipline-allow: #version -->
    - Header comment block stating: SGFP4 v2 uniform-layout dequant; FLOAT macro provided by makeshader.py; do NOT redefine FLOAT or add a version directive line (makeshader.py prepends the GLSL version header itself).
    - Bindings: binding 0 `readonly buffer ContainerBuffer { uint Container[]; }` (raw container bytes as LE u32 words); binding 1 `writeonly buffer DstBuffer { FLOAT Dst[]; }`; binding 2 `uniform ConstBuffer { uint outElementCount; uint containerBytes; }`.
    - `layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;` and dispatch divisor constant named (e.g. `const uint kWorkgroupSize = 256u;` is host-side; in-shader just document local_size 256 — no magic numbers elsewhere).
    - Named framing constants ported 1:1 from SGFP4DequantUtils.hpp (no magic numbers, per CLAUDE.md): kMagic assembled as `('S') | ('G' << 8) | ('F' << 16) | ('4' << 24)` = 0x34464753u (do NOT copy RESEARCH.md's example literal 0x46464753u — it is a typo); kVersion=2u; kVersionByteOffset=4u; kRecordCountOffset=5u; kOffsetTableStart=16u; kAlign16=16u; kLayoutEnumMask=0x7u; kLeafBiasMask=0xFFF0u; kLeafModeBit=0x1u; kNibblesPerWord=8u; kSymbolsPerWord=16u.
    - `read_u32_le(uint byteAddr)` helper: compose from at most two aligned words ((lo >> (8*off)) | (hi << (32 - 8*off)); fast path off==0). MANDATORY for ALL framing reads — record count B sits at byte 5, straddling words (Pitfall 1). Use it for sb_header, leaf headers, and payload words uniformly.
    - `unpackLeafHeader(uint h, out float S, out float bias, out uint mode)`: S = unpackHalf2x16(h).y (high 16 bits); bias = unpackHalf2x16(h & kLeafBiasMask).x; mode = h & kLeafModeBit. Never hand-roll FP16 bit math (Don't-Hand-Roll table).
    - `codeMode0(uint nib)` → float((nib ^ 0x8u) - 0x8u) (4-bit two's-complement sign-extend, c ∈ [-8,7]); `codeMode1(uint sym)` → 1u==sy? +1.0 : 2u==sym? -1.0 : 0.0 (T158 ternary 00→0, 01→+1, 10→−1, reserved 11→0).
    - `locateElement(...)` per-thread framing re-walk (D-04): read B via read_u32_le(kRecordCountOffset) (unaligned); offsetTableEnd = kOffsetTableStart + 4*B; regionStart = align16(offsetTableEnd); loop records b < B: recStart = regionStart + read_u32_le(kOffsetTableStart + 4*b); layout = read_u32_le(recStart) & kLayoutEnumMask; resolve Table-3 uniform geometry: 0→{N=1,n=64}, 1→{N=4,n=32}, 2→{N=16,n=16}, 3→{N=64,n=8}, 5→{N=256,n=4}; other enums (4=MIXED, ≥6) return false — host pre-validation (D-05) guarantees they never reach the shader. When idx falls in this record: leaf = local/(n*n); walk payload cursor sequentially to that leaf (per-leaf payload byte size = align16(4 * (mode0 ? n²/8 : n²/16) words)); read the leaf header; compute payloadWordByte + intra-word index per mode (mode0: 4*(inLeaf/8), inLeaf%8; mode1: 4*(inLeaf/16), inLeaf%16); return S/bias/mode.
    - `main()`: only guard is `if (gl_GlobalInvocationID.x >= outElementCount) return;` (D-05 — host pre-validated, no in-shader bounds checking); final write `Dst[idx] = FLOAT(S) * FLOAT(c) + FLOAT(bias);` (affine reconstruct, floats in FP32 math; the FLOAT cast only at the store).
    - Validate the magic+version once in main before the walk? NO — leave even that to the host (D-05); the shader body stays a pure locate+decode (keeps Phase 4's quadtree extension a drop-in replacement of locateElement).
  </action>
  <verify>
    <automated>wsl -e sh -c 'cd /mnt/w/gnus/GeniusNetwork/thirdparty/MNN/source/backend/vulkan/buffer/compiler && glslangValidator -S comp --target-env vulkan1.1 -V /mnt/w/gnus/GeniusNetwork/thirdparty/MNN/source/backend/vulkan/buffer/execution/glsl/sgfp4_dequant.comp -o /tmp/sgfp4.spv' (add a temporary `#version 450` + `#define FLOAT float` prelude in a temp copy — the real file must NOT contain them; makeshader.py prepends headers)</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: #version -->
    - File exists with no GLSL version directive line and no FLOAT/FLOAT2/FLOAT4 definition (grep: `grep -c "#version" sgfp4_dequant.comp` == 0)
    - Typos compile: a temp prelude copy (`#version 450` / `#define FLOAT float` prepended) passes glslangValidator with no errors
    - `read_u32_le` is used for every container read (grep for direct `Container[` indexing outside the helper body returns only the helper's two reads)
    - Constants match SGFP4DequantUtils.hpp exactly: kRecordCountOffset=5, kOffsetTableStart=16, kAlign16=16, kLayoutEnumMask=0x7, kLeafBiasMask=0xFFF0, kLeafModeBit=0x1, kNibblesPerWord=8, kSymbolsPerWord=16; magic uses the assembled 'S'|'G'<<8|'F'<<16|'4'<<24 form
    - All five uniform layout enums (0,1,2,3,5) are handled; enum 4 and ≥6 return false from locateElement
    - Workgroup size is the named/document 256 matching the host UP_DIV dispatch in plan 03-03
  </acceptance_criteria>
  <done>Shader compiles standalone (with temp prelude), ports the normative constants 1:1, and is stateless thread-per-weight per D-03/D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Register the FP16 variant and regenerate + verify embedded shader artifacts</name>
  <files>
    source/backend/vulkan/buffer/execution/glsl/macro.json,
    source/backend/vulkan/buffer/compiler/AllShader.cpp,
    source/backend/vulkan/buffer/shaders/AllShader.h,
    source/backend/vulkan/buffer/compiler/VulkanShaderMap.cpp
  </files>
  <read_first>
    - source/backend/vulkan/buffer/execution/glsl/macro.json (the blitregion.comp entry — the minimal useFP16 form to mirror)
    - source/backend/vulkan/buffer/compiler/makeshader.py (genShaderFileObjs ~lines 489-512 — useFP16 variant generation; findAllShader POSIC find dependency)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-RESEARCH.md (Pitfall 3 — regeneration environment; Toolchain commands section)
    - source/backend/vulkan/buffer/compiler/AllShader.cpp (top only — confirm the Auto-Generated header; never hand-edit)
  </read_first>
  <action>
    1. Add to `glsl/macro.json` an entry exactly mirroring blitregion.comp's minimal form: key "sgfp4_dequant.comp" with "useFP16": true. This is MANDATORY for D-06 — makeshader.py generates the `_FP16_comp` variant only when this entry exists.
    2. Regenerate from the POSIX environment provisioned in plan 03-01 (WSL or Git Bash — PowerShell resolves `find` to find.exe and silently breaks the file list): `cd source/backend/vulkan/buffer/compiler && python3 makeshader.py`. The `.cache/` dir is absent, so all ~70 shaders recompile once — expected and harmless (deterministic xxd output keeps the diff limited to the new shader).
    3. Verify the embedding greps (milestone 02-01 verification pattern): `grep -c 'sgfp4_dequant' AllShader.cpp` ≥ 4 (array + length symbol, FP32 + FP16); `grep -c 'sgfp4_dequant' ../shaders/AllShader.h` == 4 (extern declarations); `grep -c 'sgfp4_dequant' VulkanShaderMap.cpp` == 2 (map insertions). Inspect `git diff` on the three generated files: the sgfp4 additions must be the only new content (other shaders' byte arrays unchanged).
    4. Commit the shader + macro.json + the three regenerated artifacts together (one atomic commit — the CLAUDE.md makeshader.py contract).
  </action>
  <verify>
    <automated>grep counts above (≥4 / ==4 / ==2) via wsl or findstr; `git diff --stat` shows exactly 5 files (sgfp4_dequant.comp, macro.json, AllShader.cpp, AllShader.h, VulkanShaderMap.cpp)</automated>
  </verify>
  <acceptance_criteria>
    - macro.json contains a `"sgfp4_dequant.comp"` entry with `"useFP16": true`
    - `glsl_sgfp4_dequant_comp` and `glsl_sgfp4_dequant_FP16_comp` both resolve in the regenerated VulkanShaderMap (grep == 2 insertions; AllShader.h == 4 declarations)
    - The three artifacts' diffs contain no changes to other shaders' embedded arrays
    - No hand-edit markers in the generated files (their `/*Auto Generated File, Don't Modified.*/` headers intact)
  </acceptance_criteria>
  <done>SGV2-12 complete: both shader variants embedded through the locked makeshader.py pipeline with regenerated artifacts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-validated container SSBO → shader | The shader trusts it fully (D-05); the only guard is the elementCount early-return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/DoS | OOB shader reads from malformed geometry | mitigate | Host pre-validation (D-05, plan 03-03) rejects malformed containers before upload/dispatch — shader never sees unvalidated bytes; `idx >= outElementCount` early-return is the sole in-shader guard |
| T-03-01b | Tampering | generated-file hand edits | mitigate | Verify greps + artifact-header intact checks above; regenerate-only workflow |
</threat_model>

<verification>
- Shader compiles under glslangValidator (temp-prelude copy)
- Regeneration greps: AllShader.cpp ≥4, AllShader.h ==4, VulkanShaderMap.cpp ==2
- `cmake --build .build --target run_test.out` still compiles (the new symbols link into the shader map)
</verification>

<success_criteria>
SGV2-12 fully delivered: GLSL uniform decode (both modes, all 5 layouts) embedded via makeshader.py with regenerated artifacts committed, FP16+FP32 variants present.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Symbols / Names |
|----------|------|-----------------|
| `source/backend/vulkan/buffer/execution/glsl/sgfp4_dequant.comp` | new GLSL compute shader | `read_u32_le`, `unpackLeafHeader`, `codeMode0`, `codeMode1`, `locateElement` (shader-internal; no external linkage) |
| `glsl_sgfp4_dequant_comp` | embedded shader key (FP32 variant) | resolvable via `VulkanBackend::getPipeline` |
| `glsl_sgfp4_dequant_FP16_comp` | embedded shader key (FP16 variant) | resolvable via `VulkanBackend::getPipeline` |
| regenerated `AllShader.cpp` / `shaders/AllShader.h` / `VulkanShaderMap.cpp` | generated artifacts | byte arrays + extern declarations + map entries for both variants |

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-02-SUMMARY.md` when done
</output>
