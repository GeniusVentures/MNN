---
phase: 04-vulkan-decode-adaptive-quadtree-layout-mixed
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/op/SGFP4VulkanDequantTest.cpp
autonomous: true
requirements: [SGV2-16, SGV2-15]

must_haves:
  truths:
    - "All 14 committed fixtures (test/op/SGFP4DequantFixtures.h) decode identically through dequant_sgfp4_container_cpu and a real Vulkan module session within float tolerance, mixed_asymmetric included, with zero skipped fixtures"
    - "The parity test degrades gracefully (skip + message, suite still passes) when no Vulkan device is available"
    - "op/fp4 and op/vulkan/fp4_dequant_correctness (E2M1) remain green — SGFP4 v2 stays additive, not a replacement"
    - "The phase's complete working-tree diff contains exactly the 5 intended files across both plans (sgfp4_dequant.comp, AllShader.cpp, AllShader.h, VulkanShaderMap.cpp, SGFP4VulkanDequantTest.cpp)"
  artifacts:
    - path: "test/op/SGFP4VulkanDequantTest.cpp"
      provides: "Full 14-fixture CPU/Vulkan parity sweep (SGV2-16), no LAYOUT_MIXED skip"
      min_lines: 190
  key_links:
    - from: "SGFP4VulkanDequantTest fixture loop"
      to: "sgfp4_fixtures::kFixtures (SGFP4DequantFixtures.h)"
      via: "unconditional iteration over all kFixtureCount entries (skip removed)"
      pattern: "kFixtureCount|mixed_asymmetric"
    - from: "SGFP4VulkanDequantTest"
      to: "dequant_sgfp4_container_cpu"
      via: "CPU reference decode at test time (oracle, unchanged)"
      pattern: "dequant_sgfp4_container_cpu"
---

<objective>
Make the LAYOUT_MIXED coverage official: remove the one-fixture skip in the existing dual-backend parity test so all 14 committed fixtures (uniform + LAYOUT_MIXED) run through both `dequant_sgfp4_container_cpu` and a real Vulkan GPU dispatch, closing SGV2-16 and the phase's Success Criteria 1-3 in one sweep, per CONTEXT.md D-07/D-08/D-09.

Purpose: Confirms the Plan 04-01 shader edit is correct end-to-end (through the production external-sidecar plumbing, not just a standalone compile) and proves complete CPU/Vulkan feature parity for the full SGFP4 v2 spec.
Output: An updated `SGFP4VulkanDequantTest.cpp` (no skip) and a green phase-gate regression sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-CONTEXT.md
@.planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-VALIDATION.md
@.planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-01-SUMMARY.md
@test/op/SGFP4VulkanDequantTest.cpp
@test/op/SGFP4DequantFixtures.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the LAYOUT_MIXED skip + update messaging (SGV2-16)</name>
  <files>test/op/SGFP4VulkanDequantTest.cpp</files>
  <read_first>
    - test/op/SGFP4VulkanDequantTest.cpp (current full file; fixture loop with the skip block at lines 122-136; `MNN_PRINT` summary + `MNNTestSuiteRegister` at lines 191-198)
    - test/op/SGFP4DequantFixtures.h (`Fixture` struct at lines 13-23; `kFixtures[]` array at lines 123-138 — confirms 14 total fixtures; `mixed_asymmetric` at line 137 is the sole `layout == 4` entry; `kFixtureCount` at line 139)
    - .planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-CONTEXT.md (D-07 one-line-deletion framing, D-08 tolerance/skip convention, D-09 why no separate GPU structural traversal test is needed)
    - .planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-RESEARCH.md (Architecture Patterns → Pattern 4 "D-07 sweep is a one-line deletion + rename, not a rewrite")
    - .planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-01-SUMMARY.md (exact helper name/constants Plan 04-01 landed with, and any deviations from this plan's assumptions about the shader's behavior on `mixed_asymmetric`)
  </read_first>
  <action>
    <!-- planner-discipline-allow: kSGFP4LayoutMixed -->
    Delete the `if (fixture.layout == MNN::kSGFP4LayoutMixed) { continue; }` block and its preceding "Uniform-only filter..." explanatory comment (current lines ~126-135) from the fixture loop in `run()`, so `++checked;` and the full per-fixture body (temp sidecar write, CPU reference decode + fixture-drift guard at `kFixtureRelativeTolerance`, tight `Precision_High` Vulkan pass, relaxed default-precision pass, temp-file cleanup) run unconditionally for all `sgfp4_fixtures::kFixtureCount` (14) fixtures, `mixed_asymmetric` included. Do not change any other part of the loop body.

    Update the final `MNN_PRINT` format string (current: `"SGFP4VulkanDequantTest: %d uniform fixtures matched CPU reference on Vulkan (FP32 tight + default-precision passes)\n"`) to drop the now-inaccurate "uniform" qualifier, e.g. `"SGFP4VulkanDequantTest: %d fixtures (including LAYOUT_MIXED) matched CPU reference on Vulkan (FP32 tight + default-precision passes)\n"`.

    Keep the class name `SGFP4VulkanDequantTest` and the `MNNTestSuiteRegister(SGFP4VulkanDequantTest, "op/sgfp4/vulkan_uniform_parity")` registration string UNCHANGED — Claude's Discretion per CONTEXT.md/D-07 explicitly allows reuse-in-place; keeping the existing name avoids touching unrelated docs/scripts that already reference this exact suite string (04-VALIDATION.md, ROADMAP.md quick-run references).
  </action>
  <verify>
    <automated>cmake --build .build --target run_test.out && ./.build/run_test.out.exe "op/sgfp4/vulkan_uniform_parity"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kSGFP4LayoutMixed" test/op/SGFP4VulkanDequantTest.cpp` == 0 (the skip condition and its class-qualified enum reference are fully removed — it was the only reference to this enum in the file)
    - `run_test.out.exe "op/sgfp4/vulkan_uniform_parity"` passes and its printed `checked` count == 14 (matches `sgfp4_fixtures::kFixtureCount`)
    - `mixed_asymmetric` produces no `MNN_ERROR` output (both the tight FP32 pass and the relaxed default-precision pass succeed)
    - The registration string `"op/sgfp4/vulkan_uniform_parity"` and class name `SGFP4VulkanDequantTest` are unchanged (grep confirms both still present, exactly once each)
  </acceptance_criteria>
  <done>SGV2-16 satisfied: the single full-sweep parity test exercises all 14 committed fixtures (uniform + LAYOUT_MIXED) through CPU reference vs. Vulkan GPU decode within float tolerance, with no skip remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Regression sweep + phase verification gate</name>
  <files>test/op/SGFP4VulkanDequantTest.cpp (fix-ups only if regressions surface)</files>
  <read_first>
    - .planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-VALIDATION.md (Sampling Rate, Per-Task Verification Map, Coverage Gaps)
    - .planning/workstreams/sgfp4-pivot/phases/01-affine-dual-mode-decode-core-cpu-uniform-layouts/deferred-items.md (FP4ModelTest.cpp stub-and-restore workaround)
    - .planning/workstreams/sgfp4-pivot/phases/03-vulkan-decode-uniform-layouts/03-04-SUMMARY.md (identical phase-gate precedent from Phase 3)
  </read_first>
  <action>
    Run the merge-wave/phase-gate sweep per 04-VALIDATION.md: `./.build/run_test.out.exe "op/sgfp4/"` (all 3 registered sgfp4 suites, now including the full 14-fixture Vulkan sweep) + `./.build/run_test.out.exe "op/fp4"` + `./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness"` (E2M1 additivity regression guard, unchanged since Phase 3) — all three must be green.

    Re-verify the shader-embedding artifact grep counts from Plan 04-01 are still exactly 4/4/2 (`sgfp4_dequant` in `AllShader.cpp`/`AllShader.h`/`VulkanShaderMap.cpp`) — confirms no accidental additional regeneration drift crept in from this plan's edits (which only touch the test file, not the shader).

    Attempt the full unfiltered `run_test.out.exe` (no filter) only via the Phase-1 temp-stub workaround for `test/op/FP4ModelTest.cpp` (stub its contents with a trivial neutral body, build, run the full suite, restore byte-for-byte, confirm `git diff --exit-code test/op/FP4ModelTest.cpp` is clean, never commit the stub); if this step is skipped, record that explicitly in the SUMMARY for `/gsd-verify-work` per the Phase-1/Phase-3 precedent.

    Confirm the phase's complete working-tree diff (`git status` / `git diff --stat` across both plans) contains exactly 5 intended files: `sgfp4_dequant.comp`, `AllShader.cpp`, `AllShader.h`, `VulkanShaderMap.cpp` (Plan 04-01) plus `SGFP4VulkanDequantTest.cpp` (this plan) — no E2M1 file changes, no `VulkanSGFP4Dequant.{hpp,cpp}` changes (D-06), no FlatBuffers schema changes, no generated-file hand edits beyond the makeshader.py regeneration already committed in Plan 04-01.
  </action>
  <verify>
    <automated>./.build/run_test.out.exe "op/sgfp4/" && ./.build/run_test.out.exe "op/fp4" && ./.build/run_test.out.exe "op/vulkan/fp4_dequant_correctness"</automated>
  </verify>
  <acceptance_criteria>
    - All three filtered suites report pass, with `op/sgfp4/vulkan_uniform_parity` showing `checked` == 14
    - Regenerated-artifact grep counts unchanged from Plan 04-01 (4/4/2)
    - `git status` / `git diff --stat` across the phase shows only the 5 intended files (plus planning docs)
    - `git diff --exit-code test/op/FP4ModelTest.cpp` clean if the temp-stub workaround was used
    - No changes to `VulkanSGFP4Dequant.{hpp,cpp}`, `SGFP4DequantUtils.hpp`, or any FlatBuffers schema file
  </acceptance_criteria>
  <done>Phase 4 verification gate green: SGV2-15/16 delivered, the complete SGFP4 v2 feature set (both code modes, all uniform layouts, LAYOUT_MIXED) decodes consistently on CPU and Vulkan within float tolerance, E2M1 regression guard clean, working tree contains exactly the phase's additive changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-side fixture bytes → temp sidecar | Trusted committed data; exercises the same untrusted-file posture as production (probe + validate reject paths), now covering the LAYOUT_MIXED fixture too |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | DoS | Test machine without a Vulkan device | low | accept | Graceful skip (D-07, unchanged since Phase 3): nullptr-creator check returns true with a clear `MNN_PRINT` — suite never fails from a missing device |
| T-04-05 | Tampering | Malformed/adversarial split-maps beyond host pre-validation | low | accept | Out of scope per Roadmap Note 3 and D-06 — the shader only ever receives containers that already passed `dequant_sgfp4_container_cpu`; this test only ever feeds pre-validated committed fixtures |
</threat_model>

<verification>
- `op/sgfp4/vulkan_uniform_parity` passes with `checked` == 14, `mixed_asymmetric` included
- `op/fp4` + `op/vulkan/fp4_dequant_correctness` green (E2M1 additivity)
- Artifact greps unchanged (4/4/2); working tree exactly the phase's 5 intended files
</verification>

<success_criteria>
SGV2-16 delivered; the complete SGFP4 v2 feature set (both code modes, all uniform layouts, LAYOUT_MIXED) decodes consistently on CPU and Vulkan within float tolerance via a single full-sweep test. Phase 4 (and the sgfp4-pivot v1 milestone's Vulkan track) complete.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Symbols / Names |
|----------|------|-----------------|
| `test/op/SGFP4VulkanDequantTest.cpp` (modified, no new file) | test edit | no new symbols; removes the `fixture.layout == MNN::kSGFP4LayoutMixed` skip block; updates the `MNN_PRINT` format string; class name `SGFP4VulkanDequantTest` and registration string `"op/sgfp4/vulkan_uniform_parity"` unchanged |

No new files this phase overall — Phase 4 is purely an extension of the existing shader (`locateElement`, Plan 04-01) and the existing test loop (this plan), per 04-PATTERNS.md's "no new files" framing.

<output>
Create `.planning/workstreams/sgfp4-pivot/phases/04-vulkan-decode-adaptive-quadtree-layout-mixed/04-02-SUMMARY.md` when done
</output>
