---
phase: 01-vulkan-attention-correctness-llm-e2e
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - test/op/VulkanAttentionTest.cpp
  - test/op/VulkanLinearAttentionTest.cpp
autonomous: true
requirements: [VULK-01, VULK-02, VULK-03, VULK-04, VULK-05]
user_setup: []

must_haves:
  truths:
    - 'Vulkan Attention op produces output matching CPU reference for GQA, MHA, and MQA configurations'
    - 'Vulkan LinearAttention op produces correct output matching the naive reference implementation'
    - 'Vulkan Attention KVCache page table logic is correct across single-turn and multi-turn inference'
    - 'Vulkan Attention handles variable sequence lengths (1 to 256+) and batch sizes correctly'
    - 'Vulkan-specific test cases are integrated and pass via ./run_test.out'
  artifacts:
    - path: 'test/op/VulkanAttentionTest.cpp'
      provides: 'Vulkan Attention correctness test covering GQA/MHA/MQA, KVCache, variable lengths'
      min_lines: 250
    - path: 'test/op/VulkanLinearAttentionTest.cpp'
      provides: 'Vulkan LinearAttention correctness test with naive reference comparison'
      min_lines: 150
  key_links:
    - from: 'test/op/VulkanAttentionTest.cpp'
      to: 'source/backend/vulkan/buffer/execution/VulkanAttention.cpp'
      via: 'RuntimeManager with MNN_FORWARD_VULKAN'
      pattern: 'MNN_FORWARD_VULKAN'
    - from: 'test/op/VulkanLinearAttentionTest.cpp'
      to: 'source/backend/vulkan/buffer/execution/VulkanLinearAttention.cpp'
      via: 'RuntimeManager with MNN_FORWARD_VULKAN'
      pattern: 'MNN_FORWARD_VULKAN'
---

<objective>
Create Vulkan-specific correctness tests for the Attention and LinearAttention ops. Each test computes a CPU reference result using existing naive implementations, runs the same computation through the Vulkan backend, and compares outputs within float tolerance. Tests cover all supported attention configurations: GQA (Grouped Query Attention with group=8), MHA (Multi-Head Attention with group=1), MQA (Multi-Query Attention with kvHeadNum=1), KVCache multi-turn page table logic, and variable sequence lengths.

Purpose: Establish a correctness baseline for the 1505-line VulkanAttention and 243-line VulkanLinearAttention implementations that currently have zero automated tests. The tests serve as the verification mechanism for VULK-01 through VULK-04.
Output: Two new test files under test/op/, auto-picked up by run_test.out glob build.
</objective>

<execution_context>
@/Users/khurley/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/khurley/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@test/op/AttentionTest.cpp (existing CPU test \u2014 study reference implementation, KVMeta usage, makeAttentionModule pattern, test class structure, and MNNTestSuiteRegister registration)
@test/op/LinearAttentionTest.cpp (existing CPU test \u2014 study NaiveLinearAttention reference forward() and test structure)
@test/MNNTestSuite.h (study MNNTestCase base class, MNNTestSuiteRegister macro, and pStaus.forwardType for backend selection)
@test/TestUtils.h (study checkVectorByRelativeError for float comparison and verification helpers)
@test/core/BackendTest.cpp (study multi-backend dispatch pattern with MNNGetExtraRuntimeCreator)
@source/backend/vulkan/buffer/execution/VulkanAttention.cpp (entire file \u2014 1505 lines, study constructor, onEncode, onBeforeExecute to understand test requirements)
@source/backend/vulkan/buffer/execution/VulkanAttention.hpp (understand GpuParam, KVCache, and the interface)
@source/backend/vulkan/buffer/execution/VulkanLinearAttention.cpp (entire file \u2014 243 lines, study constructor and onEncode dispatch patterns)
@.planning/codebase/TESTING.md (study test patterns, registration conventions, and run_test.out invocation)
</context>

<tasks>

<task type='auto' tdd='false'>
  <name>Task 1: Create VulkanAttentionCorrectnessTest (VULK-01, VULK-03, VULK-04, VULK-05)</name>
  <files>
    test/op/VulkanAttentionTest.cpp
  </files>
  <read_first>
    - test/op/AttentionTest.cpp (entire 1041-line file: study _makeAttentionModule pattern, KVMeta struct usage, _computeAttentionExpr CPU reference, generateRandTensor helper, and existing test class structure for GQA/MHA/MQA configurations)
    - test/MNNTestSuite.h (study MNNTestCase, MNNTestSuiteRegister macro, and how forwardType controls backend dispatch)
    - test/TestUtils.h (study checkVectorByRelativeError signature and usage)
    - test/core/BackendTest.cpp lines 272-294 (study MNNGetExtraRuntimeCreator pattern for checking Vulkan backend availability)
    - source/backend/vulkan/buffer/execution/VulkanAttention.cpp lines 598-985 (onEncode) and lines 987-1488 (onBeforeExecute) \u2014 understand what inputs the op expects and how outputs are produced
  </read_first>
  <action>
    Create test/op/VulkanAttentionTest.cpp with a test class VulkanAttentionCorrectnessTest that runs Attention on the Vulkan backend and compares output against a CPU reference implementation.

    **Test structure** (follow existing AttentionTest.cpp patterns exactly):

    1. Include headers: MNNTestSuite.h, TestUtils.h, the standard MNN expr headers, core/OpCommonUtils.hpp, and stdlib/vector/stdio.

    2. Guard with #ifdef MNN_SUPPORT_TRANSFORMER_FUSE (the Vulkan attention code is gated behind this flag).

    3. Define a KVMeta struct identical to the one in AttentionTest.cpp lines 27-62 (needed by the attention Module).

    4. Define a _makeVulkanAttentionModule() function that mirrors _makeAttentionModule from AttentionTest.cpp but forces Vulkan backend:
       - Create attention OpT with OpType_Attention and kv_cache=true (same as AttentionTest.cpp lines 65-87)
       - Use MNN::ScheduleConfig with config.type = MNN_FORWARD_VULKAN
       - Set backendConfig.precision = BackendConfig::Precision_High and backendConfig.memory = BackendConfig::Memory_High
       - Create RuntimeManager with the config
       - Set KVCACHE_INFO hint and ATTENTION_OPTION hint (attentionMode=8)
       - Return Module::load

    5. Define a CPU reference function _computeAttentionCPU_naive that mirrors _computeAttentionExpr from AttentionTest.cpp lines 110-157: uses Express API with _Reshape, _Transpose, _MatMul, _Softmax to compute standard scaled dot-product attention. This runs on CPU backend.

    6. Create test class VulkanAttentionCorrectnessTest : public MNNTestCase:
       - virtual bool run(int precision) override
        - In run(): first check Vulkan backend availability via MNNGetExtraRuntimeCreator(MNN_FORWARD_VULKAN); if nullptr, log a warning via MNN_ERROR or MNN_PRINT indicating Vulkan is not available, then return true (skip \u2014 per D-02, Vulkan-unavailable environments must produce a visible diagnostic, not a silent pass)
       - Run test cases covering:

       **Case: GQA (Grouped Query Attention)**
       - NumHead=16, KvNumHead=2, HeadDim=128, pastLength=101, queryLen=16
       - Generate random Q/K/V tensors using generateRandTensor pattern (AttentionTest.cpp lines 159+)
       - Run through Vulkan module, run through CPU reference
       - Compare outputs via checkVectorByRelativeError with rtol=0.01

       **Case: MHA (Multi-Head Attention)**
       - NumHead=16, KvNumHead=16 (group=1), HeadDim=128, pastLength=101, queryLen=16
       - Same comparison pattern

       **Case: MQA (Multi-Query Attention)**
       - NumHead=16, KvNumHead=1, HeadDim=128, pastLength=101, queryLen=16
       - Same comparison pattern

       **Case: KVCache Multi-Turn**
       - Run 3 successive forward passes with cumulative KVCache (gMeta struct tracks previous/add/remove)
       - Each turn: queryLen=8, accumulating pastLen over turns
       - Verify output at each turn matches CPU reference with the same KVCache state

       **Case: Variable Sequence Lengths**
       - Test queryLen = 1, 8, 32, 128 with NumHead=16, KvNumHead=2, HeadDim=128
       - Each length run independently (fresh KVCache)

       Return true if all cases pass.

    7. Register: MNNTestSuiteRegister(VulkanAttentionCorrectnessTest, 'op/vulkan/attention_correctness');

    8. Use TEST_RANDOM_SEED for deterministic default runs (per D-04: fixed seed for reproducibility). Variable (unseeded) data is acceptable as a secondary coverage mode to catch dimension-specific bugs, but the default test invocation must produce consistent results.
  </action>
  <verify>
    <automated>test -f test/op/VulkanAttentionTest.cpp && grep -c 'MNNTestSuiteRegister.*VulkanAttentionCorrectnessTest' test/op/VulkanAttentionTest.cpp && grep -c 'MNN_FORWARD_VULKAN' test/op/VulkanAttentionTest.cpp</automated>
  </verify>
  <acceptance_criteria>
    - test/op/VulkanAttentionTest.cpp exists, compiles, and registers as 'op/vulkan/attention_correctness'
    - File guarded by #ifdef MNN_SUPPORT_TRANSFORMER_FUSE (must match Vulkan attention compilation gate)
    - Test function run() checks Vulkan backend availability via MNNGetExtraRuntimeCreator; when unavailable, logs warning via MNN_ERROR/MNN_PRINT (per D-02) then returns true (skip)
    - Test covers GQA (group=8), MHA (group=1), MQA (kvHeadNum=1) configurations
    - Test covers KVCache multi-turn with 3 successive forward passes
    - Test covers queryLen values: 1, 8, 32, 128
    - All outputs compared via checkVectorByRelativeError with rtol <= 0.01 against CPU reference
    - Uses TEST_RANDOM_SEED for deterministic default runs (per D-04); variable data tested as secondary coverage mode
  </acceptance_criteria>
  <done>VulkanAttentionCorrectnessTest created with GQA/MHA/MQA, KVCache multi-turn, and variable sequence length test cases</done>
</task>

<task type='auto' tdd='false'>
  <name>Task 2: Create VulkanLinearAttentionCorrectnessTest (VULK-02, VULK-05)</name>
  <files>
    test/op/VulkanLinearAttentionTest.cpp
  </files>
  <read_first>
    - test/op/LinearAttentionTest.cpp (entire 472-line file: study NaiveLinearAttention struct with init/forward, the forward() reference implementation using Conv1D+SiLU+Split+QKV+GQA+L2Norm+GatedDeltaRule, and the test class structure)
    - source/backend/vulkan/buffer/execution/VulkanLinearAttention.cpp (entire 243-line file: study constructor pipeline creation, onEncode dispatch order, and descriptor set binding indices)
    - test/op/AttentionTest.cpp lines 65-91 (study _makeAttentionModule pattern for creating Vulkan-specific modules from OpT)
    - test/MNNTestSuite.h (study registration pattern)
    - test/TestUtils.h (study checkVectorByRelativeError)
  </read_first>
  <action>
    Create test/op/VulkanLinearAttentionTest.cpp with a test class VulkanLinearAttentionCorrectnessTest that runs LinearAttention on the Vulkan backend and compares output against the NaiveLinearAttention CPU reference from LinearAttentionTest.cpp.

    **Test structure:**

    1. Include the same headers as LinearAttentionTest.cpp + MNNTestSuite.h + TestUtils.h.
    2. Guard with #ifdef MNN_SUPPORT_TRANSFORMER_FUSE.

    3. Copy the NaiveLinearAttention struct from LinearAttentionTest.cpp lines 28-178 as a private helper within the test file. This provides the CPU reference implementation (Conv1D + SiLU + QKV split + GQA + L2Norm + Gated Delta Rule).

    4. Create _makeVulkanLinearAttentionModule() that:
       - Creates OpT with OpType_LinearAttention
       - Sets LinearAttentionParam: attn_type='gated_delta_rule', num_k_heads, num_v_heads, head_k_dim, head_v_dim, use_qk_l2norm
       - Creates RuntimeManager with config.type = MNN_FORWARD_VULKAN
       - Returns Module::load

    5. Create test class VulkanLinearAttentionCorrectnessTest : public MNNTestCase:
        - run(int precision) checks Vulkan backend availability via MNNGetExtraRuntimeCreator(MNN_FORWARD_VULKAN); if nullptr, logs warning via MNN_ERROR/MNN_PRINT (per D-02) then returns true (skip)
       - **Test case 1: Basic Gated Delta Rule**
         - batch=1, convDim=128, K_conv=3, L=8, numKHeads=2, numVHeads=8, headKDim=64, headVDim=64, useL2Norm=true
         - Generate random qkv [B, D, L], gate [B, L, H], beta [B, L, H], convW [D, 1, K]
         - Run CPU reference via NaiveLinearAttention::forward()
         - Run Vulkan module via module->onForward({qkv, gate, beta, convW})
         - Compare outputs with checkVectorByRelativeError rtol=0.02

       - **Test case 2: Without L2Norm**
         - Same config but useL2Norm=false

       - **Test case 3: Variable sequence length**
         - L=1 and L=16 with same head dimensions

       Return true if all cases pass.

    6. Register: MNNTestSuiteRegister(VulkanLinearAttentionCorrectnessTest, 'op/vulkan/linear_attention_correctness');
  </action>
  <verify>
    <automated>test -f test/op/VulkanLinearAttentionTest.cpp && grep -c 'MNNTestSuiteRegister.*VulkanLinearAttentionCorrectnessTest' test/op/VulkanLinearAttentionTest.cpp && grep -c 'MNN_FORWARD_VULKAN' test/op/VulkanLinearAttentionTest.cpp</automated>
  </verify>
  <acceptance_criteria>
    - test/op/VulkanLinearAttentionTest.cpp exists, compiles, and registers as 'op/vulkan/linear_attention_correctness'
    - File guarded by #ifdef MNN_SUPPORT_TRANSFORMER_FUSE
    - NaiveLinearAttention struct copied from existing test file provides CPU reference with Conv1D+SiLU+Split+QKV+GQA+L2Norm+GatedDeltaRule
    - Test covers: basic gated delta rule (L=8, useL2Norm=true), without L2Norm, variable sequence length (L=1, L=16)
    - All outputs compared via checkVectorByRelativeError with rtol <= 0.02 against CPU reference
    - Test checks Vulkan backend availability via MNNGetExtraRuntimeCreator; when unavailable, logs warning via MNN_ERROR/MNN_PRINT (per D-02) then returns true (skip)
  </acceptance_criteria>
  <done>VulkanLinearAttentionCorrectnessTest created with CPU reference comparison covering gated delta rule, L2Norm toggle, and variable lengths</done>
</task>

<task type='auto' tdd='false'>
  <name>Task 3: Build and run Vulkan tests, fix any bugs (VULK-01 through VULK-05)</name>
  <files>
    test/op/VulkanAttentionTest.cpp
    test/op/VulkanLinearAttentionTest.cpp
    source/backend/vulkan/buffer/execution/VulkanAttention.cpp
    source/backend/vulkan/buffer/execution/VulkanLinearAttention.cpp
  </files>
  <read_first>
    - test/CMakeLists.txt (verify auto-glob picks up new test files; confirm no CMake edits needed)
    - test/op/VulkanAttentionTest.cpp (the file just created in Task 1)
    - test/op/VulkanLinearAttentionTest.cpp (the file just created in Task 2)
  </read_first>
  <action>
    Build the test binary with Vulkan support, then run the new Vulkan attention tests. If tests fail due to correctness bugs in VulkanAttention.cpp or VulkanLinearAttention.cpp, fix the bugs in those source files and re-run until all tests pass.

    **Build command:**
    mkdir -p build && cd build && cmake .. -DMNN_BUILD_TEST=ON -DMNN_VULKAN=ON -DMNN_SUPPORT_TRANSFORMER_FUSE=ON && make -j24 run_test.out

    **Run command:**
    ./build/run_test.out 'op/vulkan/'

    Note: run_test.out auto-globs all .cpp under test/ (including the new test/op/VulkanAttentionTest.cpp and test/op/VulkanLinearAttentionTest.cpp). No CMakeLists.txt modification needed.

    If tests fail with numerical mismatches:
    - Check VulkanAttention.cpp onBeforeExecute: verify mask buffer binding indices match shader expectations
    - Check VulkanAttention.cpp onEncode: verify descriptor set buffer bindings align with GLSL layout(binding=N)
    - Check VulkanLinearAttention.cpp: verify dispatch dimensions and descriptor set writes match shader expectations
    - Fix any bugs found, rebuild, re-run

    If tests fail with 'skip' (Vulkan backend not found):
    - Ensure Vulkan runtime is available (Vulkan SDK/drivers installed)
    - Verify cmake found Vulkan: check build/CMakeCache.txt for Vulkan_INCLUDE_DIR and Vulkan_LIBRARY

    All tests must PASS (not skip) on a system with Vulkan support.
  </action>
  <verify>
    <automated>cd build && ./run_test.out 'op/vulkan/' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - run_test.out builds successfully with MNN_VULKAN=ON and MNN_SUPPORT_TRANSFORMER_FUSE=ON
    - 'op/vulkan/attention_correctness' test executes and reports PASSED
    - 'op/vulkan/linear_attention_correctness' test executes and reports PASSED
    - No compilation errors in VulkanAttention.cpp, VulkanLinearAttention.cpp, or the test files
    - If any correctness bugs were found and fixed in source files, those fixes are committed with descriptive messages
  </acceptance_criteria>
  <done>Both Vulkan attention tests build and pass via run_test.out, with any revealed correctness bugs fixed in the Vulkan backend source files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPU reference result -> Vulkan GPU result | Test compares float tensors across device boundaries; GPU rounding differences may cause false negatives |
| Test generation -> Vulkan backend | Random test data exercises code paths that may not be covered by LLM model weights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | VulkanAttentionTest comparison threshold | mitigate | Use rtol=0.01 for Attention (float precision) and rtol=0.02 for LinearAttention (multi-step accumulation). Thresholds match existing MNN test conventions from ConvolutionCommonTest |
| T-02-02 | Information Disclosure | Test output logging | accept | Test output contains only tensor statistics, no model weights or PII |
| T-02-03 | Denial of Service | Large tensor allocation in tests | accept | Max test dimensions (queryLen=128, HeadDim=128) are bounded and fit in typical GPU VRAM; allocate via RuntimeManager which manages lifecycle |
</threat_model>

<verification>
Build and test:
  mkdir -p build && cd build && cmake .. -DMNN_BUILD_TEST=ON -DMNN_VULKAN=ON -DMNN_SUPPORT_TRANSFORMER_FUSE=ON && make -j24 run_test.out && ./run_test.out 'op/vulkan/'

Expected: output shows PASSED for both test entries, no FAILED entries.
</verification>

<success_criteria>
- VulkanAttentionCorrectnessTest passes all 5 configuration scenarios (GQA, MHA, MQA, multi-turn KVCache, variable lengths)
- VulkanLinearAttentionCorrectnessTest passes all 3 configuration scenarios (basic, no-L2Norm, variable lengths)
- All 8 Phase 1 requirements (VULK-01 through VULK-05) are verifiably met by passing tests
- Zero correctness bugs remain in VulkanAttention.cpp or VulkanLinearAttention.cpp
</success_criteria>

<output>
Create .planning/phases/01-vulkan-attention-correctness-llm-e2e/01-02-SUMMARY.md when done
</output>
